.tools/
node_modules/
.netlify/
**/.netlify/

# Local env files — may hold secrets (Supabase keys, etc.). Never commit.
# Ignore every .env variant but keep the committed .env.example template.
.env
.env.*
!.env.example

# Backlog local-sync cache — pulled from Supabase / written by the backlog scripts,
# not source. Only the README in backlog/ is tracked.
backlog/*
!backlog/README.md

# QA report output — generated per run, not committed.
# Exception: a11y.json is a build-time data dependency of the a1-web
# Accessibility page (statically imported), so it must be tracked.
reports/*
!reports/a11y.json
# Playwright test artifacts — generated per run, not committed
test-results/
# Storybook build output — generated per run, not committed
storybook-static/
# Walkthrough capture/narration artifacts — generated per run, not committed
walkthroughs/artifacts/
walkthroughs/narration/
# visual-baselines/ is intentionally tracked (via Git LFS)
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.ds-sync/
ds-bundle/
