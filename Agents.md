# A1 Design System — Agent Instructions

> **Do not add content directly to this file.**
> All project context, policies, and reference material belongs in `packages/react/ai/`.
> Add a link below when you create or update a file there.

---

Read and follow the central project context before starting any task:

@packages/react/ai/project-context.md

For repository structure, tokens, themes, labels, and shared system foundations:

@packages/react/ai/project-foundations.md

For CSS rules, component workflows, package rules, and invariants:

@packages/react/ai/project-workflows.md

Before asking whether a component exists or where it is implemented, check the component registry:

@packages/react/ai/components.md

For build commands and key file locations:

@packages/react/ai/quick-orientation.md

For the accessibility check policy and a11y commands:

@packages/react/ai/a11y-policy.md
