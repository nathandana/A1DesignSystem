[build]
  command = "cd ../.. && npm ci && npm run build:priority-guide"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
