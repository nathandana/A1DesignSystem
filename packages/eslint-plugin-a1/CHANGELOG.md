# eslint-plugin-a1 Changelog

## Unreleased

- **Package release notes** — ESLint plugin changes are now tracked separately from the app and component package release notes so rule consumers can review package-specific updates.

