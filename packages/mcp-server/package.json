{
  "name": "@gtivr4/a1-design-system-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Remote MCP server exposing the A1 Design System's components, tokens, rules, and page-definition contract to AI coding agents.",
  "type": "module",
  "scripts": {
    "build:mcp-index": "node scripts/build-index.mjs",
    "dev": "cd ../.. && netlify dev --cwd packages/mcp-server --port 5190",
    "netlify:build": "cd ../.. && npm ci && npm run build:tokens && npm run build:mcp-index --workspace=@gtivr4/a1-design-system-mcp"
  },
  "dependencies": {
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
