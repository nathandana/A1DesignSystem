# A1 Design System — Component Maintenance Log

Dated log of component, token, theme, label, rule, and a1-web changes. **Newest at the top.**
Split out of `components.md` to keep the registry focused — add an entry here whenever you change a
component API, variant, token, theme, label, rule, feature, or a1-web behaviour (this is part of the
component change checklist in `project-context.md`).

| Date | Change |
|------|--------|
| 2026-07-24 | **A1-Web release baseline (A1-2064):** added a release-blocking Playwright suite driven by A1-Web's runtime route registry. Every production route and component/blog example receives a load check, viewport visual baseline, uncaught-error check, and axe-core scan with zero critical or serious findings allowed. A representative default/Accessible/Heritage matrix covers xs–xl. Added root test/update/release commands, made npm publishing depend on the gate, and documented version, changelog, blog, simplified release-note, deployment-smoke, monitoring, and rollback steps in `apps/a1-web/RELEASE_CHECKLIST.md`. |
| 2026-07-24 | **Content standards Foundation page and overview:** added Foundations → Content → Standards, backed directly by the canonical shipped Markdown guide so the public page and package documentation cannot drift. Added translated page metadata for every supported locale. Simplified the Foundations overview to A–Z navigation cards with a title and short description preview; removed the checklist content without adding custom styling or interaction. |
| 2026-07-24 | **Foundations menu order:** kept Overview pinned first and sorted the six Foundations navigation groups A–Z (Content, Figma, Layout, Standards, Visual, Visualize). Child pages remain alphabetized within each group, and the Virtual Information Architect menu mirror uses the same order. |
| 2026-07-24 | **Iconography Foundation runtime fix:** imported the existing A1 `Code` component used in the page description, preventing the `Code is not defined` crash. Removed the always-mounted global search field's ignored placeholder while retaining its existing translated accessible label, removing the development warning without adding copy or custom styling. |
| 2026-07-24 | **Content standards (A1-2063):** added a canonical public content guide covering voice, grammar, sentence case, punctuation, numerals, dates and times, phone numbers, addresses, interface copy, accessibility, and localization. The current AP Stylebook is the default unless an explicit A1 override applies. Added matching AI guidance and linked both standards from the central project and shipped React documentation indexes. Documentation only; no linting, templates, validators, or prompt enforcement. |
| 2026-07-24 | **A1 web 0.30.0 and React 0.29.0 release preparation:** cut the accumulated Unreleased changelog entries into dated release sections, synchronized workspace versions and package-lock metadata, restored missing simplified public release summaries, added release-focused blog coverage, and introduced a Figma Foundations category with plugin documentation plus a placeholder for embedded component views. |
| 2026-07-24 | **Build with AI virtual engineer:** added a Codex-backed planning route with work classification, manual direction, blocking questions, and ponytail-guided minimal plans. CSS/styling guidance is omitted for unrelated ticket types; the existing local/deterministic planner remains the fallback. |
| 2026-07-24 | **Build with AI virtual engineer review:** the engineer action is now one click like Virtual PO, evaluates the ticket, surfaces clarifying questions inline, and appends guidance to the existing plan rather than replacing it. |
| 2026-07-24 | **Backlog virtual team:** development ticket dialogs now combine Product Owner and virtual-engineer actions into one Virtual team tab with medium-sized buttons; production keeps the Build with AI tab. |
| 2026-07-24 | **Virtual Product Owner context (A1-341):** Build with AI now carries ticket comments and Q&A into both local and deterministic plans, warns when the Product Owner review has not run or questions remain unanswered, and mirrors Product Owner questions/comments in the Virtual PO panel with inline answers. Discussion entry rendering is shared between Activity and Virtual PO. |
| 2026-07-24 | **Virtual Product Owner Supabase compatibility (A1-341):** added the checked-in `backlog_items.reviews` migration and a backward-compatible cloud update retry. Older workspaces can now apply Codex/deterministic clarifying questions even before the optional review-stamp column is migrated; once the migration runs, review signatures persist normally. |
| 2026-07-24 | **Dispatch theme:** added a new warm editorial theme (`system/themes/dispatch/`, selector `.a1-theme-dispatch`). Cream paper (`surface.page` #FAF7F4) + ink (`text.default` #252525); a single azure-blue ramp drives both `accent` (buttons) and `info` (links), anchored at #0A76B4 — one step deeper than the source #088CD4 — so white button labels and blue link text both clear WCAG AA on the cream; `warn` becomes a highlighter yellow (#E9E642) with a dark-ink `status.warn.foreground` for the "important" badge; `neutral` warmed to greige (100 = #E6E3E0 soft-grey secondary button / raised). Secondary button overridden to a soft-grey solid fill (not the default outline). Fonts: display **Zilla Slab** (300), heading + button **Geist** (600), body **Atkinson Hyperlegible** (400); base radii bumped (md 0.5rem). Built via `npm run build:tokens` (13 theme files, no path-validation errors). Registered in a1-web `appThemes.ts` (label "Dispatch"); Zilla Slab + Geist added to `apps/a1-web/index.html` webfonts (Atkinson already present). Live example `examples/dispatch/` reproduces the source mock with real components; validated light-mode at desktop + xs via Playwright. Light-mode only. **a11y-validated:** analytical WCAG contrast matrix (every text/link/button/badge pair ≥ AA) plus empirical axe-core (`@axe-core/playwright`, WCAG 2.0/2.1/2.2 A+AA) on a full themed component gallery — **0 colour-contrast violations across 265 elements**. Testing drove three fixes: muted/strong-neutral darkened (`neutral.500` #6E675F → **#635C54**) so muted text clears AA on the raised surface; links shifted one step deeper (`link.color` → `info.600` = #0A6197) so link body text clears AA on every surface including raised; and **51 component-tier tokens** that bake the default accent (purple) / info (blue) literal at build time — links, field/action-tile/chart focus rings, chip/tab/tree-menu selected states, switch track+thumb, slider, status-bar + circular-progress fills, heading mark, card hero/icon/nav-border, card/chart status-info, read-only field, info subtle-badge border — were remapped to the azure ramp via a `customProperties` block in the theme override (the same component-token-baking gotcha the `wireframe` theme documents; overriding the base ramps alone does not reach them). |
| 2026-07-24 | **Figma Autocomplete (A1-1401):** created the Autocomplete page — an `Autocomplete` control set (`882:7620`, `Size=compact\|default\|comfortable` × `State=default\|error\|disabled`, 9 variants mirroring the Select field family; `Label`/`Value`/`Hint`/`Error message` TEXT props + `Required`/`Show value`/`Show hint` BOOLEANs) plus a separate `Autocomplete Menu` listbox composition (`886:17`: plain/active/selected+check/colour-swatch/multi-checkbox rows, a `Menu/Section label` group heading, and the bold "Add …" create row reusing the field/menu/`shadow/md` tokens). `multiple` (chips) and `variant="color"` (swatch) ship as example frames; a Dark Color-mode frame validates dark. All fills/strokes bind shared Color vars, geometry binds the `field/*` collection, radii `radius/sm\|control\|lg`, labels `Field/Label/{SM,MD,LG}`, value `body/{sm,md}`, hint/error `body/xs`, options `body/sm`. Component keys: set `70b9bc426a6780ba5bd66f8a034b1c35876b6c43`, menu `8376317911f324eaace29e9baca95b56e4c79fb1`. Code Connect `packages/figma/code-connect/Autocomplete.figma.ts`; `components.md` Autocomplete Figma coverage flipped to ✓ and a1-web `data.js` coverage → `['React','Figma']`; `figma-workflow.md` gained the Autocomplete section + pages row. Options/selection/create/grouping/portal/keyboard/ARIA remain runtime-owned; JSON-bridge is a follow-up (a1-json plugin mid-refactor on this branch). |
| 2026-07-16 | **A1:Figma Convert to Tree Menu:** added Tree Menu to Fix > Convert. Selected visible text, including text inside selected containers, now converts into a real A1 `Tree Menu` component with nested `items`; horizontal indentation is used as a best-effort depth signal, the first item becomes `selectedId`, and parent rows are added to `expandedIds`. |
| 2026-07-16 | **A1:Figma Tree Menu bridge:** added Tree Menu support to the plugin JSON model. Build can add the `Tree Menu` component, JSON import/export/update round-trips `TreeMenu.props.variant`, `showExpandControls`, `draggable`, nested `items`, `selectedId`, and `expandedIds`, and audit/export treats Tree Menu Item rows as supported A1 implementation children. a1-web's Tree Menu detail page now exposes `jsonType`/`toJson`/`fromJson`, and the all-new-components fixture includes a nested TreeMenu example. |
| 2026-07-16 | **a1-web Dialog close default:** Dialog detail/JSON preview now treats omitted `showClose` as enabled, matching the JSON contract default and generating/passing `onClose={() => setOpen(false)}` unless `props.showClose` is explicitly `false`. |
| 2026-07-16 | **Choice Group JSON bridge + embedded-Grid detection:** the A1:Figma plugin now round-trips `ChoiceGroup` with real React props — export reads legend/required/helper and the Options slot's Choice Option tiles (label, subtext, icon, disabled; selected → `defaultValue` string/array; checkbox Type → `multiple`; first-tile density → `size`); import/update reconciles tiles (1–20) and applies per-tile type/state/size/subtext/icon. **Embedded Grids are detected**: a Grid frame inside the Options slot (native GRID layout or a responsive plugin Grid with `{xs:n, md:n}` name/plugin-data metadata) exports its columns as the `columns` prop, reconciliation appends new tiles inside it, and apply syncs responsive metadata back via `syncResponsiveGridColumnsMetadata` (a `columns` value with no embedded Grid warns). Fixed `stripResponsiveGridColumnsName` to also strip dashless hand-authored suffixes (`Grid {xs:2}`), preventing suffix accumulation on re-sync. Registries/aliases/JSON-type map updated (manifest keys were already captured). a1-web: the choice-group configurator gained `jsonType`/`toJson`/`fromJson` (columns map between the config's string/`auto` form and numeric/responsive props; selection isn't in the config, so `defaultValue` renders in the playground/editor only). Showcase JSON gained a ChoiceGroup with responsive columns. Verified: 13 stubbed bridge checks (grid detection, reconcile-inside-grid, metadata sync, selection/disabled, columns-without-grid warning) and Playwright probes — the playground renders legend + required marker, Starter checked from `defaultValue`, and the disabled tile. |
| 2026-07-16 | **A1:Figma selected property auto-export:** the plugin now snapshots the selected A1 instance's component properties and polls for sidebar property changes, so Dialog controls such as `Show close` refresh the JSON even when Figma does not emit a useful document-change event. |
| 2026-07-16 | **A1:Figma Dialog close fallback detection:** Dialog export now falls back to the visible close affordance layer when Figma does not expose the `Show close` boolean through `componentProperties`. Hidden `Close` / `Close button` / close Icon Button layers now still emit `props.showClose: false`. |
| 2026-07-16 | **Figma Choice Group:** created the Choice Group page with a `Choice Option` set (`762:124`, `Type=radio|checkbox` × `State=default|selected|disabled` × `Size=compact|default|comfortable` = 18 tile variants — 8px-radius field-surface cards, top-start selection indicator (circle/rounded square, filled with an action-foreground dot/check when selected), selected = action surface + 2px accent border, disabled = raised surface at 50%; densities on the 8/12/16 padding · 12/16/20 indicator · 20/24/32 icon scale; new `Choice/Label {compact,default,comfortable}` weight-500 and `Choice/Subtext compact` body-2xs text styles; Label / Show icon + Icon swap / Show subtext + Subtext properties) and a `Choice Group` shell (`762:125`: legend + required marker, wrapping equal-width tile row, helper text; Label/Required/Helper properties). `multiple`, `columns`, `sections`, `inlineIcon`, and error/success messages are documented runtime gaps. Dark mode validated. Choice Group added to the fill sizing contract; manifest keys captured for the set, shell, and four text styles (code.js + `a1-library-manifest.json`). Code Connect template `ChoiceGroup.figma.ts`; registry/coverage flips + `figma-workflow.md` section and pages-table row. JSON bridge support is a follow-up. |
| 2026-07-16 | **A1:Figma Dialog close visibility:** Dialog export/import now resolves the close affordance boolean from the current and likely published Figma property names (`Show close`, `Show close button`, `Close button`, or `Close`) so `props.showClose: false` is emitted when the close control is hidden. Footer visibility received the same tolerant boolean resolver. |
| 2026-07-16 | **Dialog body as slot JSON:** Dialog now treats its body as a real content slot in the Figma→JSON→a1-web path. The A1:Figma plugin exports the Dialog Body Slot as `Dialog.children` using the same ordered free-content traversal as Card/Section, with `props.body` retained only as a legacy plain-text fallback. The a1-web Dialog preview/adapter now renders renderer-provided `children` inside the Dialog body instead of always replacing body content with one Paragraph. |
| 2026-07-16 | **a1-web Dialog `footerActions` handoff:** fixed the JSON playground/page-renderer path for Dialog footer buttons. The renderer already converted `Dialog.props.footerActions` into a rendered `footer`, but the editor/detail Dialog adapter ignored `config.footer`; the Dialog preview now accepts renderer-provided footer content while still using `footerActions` for configurator/JSON-detail previews. |
| 2026-07-16 | **A1:Figma Dialog footer export:** Dialog footer export now detects Button instances inside native footer slots, named footer/action frames, and nested footer wrappers such as Button Containers. Exported footer Buttons are emitted as `Dialog.props.footerActions`, scoped to footer/action ancestors so close buttons and body buttons are not captured, and Button export warnings are preserved. |
| 2026-07-16 | **A1:Figma Inline bridge:** added Inline to the A1:Figma Build quick-add menu and JSON bridge as `Inline`. The plugin now recognizes the A1 Inline asset, imports/exports inline markdown/text through `content.fallback`, preserves an optional `props.inlineElement` selector when the Figma component exposes it, and the a1-web Inline detail page now exposes `jsonType` / `toJson` / `fromJson` for JSON playground/configurator flows. The checked-in library manifest includes an `Inline` entry with its component key pending if the published Figma key is exported later. |
| 2026-07-16 | **A1:Figma Breadcrumb + Code bridge:** added Breadcrumb and Code to the A1:Figma Build quick-add menu and JSON bridge. Breadcrumb now round-trips `Breadcrumb.props.items` plus optional `backLabel`; Code now round-trips block/inline metadata where the Figma asset exposes it and stores the actual code string in `content.fallback`. The component detail pages now expose `jsonType` / `toJson` / `fromJson` for both components so a1-web JSON playground/configurator flows use the same contract. The checked-in manifest includes `Breadcrumb` and `Code` entries with keys pending if the published Figma component keys are exported later. |
| 2026-07-16 | **A1:Figma Dialog Body Slot children:** Dialog JSON bridge now supports arbitrary supported A1 content in the Dialog Body Slot. Local slot children export as `Dialog.children`, import/update renders `children` back into the body slot, and `props.body` remains the simple text fallback when no children are supplied. |
| 2026-07-16 | **A1:Figma Bottom Sheet bridge:** added Bottom Sheet to the A1:Figma Build quick-add menu and JSON bridge as `BottomSheet`. The plugin now recognizes the A1 `Bottom Sheet` asset, imports/exports title, default detent preview, and ordered content-slot children, and a1-web's JSON renderer preserves real BottomSheet children instead of showing only the detail-page sample preview. The checked-in library manifest includes a `Bottom Sheet` entry with its key pending until the newly published Figma component key is exported. |
| 2026-07-16 | **Chip `selected` fix in `selectionMode="none"` groups (React):** the user's Figma-exported selected chip rendered unselected in the playground — `ChipGroup` passed an explicit `__selected: false` to every chip in non-selectable rows (`isSelectedValue` returned `false` for none mode), clobbering the chip's own `selected` prop through the `__selected ?? selected` fallback. None mode now returns `undefined` (the group has no selection opinion there), so filter/menu chips honor their own `selected` — this also un-breaks the existing "Filters" story, which had silently rendered unselected. The chip configurator preview additionally passes `selected` per item in none mode. Verified via Playwright on both the playground (verbatim adapter path) and the configurator handoff: the reported five-chip row renders All selected + Sort by caret + Archived disabled on both surfaces. |
| 2026-07-16 | **Chip bridge fix — per-item flags instead of group inference:** a Figma Chip row with one caret chip exported as `behavior: "menu"`, which a1-web renders as a single representative filter chip — the whole row collapsed (user-reported). ChipGroup items now carry **`selected` / `disabled` / `menu`** per item: export reads each chip's State and caret (never inferring the group `behavior`, which is a runtime selection semantic), and apply honors explicit item states, falling back to the first-chip demo selection only when no flags are present. a1-web chip configurator: per-item **Selected / Disabled / Menu chip** toggles in the item editor, the preview renders per-item menus/disabled states and re-syncs its demo selection when JSON arrives after mount, and the code snippet emits `disabled` / `menu={...}` per chip. Showcase ChipGroup updated to the mixed row (selected + icons + menu chip + disabled). Verified: 18/18 stubbed bridge checks (incl. caret-stays-per-item and explicit-selection cases) and a Playwright probe of the exact reported design — 5 chips render with group label, All selected, Sort by caret, Archived disabled. |
| 2026-07-16 | **A1:Figma Build Add Chip:** Chip is now exposed in the Build > Quick Add component list. The plugin uses the existing checked-in A1 library manifest keys for `Chip` / `Chip Group`, adds `chip` / `chip-group` to the Add target resolver, applies starter Chip props on insertion, and keeps the page JSON bridge consistent by exporting/adding a single Chip as a one-item `ChipGroup`. |
| 2026-07-16 | **Chip + Data Table hooked into the JSON bridge:** the A1:Figma plugin now round-trips **ChipGroup** (export: group label, first-chip size, ordered `Chip slot` items with icons, any caret ⇒ `behavior: "menu"`; import/update: slot reconciliation 1–12 via new `GROUP_SLOT_CONFIG.ChipGroup`, first-chip selected for single/multiple; a lone Chip exports as a one-item ChipGroup) and **DataTable** (export: columns with label/sortable/end-align + `defaultSort` from the sorted header, rows from visible cell values; import/update: clamped onto the fixed 4×4 grid with visibility reconciliation, >4 warns, last visible row drops its hairline). Manifest keys added for Chip / Data Table Header Cell / Data Table Cell (sets), Chip Group / Data Table (components), and the four `Chip/*` text styles — in both the code.js manifest and `a1-library-manifest.json`. Name aliases + `JSON_TYPE_BY_COMPONENT_NAME` entries added. a1-web: the chip configurator gained a **Group label** control (ChipGroup `label` prop now exposed; Preview/snippet updated) and `jsonType`/`toJson`/`fromJson`; the data-table configurator accepts JSON **`rows`** (replacing the editable sample set) and gained its JSON view. Showcase JSON gained ChipGroup + DataTable nodes. Verified: 16 stubbed bridge checks (export shapes, slot growth, selection, 4×4 clamping, hairline move) and a Playwright playground render (chips + label + selection, table headers with sort glyphs, row values). |
| 2026-07-16 | **A1:Figma audit A1-coverage gate:** Audit scoring now includes explicit A1 coverage. Styled A1 text and real/supported A1 components count as coverage; selections with zero A1 components and zero A1 text styles get a blocker finding and a hard score cap of 55, preventing fully custom/non-A1 pages from receiving high grades just because their findings are repetitive. Large text-only A1-styled selections without components are also capped below an A to reflect missing structural conversion. |
| 2026-07-16 | **Figma Chip + Chip Group:** created the Chip page with a `Chip` set (`752:61`, `Size=sm|md|lg` × `State=default|selected|disabled` — pills on the 28/40/56 total-height standard; transparent + `border/default` outline, selected binds the action background/foreground, disabled at the 55% button-disabled opacity; new `Chip/{sm,md,lg}` weight-500 text styles; `Label`, `Show icon` + `Icon` swap, `Show caret` = `expand_more` menu-chip affordance) and a `Chip Group` component (`752:62`, wrapping md chip row with 6px gaps under an optional compact muted label — new `Chip/Group label` style — with `Label`/`Show label` properties). Selection behavior, menu contents, navigation chips, and `wrap={false}` are documented runtime gaps. Dark mode validated. `ChipGroup` added to the fill sizing contract (chips hug inside; doc + plugin `FILL_CONTAINER_TYPES`). Code Connect template `Chip.figma.ts`; registry/coverage flips + `figma-workflow.md` section and pages-table row. JSON bridge support is a follow-up. |
| 2026-07-16 | **A1:Figma Tabs panel-content bridge:** Tabs export now preserves the Figma `folder` variant and supports nearby content markers like `{tab=Dashboard}`. A frame/Section/Stack/Grid in the same containing area whose marker matches a tab label/id/value is exported into that tab's `items[].children` panel content, and A1-web's Tabs adapter now renders those real panel children instead of generic sample copy. Audit flags Tabs with no connected panel content and Help documents the marker convention. |
| 2026-07-16 | **A1:Figma audit category filters:** Audit metric rows now act as in-plugin issue filters. Clicking A1 components, Color values, Text styles, Element support, Figma components, or AutoFix narrows the Issues list to the matching audit findings, with click-again toggle behavior and an active category state. Findings now carry metric keys from the audit walker so component color overrides can appear under both A1 components and Color values. |
| 2026-07-16 | **A1:Figma audit severity scoring:** Audit grading now scores issue families by severity instead of raw finding count. Missing/unmapped A1 components carry the largest impact, unsupported export layers are major findings, repeated hygiene findings like many unbound text colors collapse into one scored pattern with a small repetition multiplier, and minor spacing/token advisories have capped low-grade impact. The in-plugin Audit report is now a flatter white inspector panel, and issue cards show severity/category metadata plus repeated-pattern counts. |
| 2026-07-16 | **A1:Figma Icon JSON bridge:** standalone Material Symbols instances now export as A1 `Icon` nodes (`props.name`, with nearest A1 icon size inferred from rendered dimensions), import/update by looking up the glyph name in the current file or enabled A1 library icon page, and appear as supported/exportable selections. Individual Material glyphs remain convention-based and are intentionally not listed one-by-one in the A1 library manifest. |
| 2026-07-16 | **A1:Figma Page Nav bridge:** added Page Nav support to the plugin using the published A1 library `Page Nav` component and `Page Nav Item` component-set keys. Build can add Page Nav, Fix can convert selected text to Page Nav, JSON import/export/update round-trips `PageNav.props.label` and up to five `sections` rows, and audit treats Page Nav Item as a supported private implementation component. |
| 2026-07-16 | **A1:Figma icon audit support:** audit now recognizes nested Material icon instances/layers named Icon or Nav icon, plus icon-like A1 implementation component names, as supported A1 implementation details instead of flagging them as unsupported component instances. This is audit-only and does not make arbitrary standalone icon components exportable JSON nodes. |
| 2026-07-16 | **A1:Figma Radio Group helper visibility:** Radio Group import now applies the source A1 component's `Show helper` boolean when JSON supplies `props.hint`, and export only emits `hint` when helper text is visible. This keeps the plugin aligned with the updated Radio Group component property contract. |
| 2026-07-16 | **A1:Figma form conversions:** Fix > Convert now includes Text Field, Search Field, Textarea, Select, Radio Group, and Checkbox Group. Form conversions infer labels, values, hints, and choice options from selected visible text, create the real A1 Figma component, preserve placement, and skip converting when the selection is already inside that A1 form component. |
| 2026-07-16 | **A1:Figma Heading/Body conversion alignment:** Convert to Heading and Convert to Body now explicitly preserve each source text layer's left, center, or right text alignment when applying A1 text styles. Justified text now reports that A1 Heading/Body does not support justify and falls back to left alignment instead of silently changing. |
| 2026-07-16 | **A1:Figma text AutoFix heading inference:** unstyled/free text AutoFix now classifies each text layer individually using visible font size, weight, title-like copy length, and layer names such as Heading/Title/H1 before choosing Heading/Display versus Body. Bulk AutoFix all Text no longer collapses every free text layer to Body by default. |
| 2026-07-16 | **A1:Figma AutoFix all Text:** Fix mode now includes an AutoFix all Text action that walks the selected frame/group/section/text subtree, skips component instances and implementation layers, and applies the existing Heading/Body/Link text AutoFix to eligible free text layers only. |
| 2026-07-16 | **A1:Figma audit issue action polish:** in-plugin audit issue actions now use Pure small button sizing and remain hidden until the issue card is hovered or focused, keeping the report list scannable while preserving keyboard access. |
| 2026-07-16 | **A1:Figma audit ignore markers:** Audit now skips any layer/subtree whose layer name contains `ignore` and reports skipped ignored-layer counts in the in-plugin report summary. Audit issue rows now include Select and Ignore actions; Ignore renames the linked Figma layer with ` [A1 ignore]` so future audits omit it without creating plugin-only state. |
| 2026-07-16 | **A1:Figma Stack/Grid gap variable binding:** Stack and Grid conversion/import/update/AutoFix now awaits A1 gap variable imports from the checked-in/stored library manifest before audit runs, binding `gap/*` float variables to Stack item/wrap spacing and Grid row/column gaps when available. Converted Stack layers also carry visible direction/wrap metadata in the name, e.g. `Stack - {direction:row, wrap:false}`. |
| 2026-07-16 | **A1:Figma Tab audit support:** Audit now treats the A1 `Tab` / `Tab Item` child component as a supported private Tabs implementation component instead of an unsupported instance. Known A1 library text styles from the manifest, including `Tab/Label`, now count as valid A1 text styles and no longer trigger body-style AutoFix recommendations when used inside A1 components. |
| 2026-07-16 | **A1:Figma zero-gap audit fix:** Stack/Grid audit and AutoFix now treat `0` gap as a valid literal no-space value that does not need a Figma gap variable binding. Zero item spacing, wrap spacing, row gap, and column gap no longer produce “uses an A1 value but is not bound to a Figma gap variable” findings or missing-variable warnings. |
| 2026-07-16 | **A1:Figma Audit tab:** split Audit into its own top-level plugin mode. The left rail now has separate Audit selection and Print report actions: Audit renders an in-plugin report with score, metric rows, and all findings; Print report keeps the existing canvas report-card behavior. In-plugin findings carry linked Figma node ids, and clicking a finding selects/scolls to the matching layer or nearest selectable parent. |
| 2026-07-16 | **A1:Figma slot-bound conversion roots:** conversion preparation now stops at native Figma `SLOT` boundaries. Converting a frame/Stack/Grid or other selected content inside a Section/Page/Card slot no longer promotes the selection to the host A1 component or its internal part instance, so Convert to Grid/Stack/Section works on the actual selected slot content. |
| 2026-07-16 | **A1:Figma Section conversion child sizing:** Convert to Section now normalizes each moved Section Content Slot child after append: fill/stretch width, hug vertical sizing, and no inherited layout grow. This prevents content moved from Stack/Grid or other auto-layout parents from inheriting fill-height behavior and collapsing to 1px inside the Section. |
| 2026-07-16 | **A1:Figma hidden text ignored in conversions:** shared conversion text collection now skips layers with `visible === false`, including hidden container subtrees. Heading/Body, Button, Switch, Link, Figure caption, Definition Item, and Tabs conversions no longer infer labels from hidden implementation/helper text. |
| 2026-07-16 | **A1:Figma Tabs item-slot conversion:** Convert to Tabs now treats the exact `Tabs` slot/layer as the Tab item container. Selected text layers, or text layers inside a selected frame/group, become Tab labels; the selected source is replaced by the Tabs component. The plugin no longer tries to move selected content into a Tabs panel slot for this Figma asset. |
| 2026-07-16 | **A1:Figma warning compaction:** plugin-to-UI result messages now de-duplicate repeated warnings, append occurrence counts such as `(12×)`, and cap long unique warning lists with a summary line. Export, import, conversion, AutoFix, breakpoint, and linked-page statuses keep the same findings without flooding the right-panel status area. |
| 2026-07-16 | **A1:Figma Convert to Tabs hardening:** Convert to Tabs now prefers the exact `Tabs` item slot before falling back to the existing Tab Item parent, and infers tab labels from selected text layers or nested text inside selected containers. This prevents unrelated body copy outside the selection from creating extra tab items. |
| 2026-07-16 | **A1:Figma Tabs item reconciliation:** Tabs import/convert can append or remove Tab Item instances in the exact `Tabs` item slot when JSON supplies a different item count from the current component default. Panel children are intentionally not represented by this Figma Tabs asset. |
| 2026-07-16 | **A1:Figma Convert to Tabs:** added Tabs to the Audit & Fix conversion palette. Convert to Tabs now creates the real A1 Tabs component through the manifest-backed resolver, seeds tab labels from selected text when available, defaults to Overview/Details otherwise, and applies fill-width sizing in layout parents. |
| 2026-07-16 | **A1:Figma A1-only component resolution:** Add/Convert component lookup now refuses same-name local or external components unless their published component/component-set key matches the checked-in A1 Design System manifest. The resolver imports known A1 manifest keys before scanning local instances, ignores non-A1 library matches such as another design system's `Link`, and lets the checked-in manifest override any stale client-storage keys for known A1 components. |
| 2026-07-16 | **A1:Figma slot text conversion:** Convert to Heading/Body now supports text selected inside native Figma component slots. Slot nodes are allowed as text-conversion roots, and text inside a slot is replaced with a freshly imported A1-styled text node in the same position instead of relying on in-place style mutation across slot/instance boundaries. Canvas and ordinary frame text still use the faster in-place conversion path. |
| 2026-07-16 | **A1:Figma Build contextual add:** Build > Add now inserts components relative to the current selection. Selecting a Page Layout, Section, Card, or other slot-backed instance adds into its content slot; selecting a normal frame/group/section adds inside that container; selecting leaf content adds the new component immediately after the selected layer. Empty selection still adds on the page near the viewport/default canvas location. |
| 2026-07-16 | **A1:Figma Card fill sizing:** added shared fill-width normalization for converted/added Cards. Convert to Card now forces the resulting Card to Fill/stretch/grow when its parent is auto-layout or Grid, even if the source frame was fixed/hug. Build > Add Card applies the same fill-width rule after placement. |
| 2026-07-16 | **A1:Figma library manifest:** added `packages/figma/plugins/a1-json/a1-library-manifest.json` as the checked-in reference for the published A1 Design System Figma library. The plugin runtime now mirrors that manifest for component set/component imports and can export a full maintainer manifest containing component keys, text style keys, and color/float variable keys. Heading/Body/Link styling now attempts published-manifest text style and color variable imports before enabled-library name search, while still refusing to create local styles or variables in consumer files. |
| 2026-07-16 | **A1:Figma no local style/token creation:** removed the plugin paths that created local text styles, color variables, spacing variable collections, or gap variables in consumer files. Heading/Body/Link conversion and JSON text rendering now only apply existing/imported A1 text styles and color variables; missing assets are reported instead of recreated. Stack/Grid gap binding now uses existing spacing variables only and otherwise leaves pixel values with a warning. |
| 2026-07-15 | **A1:Figma Convert to Section fill sizing:** converted Sections now run through the shared converted-layout fill helper after their content moves into the Section slot. In auto-layout or Grid parents, Convert to Section sets the new Section to Fill/stretch/grow as appropriate instead of inheriting a narrower selected layer width. |
| 2026-07-15 | **A1:Figma Convert respects A1 instance boundaries:** Convert actions no longer climb past a registered A1 component instance when the selected layer is inside its slot/content. Selecting content inside a Page Layout and choosing Convert to Section now wraps only the selected content, while custom/non-A1 selected instances can still be detached before conversion. |
| 2026-07-15 | **A1:Figma built-in library registry:** populated the plugin's checked-in A1 Figma component key registry from the published A1 Design System file so consumer files can import real Page Layout, Top Header, Section, Card, and other A1 components by key without requiring users to seed an instance or run Help → Export component keys first. Tabs and Menu are now registered as standalone component keys, private slot item sets such as Tab/Menu/Radio/Checkbox/Segmented/Top Header Nav items are included, and resolver/help copy now treats key export as a maintainer refresh tool rather than user setup. |
| 2026-07-15 | **A1:Figma no component fallbacks:** removed the plugin's missing-component placeholder renderer and native fallback component builders. JSON render, Build > Add, and component conversions now require the actual A1 Figma component/importer to resolve; failures report the resolver/import error instead of creating local mock frames. Definition List Item no longer has an older-library fallback frame, Section and Button Container import no longer detach to host children when native slots are missing, and Convert to Figure now fails unless the selection contains an actual image fill. |
| 2026-07-15 | **A1:Figma component-only Convert flow:** Page Layout, Section, Card, and Definition List Item conversions no longer generate editable native fallback frames when component lookup fails. Convert now requires the published A1 component instance, reports resolver errors through the conversion result/status, and AutoFix-all catches per-target component resolution failures without aborting unrelated fixes. Card/Page Layout candidate detection now enables conversion attempts even when the component is only available through async library/key import. |
| 2026-07-15 | **A1:Figma mixed numeric selection crash fix:** hardened Audit/AutoFix selection heuristics against Figma mixed values. `cardSuggestion`, Stack/Grid suggestions, Grid export, Grid span export, and responsive breakpoint sync now read numeric Figma properties through a safe helper instead of calling `Number(...)` directly on values that may be `figma.mixed`/Symbol. This fixes `TypeError: cannot convert symbol to number` when selecting frames with mixed stroke/padding/grid values. |
| 2026-07-15 | **A1:Figma audit report component-only rendering:** removed the plugin-owned backup renderer for audit report cards. Audit now inserts or updates only the published design-system `A1 Audit Report Card` component; if a previous plain report frame exists, the plugin replaces it with the real component instance after that instance resolves. If the report card component cannot be resolved from local/seed/key/library lookup, Audit reports the resolver error instead of generating a local frame or component. |
| 2026-07-15 | **A1:Figma Audit Report Card source component repair:** fixed the published Figma `A1 Audit Report Card` component property contract in the A1 Design System file. The component already had the expected `Title`, `Score`, `Summary`, metric, finding, and recommendation properties, but several visible text layers were not bound to those properties and many fallback layer names were generic. Bound `Title`, `Score`, all six metric rows, findings, and recommendation rows to the proper component properties and renamed the layers to the plugin contract names. Smoke-tested a temporary instance to confirm property updates now populate score, metric counts, findings, and recommendation visibility correctly. |
| 2026-07-15 | **A1:Figma audit report design-system component:** Audit report rendering no longer creates a reusable `A1 Audit Report Card` component/template inside every consumer file. The report now uses the published design-system `A1 Audit Report Card` component through the normal local/seed/key/import resolver. Existing report instances are updated in place and the component key export registry now includes `A1 Audit Report Card` as a standalone component candidate. |
| 2026-07-15 | **A1:Figma text/menu/dialog fallback hardening:** Heading/Body/Link import and conversion now bootstrap missing local A1 text styles (`Body/*`, `Heading/*`, `Display/*`, `Link/*`) and text color variables (`color/text/default`, `color/text/muted`, `color/text/accent`, `link/color`) when they are absent, so blank/consumer Figma files still get semantic text styling instead of raw detached typography. Component lookup now refuses standalone components for known A1 component-set names, preventing Build > Menu from resolving the Material Symbols `menu` icon. Added editable local fallbacks for Blockquote, Menu, and Dialog, and fixed the Menu Item variant typo. |
| 2026-07-15 | **A1:Figma robust Add fallback:** Build > Add no longer requires access to the original/source A1 Figma library. It still prefers real library instances when local components, seed instances, saved component keys, or published imports are available, but if a package component cannot be resolved it now inserts an editable local fallback (Page Layout, Top Header, Section, Card, and styled text where supported) or a visible missing-component placeholder instead of failing before placement. This makes consumer files usable even when external library component import is unavailable. |
| 2026-07-15 | **A1:Figma external-library key registry:** corrected the consumer-file import path for published A1 Figma components. The plugin now prefers exact `figma.importComponentSetByKeyAsync` / `figma.importComponentByKeyAsync` lookups through checked-in `A1_FIGMA_COMPONENT_SET_KEYS` / `A1_FIGMA_COMPONENT_KEYS` plus a locally saved `clientStorage` registry instead of relying on unavailable name-search APIs in the current Figma plugin runtime. Help mode includes **Export component keys**, which should be run from the source A1 Figma library file once to save the registry and generate a code snippet; consumer files can then add real Page Layout, Top Header, Section, Card, and other library instances without needing a seed instance. Failure copy now explains missing keys instead of only telling users to enable the library. |
| 2026-07-15 | **A1:Figma external library permission:** added Figma manifest `teamlibrary` permission to the A1:Figma plugin. Without it, `figma.teamLibrary.*` calls throw `teamlibrary permission not specified`, so consumer files could not import real A1 components from the enabled external design-system library even though local/source-file components worked. |
| 2026-07-15 | **A1:Figma direct package Add:** changed Build > Add from JSON starter rendering to direct design-system component instantiation. Add now calls `createComponentInstance()` for the requested A1 package component (Page Layout, Top Header, Section, Card, etc.), applies starter props, and places the actual instance; Heading/Body remain styled text and Stack/Grid remain native layout frames. This avoids fallback rendering when the user's intent is to insert the right package variant. |
| 2026-07-15 | **A1:Figma component-name aliases:** added resolver aliases between JSON/React names and likely published Figma library names (`Top Header`/`TopHeader`, `Page Layout`/`PageLayout`, `Badge`/`MessageBadge`, `Text Field`/`TextField`, `Icon Button`/`IconButton`, etc.). Local/imported/library component lookup now uses these aliases and reports which aliases were tried when no enabled library component matches. Body text style lookup also accepts `Paragraph/*` style families as aliases for `body/*`. |
| 2026-07-15 | **A1:Figma Add failure reporting:** hardened Build > Add so failures no longer hang silently on "Adding…". Add now catches render-time failures before placement and reports the exact error in plugin status. Native fallback text creation now loads a usable font before setting characters (Figma throws if text changes before font load), and Heading/Paragraph/Link have fallback text renderers so Page Layout/Section/Card starter content can still appear even when external text styles fail. |
| 2026-07-15 | **A1:Figma resilient Add/Convert fallbacks:** when foundational A1 Figma component sources cannot be resolved from a consumer file's external library, Add/render now creates editable native fallback layers for Card, Section, Top Header, and Page Layout instead of a missing-component placeholder. Convert to Card/Section/Page Layout also falls back to wrapping selected content in native A1-compatible frames and reports the missing source in plugin status, keeping Build/Audit workflows usable while the external-library resolution is diagnosed. |
| 2026-07-15 | **A1:Figma text add/convert hardening:** made Heading/Body add and Convert work more reliably in consumer Figma files. Text style lookup now tolerates library naming differences such as `heading/md`, `Heading MD`, and foldered `A1/Heading/MD`; imported text still receives a visible A1-sized fallback if the external style cannot be applied. Convert now detaches the outermost selected instance ancestor before scanning text, so selecting text inside a non-A1/external component can still become A1 Heading or Body. |
| 2026-07-15 | **A1:Figma external component-set imports:** expanded external-library component resolution to try Figma team-library component-set imports before individual component variants, then fall back to variant imports. Matching now considers more library metadata fields (`componentSetName`, parent/set names, nested `.name` values), improving Quick Add/render support for Card, Section, Top Header, Page Layout, and other A1 assets in files that consume the A1 design system as an external library. |
| 2026-07-15 | **A1:Figma external-library name matching:** hardened A1 library component resolution for consuming Figma files where components come from the external A1 design system library. The resolver now scores published component names by exact match, first/last path segment, foldered names, and set-name metadata, so names like `Button`, `A1/Button`, or `Button / Variant=primary` can all resolve to the requested A1 component. Missing-component errors now explicitly tell users to enable the external A1 library or place an A1 instance in the file. |
| 2026-07-15 | **A1:Figma consumer-file library resolution:** made the plugin resolve A1 assets from consuming Figma files instead of only the source library file. Component lookup now recognizes component sets behind already-imported library instances, and JSON render/Quick Add/import paths can import missing components from enabled Figma libraries by name. Text AutoFix also attempts to import matching text styles and color variables from enabled libraries, with suffix matching for foldered style names like `A1/heading/md`. |
| 2026-07-15 | **Figma Data Table (default density):** created the Data Table page with `Data Table Header Cell` (`Sort=none|unsorted|ascending|descending` × `Align=start|end`; panel surface, new `Data Table/Header` text style = body-sm at weight 500, muted→default on sort, `unfold_more` 44% / arrow 72% opacity per data-table.css) and `Data Table Cell` (`Align=start|end`, body-sm, bottom hairline) sets, composed into a 640px `Data Table` shell (`728:38`) with the scroll-container chrome (1px subtle border, 8px radius, clipped) and a 4×4 sample; the last row drops its hairline via instance stroke overrides. Default density only by request — compact/comfortable, zebra, selection, search, pagination, notices, column types, and mobile cards are documented runtime gaps. Dark mode validated. `DataTable` added to the fill sizing contract (doc + plugin `FILL_CONTAINER_TYPES`). Code Connect template `DataTable.figma.ts`; registry/coverage flips + `figma-workflow.md` section and pages-table row. JSON bridge support is a follow-up. |
| 2026-07-15 | **A1:Figma header simplification:** removed the plugin's custom in-frame `A1:Figma` header and close button now that the native Figma plugin window chrome supplies that UI. The mode segmented control is now the first row, reclaiming vertical space for the workbench. |
| 2026-07-15 | **A1:Figma Publish panel status/layout polish:** removed sticky overlay positioning from the plugin status card so warnings no longer overlap the JSON editor, and moved the Playground Link into the Publish action button stack above the JSON workspace. |
| 2026-07-15 | **A1:Figma breakpoint sync root targeting:** Sync Breakpoints / Create breakpoint previews now resolves a selection to the outermost page-level design root before cloning or syncing. Selecting a child Grid/Card/text node inside a breakpoint preview no longer causes new previews to be inserted into that child’s section or local parent. |
| 2026-07-15 | **A1:Figma status placement:** moved plugin status/warning output into the bottom of the right-side detail panel instead of the global shell. Long export/audit warning lists now stay in a capped, scrollable status card so they no longer take over the entire plugin interface. |
| 2026-07-15 | **A1:Figma Publish preview merge:** combined the separate Preview mode into **Publish** so page publishing and Playground preview live in one Preview & Sync flow. Publish now uses Pure Switch styling for the default-on Playground sync toggle and the A1 Live Sync toggle; turning Playground on opens/updates the local playground handoff and enables live selection export, while the old Preview controls and JSON workspace are folded into the Publish panel. |
| 2026-07-15 | **A1:Figma segmented mode workbench:** rebuilt the plugin chrome around the Pure segmented-control styling for top-level Audit & Fix, Build, Preview, Publish, and Help modes, with a persistent contextual sidebar on the left and detail panels on the right. Existing bridge controls keep their IDs; descriptive helper copy moved out of work panels into Help; Build owns categorized Quick Add component buttons for every supported addable Figma bridge component plus responsive breakpoint tools; and Audit & Fix now has a Convert-only workflow with contextual Responsive Grid controls. |
| 2026-07-15 | **A1:Figma breakpoint Grid sync hardening:** breakpoint sync now resolves each Grid's active breakpoint from its containing breakpoint root/ancestor before applying responsive columns, then refreshes the Grid's own breakpoint metadata. Cloned grids no longer keep stale primary-breakpoint column counts when syncing xs/sm/md/lg/xl preview roots. |
| 2026-07-15 | **A1:Figma Build breakpoint tools restored:** restored the responsive breakpoint controls inside the new **Build** tab after the Playground→Preview / Build tab split. Build now exposes primary breakpoint selection, breakpoint checkboxes, Render JSON previews, Create breakpoint previews, Sync breakpoints, and Export responsive diff, all wired to the existing breakpoint bridge handlers. |
| 2026-07-15 | **A1 Grid span contract + Figma bridge:** promoted `GridItem` into the A1 page JSON/editor contract as the sanctioned direct child of `Grid` for `span` / `rowSpan` placement. The React Grid package now exports a shared `gridItemSpanClassNames` utility, a1-web can render `GridItem` nodes, and A1:Figma imports/exports native Figma Grid child spans as `GridItem` wrappers while preserving scalar and sparse responsive span props. Grid export also now serializes detected gaps explicitly, using semantic A1 gap keys when possible. |
| 2026-07-15 | **A1:Figma responsive Grid name sync:** responsive Grid dropdown changes now target the exact selected Grid node id that revealed the controls, force the visible layer-name contract to update after metadata writes, and report the updated Grid layer name in the plugin status. This keeps `Grid - {xs:1, md:2, xl:4}` in sync when users change breakpoint column values from the plugin UI. |
| 2026-07-15 | **A1:Figma Convert/Add segmented workflow:** replaced the Audit panel's static "Convert to" heading with a two-state segmented control: **Convert** keeps the existing selection-aware conversion workflow, while **Add** uses the same component button grid to insert a fresh starter A1 component near the current selection or viewport without moving/replacing selected content. Add mode supports Page Layout, Section, Card, Stack, Grid, Heading, Body, Button, Switch, Pagination, Definition List Item, Link, and Figure, and reuses the existing JSON importers where possible. |
| 2026-07-15 | **A1:Figma responsive Grid controls:** replaced the temporary Convert to Grid prompt with a contextual Audit-mode responsive Grid control shown directly below the Convert to buttons only when a Grid is selected. Users can choose compact per-breakpoint column counts (`xs`/`sm`/`md`/`lg`/`xl`, with `--` for omitted breakpoints); changing any dropdown auto-applies the visible `Grid - {xs:1, md:2, xl:4}` layer-name contract plus plugin metadata to the selected Grid. Convert to Grid also reads those values when creating or normalizing a Grid. |
| 2026-07-15 | **A1:Figma Convert to Stack/Grid chrome cleanup:** Convert to Stack and Convert to Grid now strip decorative container chrome from the converted layout frame, including fills/backgrounds, strokes/borders, effects, radius/corner values, and related local style bindings. This applies both to newly wrapped content and to already-selected Stack/Grid frames normalized through Convert to. |
| 2026-07-15 | **A1:Figma Convert to Grid breakpoint definition:** Convert to Grid now seeds responsive Grid breakpoint metadata immediately. Newly converted or normalized Grid frames get the visible layer-name contract such as `Grid - {xs:1, md:2, lg:4}`, matching plugin data, and the current preview breakpoint is stored so export preserves the intended responsive `props.columns` instead of flattening back to a fixed column count. |
| 2026-07-15 | **A1:Figma plugin mode structure:** added a dedicated **Build** tab for responsive breakpoint authoring/sync controls, renamed **Playground** to **Preview**, renamed **Page Editor** to **Live Edit**, and made the Audit-mode Convert to palette a three-column button grid for easier scanning. Build and Live Edit now receive taller plugin window presets while Audit/Preview keep the compact default. |
| 2026-07-15 | **A1:Figma Tabs JSON bridge:** added Tabs to the plugin's supported JSON import/export/update path. The bridge imports and exports `Tabs` with `props.items`/`props.tabs`, selected `value`, `variant`, `size`, `level`, `labelMode`, `equalHeight`, and tab icons/counts/statuses/icon positions. Missing Tabs or Tab Item assets still render as visible placeholders instead of disappearing. |
| 2026-07-15 | **A1:Figma responsive Grid layer names:** responsive Grid column maps can now live directly in the Figma layer name using the visible convention `Grid - {xs:1, md:2, lg:4}`. The plugin reads that suffix before hidden plugin data, writes it when rendering responsive Grid JSON, keeps it in sync while applying breakpoint previews, and exports/syncs it back to `props.columns` for responsive JSON handoff. |
| 2026-07-15 | **A1:Figma Convert to Definition Item:** replaced the Audit-mode Convert to Definition List action with **Definition Item**. The action now creates the reusable `Definition List Item` Figma component directly, infers the first label/value pair from selected `Label: Value` text or paired text layers, and leaves full `DefinitionList` JSON import/export unchanged so items still round-trip when placed inside a Definition List Items Slot. |
| 2026-07-15 | **A1:Figma Convert to Stack/Grid fill sizing:** Convert to Stack and Convert to Grid now set the converted container to Fill horizontally when the target has an auto-layout or Grid parent. Existing Stack/Grid frames normalized through the same Convert actions are also switched to Fill, while page-level conversions no-op safely because there is no fill-capable parent. |
| 2026-07-15 | **Figma Top Header nav icon tokens:** rebound the `Top Header Nav Item` component-set icon vectors so default-state icons use `color/text/muted` and hover/active icons use `color/text/default`, matching the nav label state colors documented in `figma-workflow.md`. Expanded the Figma `color/text/default` variable scope to include `SHAPE_FILL` so Material icon vectors can use the same mode-aware text token instead of raw `#000000`. Validated the child set has zero remaining raw black nav icon fills. |
| 2026-07-15 | **A1:Figma audit broken color variables:** Audit now validates that a paint's bound color variable still resolves in Figma instead of accepting any `boundVariables.color` object. Broken/stale Button color bindings, like a flagged `button/secondary/*` variable reference, now count as missing color values and are included in supported component override AutoFix/reset detection. |
| 2026-07-15 | **A1:Figma Convert to Definition List:** added Definition List to the Audit-mode Convert to palette. Convert to Definition List creates an A1 Definition List component, derives rows from selected `Label: Value` text or paired text layers when possible, and falls back to a single editable row when the source content is ambiguous. |
| 2026-07-15 | **A1:Figma gap variables + Convert to Pagination:** Stack and Grid normalization/import now creates and binds local Spacing collection FLOAT variables for supported A1 gaps (`gap/none`, semantic `gap/xs`–`gap/xl`, and numeric-only `gap/{n}` values) instead of leaving raw pixel gap values. Stack AutoFix treats unbound-but-correct gaps as fixable; Grid frames are now included in Audit/AutoFix gap checks and bind row/column gaps independently. Added Pagination to the Audit-mode Convert to palette; Convert to Pagination replaces the selected visual chunk with the A1 Pagination component. |
| 2026-07-15 | **A1:Figma Convert to Stack/Grid normalization:** Convert to Stack and Convert to Grid now normalize an already-selected auto-layout/grid frame in place instead of reporting no-op. The action renames the layer to `Stack` or `Grid` and snaps Stack/Grid spacing to the nearest supported A1 spacing value. New converted Stack/Grid wrappers also explicitly keep the normalized layer names after creation. |
| 2026-07-15 | **A1:Figma Convert to Stack/Grid + text fill:** added Stack and Grid to the Audit-mode Convert to palette. Convert to Stack creates an A1-compatible auto-layout frame, infers row/column direction and nearest A1 gap from the selected layout, and moves selected content inside. Convert to Grid creates a Figma Grid frame, infers a simple column count/gap, and moves selected content into grid cells. Heading/Body conversion now also sets converted text layers to Fill horizontally when their parent layout supports it. |
| 2026-07-15 | **A1:Figma Convert to Switch + Figure fill:** added Switch to the Audit-mode Convert to palette. Convert to Switch creates a default unchecked A1 Switch and preserves visible/component-property text as the label. Convert to Figure now forces copied image fills to Figma `Fill` scale mode so source images do not retain Fit/Crop/Tile modes. |
| 2026-07-15 | **A1:Figma Audit Detach All:** added an Audit-mode **Detach All** utility that repeatedly scans the selected roots and detaches component instances until no nested instances remain. This handles Figma components whose nested instances only become detachable after the parent instance is detached. The action reports count/warnings in the plugin UI, selects the final detached roots, and triggers the normal auto-export after successful detaches. |
| 2026-07-15 | **A1:Figma Convert to component detach:** all Convert to actions now run a shared preparation step that detaches selected top-level component instances before conversion, so their visible children/text can be moved into the new A1 component. If the selected instance is already the requested A1 target component, it stays attached and the converter reports that no conversion is needed. |
| 2026-07-15 | **A1:Figma Convert to Button simplified:** Convert to Button no longer runs visual variant/size inference or icon recovery. For speed and predictability it now creates a md secondary A1 Button, preserves visible/component-property text as the label, and skips icon conversion entirely. |
| 2026-07-15 | **A1:Figma Convert to source check:** Convert to Button, Link, and Card now only treats a selected instance as already A1 when its actual Figma component source matches the A1 component/set source. Same-named custom or external components continue through conversion, and their internal text can be used as the converted Button/Link label instead of being skipped by name alone. |
| 2026-07-15 | **A1:Figma Convert to direct text fallback:** Heading and Body conversions now operate on directly selected text layers even when the text sits inside editable component/slot structures that the generic wrapper conversion intentionally skips. Button and Link conversions now fall back to replacing directly selected layers from the same parent, so selected text inside a card/section can become a real A1 Button or Link; selecting an existing A1 Button/Link reports that it is already converted instead of replacing it with a generic instance. |
| 2026-07-15 | **A1:Figma Convert to palette:** replaced the single Convert to Section control with an Audit-mode **Convert to** palette for the common authoring targets: Page Layout, Section, Card, Heading, Body, Button, Link, and Figure. Page Layout moves page content into the `Page Content Slot`, applies a selected Top Header when present, and sets the nearest breakpoint preview from the source width; Section/Card move selected content into native slots; Heading/Body normalize selected text layers to A1 text styles; Button/Link create A1 component instances from selected text/content; and Figure creates an A1 Figure from the selected image fill when one is available. The plugin now routes conversions through a generic `convert-to` message so more components can be added later without new UI plumbing. |
| 2026-07-15 | **A1:Figma Convert to Section content width:** Convert to Section now infers the new Section's `contentWidth` from the selected content bounds, falls back to the selected frame width when needed, chooses the nearest A1 content-width mode (`xs`/`sm`/`md`/`lg`/`xl`/`2xl`), and defaults to `lg` when no useful measurement is available. |
| 2026-07-15 | **A1:Figma Convert to Section:** added an explicit **Convert to Section** action in Audit mode. It wraps selected sibling layers in a new A1 `Section` component, or replaces a selected frame/group/native Figma section with an A1 Section and moves that container's children into the `Section Content Slot`. The action reports back even when nothing changes, preserves placement as closely as the parent layout allows, selects the new Section, and schedules the normal JSON auto-export after a successful conversion. |
| 2026-07-15 | **A1:Figma AutoFix component style overrides:** supported component instances with internal raw/unbound visible fill or stroke overrides are now included in AutoFix target collection. Batch AutoFix resets only the affected internal override nodes (via Figma `resetOverrides()` when available) so a manually recolored Card icon tile returns to the component's token-bound styling without wiping content/slot overrides. |
| 2026-07-15 | **A1:Figma Audit supported-instance overrides:** Audit no longer blindly trusts every supported component instance. It now scans supported instance internals for visible solid fills/strokes that are not bound to A1 color variables and reports them as component deviations without treating implementation layers as standalone export content. This catches cases like a manually recolored Card icon tile while keeping native slots/content audited separately. |
| 2026-07-15 | **A1:Figma Page Layout AutoFix:** authored plain frames that contain a real Top Header instance plus page content are now detected as Page Layout candidates in Audit/AutoFix. Contextual AutoFix converts the frame to the `Page Layout` component, applies the detected Top Header configuration to the nested Page Layout header, maps the source frame width to the nearest breakpoint preview (`xs`/`sm`/`md`/`lg`/`xl`) and applies it to both Page Layout and Top Header, moves remaining children into the Page Content Slot, and reports a status message even when no change is applied. Batch AutoFix runs Page Layout conversion before nested Card/Stack/text repairs and now includes Page Layout targets in its counts. |
| 2026-07-15 | **A1:Figma Audit report-card resilience:** audit report instances now update through both exposed component properties and a direct named-layer fallback. If a customized `A1 Audit Report Card` component loses some text/visibility property bindings while being styled, the plugin still updates the visible score, metric counts, findings, and recommendation rows by the instance's named/structured layers. Missing bindings no longer spam report warnings. |
| 2026-07-15 | **A1:Figma AutoFix multi-selection:** the Audit tab's primary **AutoFix selection** button is now batch-aware. When multiple selected layers contain fixable A1 issues, it routes to the batch AutoFix flow instead of only supporting the single contextual target. **AutoFix all** is visible whenever the selected set has two or more fixable targets, and both controls report the number of pending fixes. |
| 2026-07-15 | **A1:Figma Audit mode + report card:** renamed the plugin's user-facing **Fix** mode to **Audit** and added a read-only **Audit selection** action. The audit scores the selected design (or current page if nothing is selected) for A1 compatibility, counting missing color-variable bindings, missing/non-A1 text styles, unsupported visual elements, missing Figma component placeholders, and available AutoFix opportunities. The report shows an A–F grade, score, neutral metric rows with check/X status icons, top findings, and recommended next steps; the score column is width-pinned so it stays visible and long findings/recommendations wrap inside the card. AutoFix remains a separate explicit action. Generated report cards are tagged, reused in place on later audits, ignored by the audit walker, and if the report is the only selected node the plugin audits the page instead. The report card is repositioned 24px to the right of the audited content on each run (page-content bounds when no explicit design is selected). Scoring uses compressed category deductions so one stray custom text layer reports as a modest finding instead of collapsing the whole page grade. |
| 2026-07-15 | **Figma sm breakpoint + Page Layout / Top Header sm variants:** added the `sm` mode to the **Breakpoint** variable collection (all 12 variables filled: `page/width/min` 481 / `max` 640 / `default` 640 per `system/tokens/breakpoint.json`; `section/padding/*` sm values matching the xs/md group; `deviceMode` "sm"; note — Figma has no mode-reorder API, so `sm` is appended after `xl`). Added **Top Header `Breakpoint=sm`** (mobile hamburger composition cloned from xs; bound width resolves to 640 via `page/width/default` + explicit sm mode, inserted between md and xs) and **Page Layout `breakpoint=sm`** (xs clone at 640 with the sm Top Header swapped in and the `deviceMode`-bound slot label resolving to "sm"; xs restored as the set's insert default). The plugin's `exportPageLayout` now warns that a non-xs `breakpoint` is a visual preview width, mirroring the Top Header warning. `figma-workflow.md` updated: the stale Variable collections table now documents the live **Breakpoint** collection (replacing the retired Spacing/Breakpoints/Gap/ContentWidth rows it still listed), and the Top Header + Page Layout sections describe the bound-width + explicit-mode variant pattern at the 375/640/768/1280/1512 default widths. |
| 2026-07-15 | **A1:Figma breakpoint design controls:** the Playground tab now lets users choose a primary breakpoint and the breakpoints they want to design for. Render on canvas honors that selection, Create breakpoints duplicates an existing selected design into chosen breakpoint roots, Sync breakpoints refreshes chosen roots from the primary design and reapplies supported breakpoint state, and Export responsive diff reads rendered breakpoint roots back into JSON by serializing supported Grid column differences. Unsupported differences are surfaced as warnings rather than silently round-tripped. |
| 2026-07-15 | **A1:Figma PageLayout breakpoint variant:** responsive multi-root renders now apply the active breakpoint to the Page Layout component's own `breakpoint` variant before populating its Top Header and Page Content Slot. The rendered `xs`/`md`/`xl` roots therefore show the matching PageLayout breakpoint in Figma's component controls instead of all remaining at the default `xs` state. |
| 2026-07-15 | **A1:Figma authored-breakpoint render roots:** Render on canvas now scans pasted JSON for authored responsive breakpoint keys (`xs`/`sm`/`md`/`lg`/`xl`) and, when more than one is present, renders a separate top-level Figma root for each breakpoint. Each root is named with the breakpoint suffix, stores `a1Breakpoint`, is resized to the shared preview width, and renders under that active context so responsive Grid columns and TopHeader breakpoint variants line up (`xs` no longer accidentally shows an `xl` four-column grid). |
| 2026-07-15 | **A1:Figma responsive Grid focused fixture:** added `packages/figma/plugins/a1-json/examples/responsive-grid.json`, a smaller PageLayout → Section → Grid composition for testing responsive `Grid.props.columns` without the full all-components showcase. It uses `columns: { xs: 1, md: 2, xl: 4 }` with four Card children so the rendered preview visibly changes between 1-up, 2-up, and 4-up breakpoints. |
| 2026-07-15 | **A1:Figma responsive Grid columns POC:** updated the all-new-components showcase so `grid-form-controls` uses `columns: { xs: 2, md: 2, xl: 4 }`. The plugin now preserves sparse responsive Grid columns in frame metadata, renders one Figma preview breakpoint at a time (fresh standalone imports use the `xs` breakpoint to match the current mobile-first TopHeader/PageLayout default; linked roots can supply `a1Breakpoint`), and exports the stored responsive object back to JSON, updating the active breakpoint value if the Figma Grid column count changed. Documented as a proof of concept in `figma-breakpoint-sync-plan.md`, not full multi-root breakpoint sync. |
| 2026-07-15 | **A1:Figma TopHeader action-slot scoping:** fixed a JSON import/render bug where applying `TopHeader.actions` could mutate the mobile/menu Icon Button because the reconciler scanned every nested Icon Button in the Top Header instance. The bridge now reconciles option instances inside the named slot only (`Nav Items` for nav, `Actions` for actions) and the TopHeader applier uses those scoped instances, so the menu icon remains library-owned and never changes when JSON actions are applied. |
| 2026-07-15 | **Pure Segmented Control + A1-styled Figma plugin UI:** added Segmented Control to the Pure package (`.a1-segmented` + `-small`/`-large`/`-full-width`, `.a1-segment`; selection via `aria-checked`/`aria-selected`/`.a1-segment-selected`; 28/40/56 total-height standard with segment heights derived by `calc`; all values from `--component-segmented-*`/semantic tokens) with a new `examples/a1-pure/segmented-control.html` demo page and nav/index updates across the example site — registry Pure column and a1-web coverage flipped. The **A1:Figma plugin UI** is now a Pure-library showcase: new `sync-ui-css.mjs` (+ `npm run figma:plugin:sync-css`) inlines `a1-light.css` + `a1-pure.css` into `ui.html` between generated markers (plus the `--semantic-shadow-*` vars from `build/css/tokens.css` — **known gap:** the a1-light generator omits semantic shadows and stringifies composite shadow tokens as `[object Object]`, though `a1-pure.css` already references `--semantic-shadow-xs`); the mode tabs are a real Pure Segmented Control, all buttons/links/fields/selects/checkboxes use Pure classes on an `.a1-body`, and plugin-local CSS is reduced to token-driven app-shell layout + the status banner (Pure has no Banner classes yet). **Live view behavior:** no longer auto-enables when the Playground tab is selected, no longer hides the editor/controls (the `live-view-active` collapse CSS is gone), and no longer shrinks the plugin window to 132px (`PLUGIN_UI_SIZE.live` removed) — it is a plain checkbox. Verified by Playwright: segmented tabs/selected pill/buttons/textarea/status all Pure-styled at 420×560, live-view toggle hides nothing, and the example page tracks measure 28/40/56. |
| 2026-07-15 | **A1:Figma PageLayout render fix:** the plugin's JSON collector now treats `PageLayout` as a renderable component instead of flattening it as page schema, so `Render on canvas` creates the Page Layout shell and populates its `Page Content Slot`. Update/export mapping now normalizes the Figma `Page Layout` component set to JSON `PageLayout`, and the slot/header lookup tolerates current native slot naming (`Page Content Slot` / `Page content`) plus region-style page-layout payloads. |
| 2026-07-15 | **Figma Page Layout + JSON bridge:** created the `Page Layout` Figma component (`node 690:1251`, own page + dark validation) — a v1 app shell of a fill-width **Top Header instance** above a **`Page Content Slot`** (SLOT property `Page content`; sidebar/aside/footer/sticky/viewport-height remain runtime-owned). The A1:Figma plugin bridges it as `PageLayout`: **export** emits the nested Top Header as the first child (full Top Header bridge) plus the slot contents as remaining `children`, with `showHeader/showSidebar/showFooter: false` playground-preview flags; **import/update** applies TopHeader props onto the nested instance and renders content children into the slot (Section-style population). `PageLayout` joined `FILL_CONTAINER_TYPES`. a1-web: the PageLayout editor adapter now **forwards children** into the layout's main area (the generic adapter dropped them, which would have swallowed a PageLayout node's page content), and the configurator Preview renders JSON children at natural height without the bounded demo frame. `all-new-components.json` is now rooted in a `PageLayout` containing the TopHeader + the showcase Section. Docs: `figma-workflow.md` (Page Layout section, pages table, fill sizing), plugin README, `components.md` + a1-web coverage flips, Code Connect template `PageLayout.figma.ts`. Verified: 5 stubbed bridge checks (export shape/flags/warning, TopHeader apply, slot clear) and a Playwright playground render of the re-rooted showcase (layout + header + login button + section content, zero placeholder slots, no errors). |
| 2026-07-15 | **a1-web Components Figma coverage audit:** synced `/components` package coverage with the live A1 Figma file after the component page reordering. Added **Split Button** to the Actions & Controls component browser list with Figma package coverage, marked **Icon** as Figma-supported to match the `Icons` page, and corrected the registry menu/coverage notes for Split Button, Switch, and Icon. A consistency audit now reports no components where a top-level Figma page exists but the a1-web package list omits `Figma`. |
| 2026-07-15 | **Figma component page ordering:** reordered the A1 Design System Figma file's component pages to match the registry category order in `components.md` and inserted blank divider pages named `----- Typography -----`, `----- Navigation -----`, `----- Actions & Controls -----`, `----- Inputs -----`, `----- Feedback & Messaging -----`, `----- Layout & Display -----`, `----- Overlay -----`, `----- Data -----`, `----- Media and iconography -----`, and `----- Disclosure / Accordion -----`. Known component pages now sit under their matching category separators; loose scratch/test pages (`Page Test`, `Page 21`, `Page`) were preserved at the end without reclassification. Figma structure only; no component API or token changes. |
| 2026-07-15 | **Figma fill-vs-hug sizing contract:** formalized every component's Figma sizing class in `figma-workflow.md` (new "Sizing convention — fill vs hug" section). **Fill container:** Text Field, Search Field, Select, Textarea, Radio Group, Checkbox Group, Accordion, Banner, Card, Blockquote, Button Container, Figure (+ the already-enforced Stack/Grid/Heading/Paragraph); **Divider fills along its orientation** (horizontal → width, vertical → row height). **Hug contents:** Link, Button, Icon Button, Badge, Switch, Pagination, Segmented Control, Menu, Definition List. The A1:Figma plugin now enforces the contract on import/render/reconcile via declarative `FILL_CONTAINER_TYPES` / `HUG_CONTENT_TYPES` sets plus a new axis-aware `fillImportedDividerAxis` helper in `appendImportedChild` (previously only Stack/Grid/Card/Banner/Blockquote/ButtonContainer/text filled). The remaining components were then classified and folded into the contract: **fill** also includes Top Header, Section, Empty State, List, Bottom Drawer, Page Nav, Tabs (strip), Tree Menu, and Code's `block` variant (variant-guarded); **hug** also includes Dialog, Tooltip, Inline, Breadcrumb, Split Button (doc-only — no JSON type), Code's `inline` variant, and Side Nav (fixed 280/52 rail widths, never stretch). Not-yet-bridged page-definition types are pre-registered in the plugin sets so sizing is correct when their importers land. Verified with 18 stubbed sizing cases (fill in column/row/grid, hug preserved, both divider orientations, Code variant split); plugin README updated. |
| 2026-07-15 | **Figma Segmented Control sizes + icon contract:** updated the Segmented Control and Segmented Control Item Figma sets to expose `sm`/`md`/`lg` sizes matching React's 28/40/56 outer track standard (items 20/32/48), kept item icons on the existing native Material icon `INSTANCE_SWAP` pattern, and bound each nested icon vector to the same foreground variable as its item label (`color/text/muted` when unselected, `color/text/default` when selected). The A1:Figma plugin now connects `SegmentedControl` options to the current Item contract: `Size`, `Selected`, `Label`, `Show label`, `Show icon`, and `Icon` instance swap, and rebinds icon vectors after JSON-driven swaps so color stays token-correct. |
| 2026-07-15 | **A1:Figma showcase JSON — bridge feature exercises:** extended `packages/figma/plugins/a1-json/examples/all-new-components.json` with a "Bridge feature exercises" group and completed coverage by adding **Dialog** (size/status/title/body + Button-only `footerActions`; the playground renders it as an Open-dialog trigger, so the composition stays usable). New exercises: Paragraph `content.inlineLinks` ranges, SegmentedControl `labelMode: "selected"`, an **open md Accordion** with slot children (today's Size × Open axes), the missing **inline** Banner variant (warn), Button `loading` and `disabled` states, and a **vertical Divider** in a row Stack. Intro copy and the DefinitionList coverage row updated from "except Dialog". Verified by rendering the full file in the JSON Playground (no errors; dialog trigger, aria-busy loading button, disabled button, inline links, three banner variants, both accordions, and the TopHeader Sign in button all present). Note: the file now exceeds the `?json=` URL handoff size — load it via the playground editor or the bridge handoff. |
| 2026-07-15 | **Ten new Figma components (typography + navigation batch):** created token-bound Figma component sets for **Inline** (kbd/mark, `656:986`), **Code** (inline/block, `656:991`, new `Code/sm` Source Code Pro text style), **List** (unordered/ordered/icon/divider, `658:1020`), **Breadcrumb** (+ `Breadcrumb Item` link/current/ancestor, `659:999`/`659:985`, new `Breadcrumb/current` style), **Split Button** (primary/secondary × sm/md/lg on the 28/40/56 standard, divider = foreground @35%, `660:1020`), **Bottom Drawer** (+ Item set with icon swap/badge, xs bar, `661:1036`/`661:1035`, new `Nav/Stacked label` style), **Page Nav** (+ Item Level 1/2 × default/active, card shell with reading-progress bar, `662:988`/`662:987`), **Tabs** (`Tab` item set covering line/pills/segment/folder/progress + completed state and a line strip, `663:1009`/`663:1010`, new `Tab/Label`+`Tab/Step number` styles), **Tree Menu** (+ `Tree Item` branch/leaf × selected with add_box/indeterminate_check_box toggles, composed indented tree, `664:1011`/`664:1010`), and **Side Nav** (+ Item set, expanded 280px / collapsed 52px shells, `665:1047`/`665:991`, new `Side Nav/Active label` style + `color/sideNav/itemActiveBackground` accent@10% variable; also `color/inline/mark*` variables). Every set has a dark-mode validation frame; each swapped icon instance was rebound to its state foreground (swap resets nested fills). Code Connect templates added for all ten in `packages/figma/code-connect/`. Registry Figma columns flipped in `components.md`; per-component axes/properties/gaps documented in `figma-workflow.md` (new batch section + pages table). a1-web `PACKAGE_COVERAGE` updated — including 17 stale entries (Blockquote, Divider, Link, Icon Button, Segmented Control, Switch, Search Field, Textarea, Select, Banner, Badge, Empty State, Card, Figure, Tooltip, Definition List, Pagination, Accordion) that already shipped Figma assets but were never flagged. Data-viz charts intentionally deferred. JSON-bridge (A1:Figma plugin) support for the new sets is a follow-up. |
| 2026-07-15 | **A1:Figma Accordion sizing + full showcase JSON:** expanded the Figma Accordion component set to `Size=sm/md/lg × Open=false/true` and updated the Component JSON bridge so Accordion Content Slot children export from collapsed instances and import/update can preserve a collapsed visual state while still rendering children into the slot. Replaced `packages/figma/plugins/a1-json/examples/all-new-components.json` with a broad smoke-test composition covering every current Figma-supported JSON component except Dialog, including TopHeader, Link, fields, choice groups, SegmentedControl, Accordion, Tooltip, Pagination, Menu, Banner, Card, Badge, Figure, DefinitionList, Blockquote, EmptyState, Divider, Stack/Grid, Heading/Paragraph, Buttons, and IconButton. |
| 2026-07-15 | **Top Header loginButton bridge + actions hardening:** a visible sign-in **Button** in the Figma Actions slot now exports as `loginButton: { label }` instead of a warn-and-drop (only the first Button; click behavior stays runtime-owned), and **Update selection/import** applies the label back onto the sign-in Button instance (object or legacy string accepted). Actions export gained a deep-scan fallback so Icon Buttons wrapped in a frame inside the Actions slot no longer silently drop. React `TopHeader` now also accepts a plain string `loginButton` as the label (JSON-safe form; previously a string rendered the default "Log in") and `TopHeader.d.ts` was corrected from the stale `React.ReactNode` to `string \| { label?, onClick? }`. a1-web: the top-header configurator's `toJson` emits the `{ label }` object (localized note text updated — `label.app.configurator.jsonTopHeaderLogin`), `fromJson` accepts object/string/boolean. Docs: `components.md` TopHeader note, `figma-workflow.md` property + gap rows, plugin README. Verified: 20 stubbed-plugin checks and Playwright renders of the login Button (object + string forms) in the Playground and configurator handoff. Investigation also confirmed the full Figma→a1 actions chain passes (export against the live instance, `?json=` handoff, Playground, editor registry, codex bridge) — a stale open plugin session is the likely cause of actions appearing to drop; re-open the plugin after updates. |
| 2026-07-15 | **A1:Figma Segmented Control item JSON:** audited Pagination, Tooltip, Accordion, Switch, Segmented Control, Search Field, and Textarea coverage in the Component JSON bridge. All were already registered; Segmented Control now also round-trips the newer child `Segmented Control Item` slot model, including selected option, option icons, and `labelMode` inference/application, while retaining a legacy text-layer fallback. |
| 2026-07-15 | **Figma Empty State icon bridge:** repaired the Empty State Figma asset so its icon defaults to the React `inbox` glyph, uses the correct page/section/card icon sizes, and binds visible icon vectors to `color/text/muted` for theme-correct muted icon color. The A1:Figma Component JSON bridge now imports and exports `MessageEmptyState.props.icon` through the component's `Icon` instance-swap property instead of warning that icons are unsupported. |
| 2026-07-15 | **A1:Figma Top Header detection** — the Component JSON plugin bridges the Top Header set both ways: **export** reads `Logo text`, the Nav Items slot's Top Header Nav Item instances (`navItems` with label/icon/active; hover and `Breakpoint` warn as visual-only; chevron warns submenus are runtime), the Actions slot's Icon Button instances (`actions` with aria-label + resolved icon), and warns that the Sign in affordance is the runtime `loginButton` node; **import/update** creates or reconciles instances via new `GROUP_SLOT_CONFIG` entries (`TopHeader` → Nav Items ≤8, `TopHeaderActions` → Actions ≤6, login Button untouched by reconciliation), applies labels/states/icon swaps/chevrons, accepts projectLayout's `logo` wordmark string, and maps `loginButton` presence to `Show login button`. a1-web: the top-header configurator gained `jsonType`/`toJson`/`fromJson` (real-prop node shape; `href` sanitized; `loginButton` string ↔ `loginLabel` with a localized note — new `label.app.configurator.jsonTopHeaderLogin`). Verified: 17 stubbed-plugin checks (export composition, slot grow/shrink with login retention, logo-string acceptance) and 9 Playwright checks (JSON view, plugin-node `?json=` handoff round trip). |
| 2026-07-14 | **Figma Top Header — breakpoint variants + slots:** converted the Top Header component into a `Breakpoint=xs|sm|md|lg|xl` set (`node 621:1132`) at the shared contract widths (480/640/1024/1440/1600 per `figma-breakpoint-sync-plan.md`); `xs`/`sm` show the mobile composition (menu-icon hamburger Icon Button, logo, actions; `Nav Items` hidden; new `topHeader/paddingInlineMobile` → `spacing/16`) mirroring React's 768px switch, `md`+ show the full nav; the insert default is `Breakpoint=lg`. `Nav Items` and `Actions` became real Figma **SLOT nodes** exposed as SLOT component properties (`slotContentId`-wired on every variant) — established that `slot.clone()` from an existing slot plus `addComponentProperty(name, 'SLOT', '')` creates slots programmatically even though `figma.createSlot` doesn't exist. Component properties (Logo text, Show login button, both slots) were deleted before the variant combine and re-added at set level to avoid duplicate definitions. Existing example/dark instances re-bound to `Breakpoint=lg` automatically; dark validation re-verified. Docs/gap tables updated in `figma-workflow.md` and `components.md`. |
| 2026-07-14 | **Figma Top Header component:** created the Top Header component on the new Top Header page (`node 613:977`, 1440×64) plus the `Top Header Nav Item` child set (`node 612:995`, `State=default|hover|active`; `Label`, `Show icon`, `Icon` swap, `Show chevron`). New `topHeader/*` Components-collection variables (height→`spacing/64`, inline padding/gaps→Primitives spacing, border widths) and a `Top Header/Nav label` text style (Inter Medium 14/20 matching React's weight-500 nav links); logo uses `heading/sm`. Bar binds `surface/page` + `border/subtle` hairline; active nav underline binds `action/background`. Composes existing Icon Button and Button sets for actions and the `Show login button`-toggled Sign in affordance. Documentation/example/dark-validation frames added and verified in both Color modes. `packages/figma/code-connect/TopHeader.figma.ts` checked in; coverage updated in `components.md` and the a1-web inventory. Gaps documented in `figma-workflow.md`: dropdown submenus, mobile overlay, responsive `navIconPosition`, action badges, `logo`/`logoHref`. Note: the Nav Items and Actions frames are plain auto-layout frames — convert them to Figma Slots in the UI (the plugin API cannot create SLOT nodes) to match the Radio/Checkbox Group slot pattern. |
| 2026-07-14 | **Control-height standard (28/40/56):** established the total-height standard for sm/md/lg controls and documented it in `project-foundations.md` ("Control heights": 28/40/56 controls, 32/40/52 field densities sharing the 40px anchor, 32px utility band). Fixes: **Segmented Control** — height tokens now mean the OUTER track (28/40/56); `.a1-segmented` gets `min-block-size`, segments derive `calc(total − 2×(padding+border))` = 20/32/48, and `segmentPaddingBlockSm` → 0 so sm fits; **Tabs `variant="segment"`** uses the same derivation (40px strip). **Accordion** — trigger height tokens were declared but never applied (heights were padding-emergent 33/40/52); triggers now bind `min-block-size: var(--a1-ac-height)` with tokens moved to 28px/`{base.spacing.40}`/`{base.spacing.56}` and sm block padding 6→2. **Chip** — added `component.chip.minHeight`/`smMinHeight`/`lgMinHeight` (28/40/56, was untokenized 28/35/42) bound in `chip.css`. Rebuilt tokens + html-css; audit clean. Measured after: segmented track 28/40/56 (segments 20/32/48), chip 28/40/56, accordion 28/40/56, tabs segment strip 40. Visual baselines need `npm run test:qa:update`. |
| 2026-07-14 | **a1-web component-page navigation crash fix:** `ComponentDetailPage` reset the per-component `config` in a `useEffect`, so the first render after client-side navigation ran the NEW component's detail module against the OLD component's config. Modules that index into config arrays crashed — reproduced with Stat → Definition List, where `definition-list.jsx` reads `config.items[0]` (`Cannot read properties of undefined (reading '0')`, the live-site error). Fixed with a synchronous render-time reset (`configComponentId` state compared against `component.id`, setState during render per the React derive-state pattern) so every detail module always renders against its own config shape; the existing effect continues resetting viewAs/display/example state. Verified by Playwright: Stat→Definition List, Definition List↔Button, and multi-hop navigation across Menu/Data Table/Segmented Control with zero page errors. No component API, token, or CSS change. |
| 2026-07-14 | **Figma required fields and Switch messages:** Rebuilt the Figma Textarea and Select component sets to add bound `Required` booleans and visible required markers. Rebuilt Switch to add editable Label, Hint, and Error properties, hint/error visibility controls, and a token-bound error outline. The A1:Figma bridge now round-trips `required` for Textarea/Select and `hint`/`error` for Switch. Prior component sets remain as named legacy assets to preserve earlier work. |
| 2026-07-14 | **SegmentedControl className clobber fix:** the root element spread `{...props}` after the computed `className`, so any consumer-supplied `className` (even `undefined` — e.g. the a1-web configurator's `className={utilityClass || undefined}` utility wiring) replaced the whole `a1-segmented` class list, leaving the control unstyled (no raised track, no white selected segment, no shadow — it read as reverted to a pre-styling state). Fixed by destructuring `className` and merging it into the class list, matching the Button pattern; `className` added to `SegmentedControl.d.ts`. Scanned all React components for the same pattern — SegmentedControl was the only instance (DataGrid already merges). Verified in a1-web: track `--semantic-color-surface-raised` + subtle border, selected segment `surface-page` + `shadow-xs`, muted unselected labels. No token, CSS, or API change beyond the additive `className` prop. |
| 2026-07-14 | **Figma component coverage and responsive-sync plan:** Added token-bound Figma component sets plus A1:Figma JSON import/export/update coverage for Search Field, Textarea, Switch, Segmented Control, Accordion, Tooltip, Pagination, and Empty State. The assets expose only stable visual/authoring properties; native form behavior, controlled state, action handlers, and unsupported dynamic layouts remain runtime-owned and produce bridge warnings. Added `figma-breakpoint-sync-plan.md`, defining an upcoming five-frame `xs`–`xl` contract that preserves sparse inherited responsive values. |
| 2026-07-14 | **Figma Textarea content controls:** Rebuilt the Textarea Figma component set so Label, Value, Hint, and Count are editable component properties. Value, hint, and count are hidden by default through explicit visibility booleans; the bridge maps a displayed count to `showCount` and, when present, `maxLength`. Added a token-bound bottom-right resize grip. The earlier set remains on the Figma page as `Textarea (legacy)` to avoid deleting prior work. |
| 2026-07-14 | **Figma Figure image page sync:** linked-page exports and Figma-created A1 pages now collect every supported Figure image fill automatically, turn its Figure `src` into a stable `a1img://` reference, and carry the image bytes in the localhost-only page-sync sidecar. A1 persists the images before applying the JSON, so users no longer need the per-Figure handoff for page work. A handoff is deliberately limited to PNG/JPEG/GIF, eight images, and 4 MB total; unsupported or oversized fills stay unchanged and report a non-blocking warning. |
| 2026-07-14 | **Figma → A1 new-page handoff:** A1:Figma’s Page Editor now offers an explicit **Create A1 page** action. With a local project selected, it exports the selected top-level Figma frame into a fresh A1 project page, assigns A1’s durable page id, and saves a manual Figma root link so later Page Editor sends and pulls remain connected. The localhost bridge validates and short-lived-queues this creation request; A1-web commits it only while the workspace is open and immediately republishes the workspace manifest. |
| 2026-07-14 | **Figma Select Menu composition:** Added a reusable open-state `Select Menu` component on the Select page. It composes the existing token-bound Menu shell with fill-width Menu Item instances for default, selected, and disabled options, plus an in-file usage note. This is a Figma visual-composition aid only; `SelectField` remains the single React/JSON contract for native options, selection, keyboard behavior, and overlay positioning. |
| 2026-07-14 | **Figma Select visible value:** The Component JSON bridge now treats the Select asset's `Show value` property as explicit JSON state. A visible Figma value exports as `showValue: true` with `defaultValue`; import only reveals the Figma Value layer when that boolean is set. The a1-web page renderer converts the selected preview value into a matching native option, so SelectField visibly renders the same selection without serializing Figma-only display text as a runtime option model. |
| 2026-07-14 | **Figma Select and Divider JSON bridge:** The Component JSON plugin now imports, exports, and updates Figma Select as `SelectField` (size, label, hint/error, disabled, and static visible-value preview) and Divider as `Divider` (orientation, semantic variant, line style, and size). Native Select options/selected data and Divider responsive orientation/spacing/semantics remain explicit runtime-only warnings. |
| 2026-07-14 | **Figma Icon Button JSON bridge:** The Component JSON plugin now imports, exports, and updates Icon Button’s `variant`, `size`, Material `icon`, and accessible `label`. The Figma asset now exposes a real `Icon` instance-swap property across all 12 variants plus a set-level `Aria label` text property. Disabled, link-rendering, event, and DOM-only props remain explicit representation warnings; an unresolved nested icon falls back to the asset’s visible `star` glyph so exported JSON stays valid. |
| 2026-07-14 | **Figma Icon Button token alignment:** audited the `secondary` and `tertiary` Icon Button variants across `sm` / `md` / `lg`; every root fill, border, and Material icon is bound to its matching Button token triplet (`button/{variant}/background`, `/border`, `/foreground`). Corrected the Figma `success` and `destructive` treatments to dedicated `iconButton/{status}` aliases that route through the semantic status surface, border, and foreground tokens instead of solid Button action colors. |
| 2026-07-14 | **Figma Link JSON bridge + AutoFix:** The Component JSON plugin now imports, exports, and updates Link instances, including size, weight, editable label, optional Material icon, and icon position. Runtime navigation props (`href`, `target`, `rel`, and event handlers) remain explicit representation limits. It also recognizes standalone blue or blue-violet underlined text as an A1 Link candidate; contextual **AutoFix** applies the nearest named Link text style, underline, and semantic `link/color` variable so the exported JSON carries `type: "Link"` rather than a raw color. Any underlined substring inside a Heading or Paragraph remains inline through the new `content.inlineLinks` ranges, rendered as nested A1 Links and bound to `link/color` by AutoFix. Plain black AutoFix resolves to `color/text/default` rather than retaining a raw `#000000` fill. In Figma, Link normal/hover/pressed variables now route through mode-aware `color/link/*` aliases (Light: info 500/600/700; Dark: info 100/50/200) instead of the action-background token. |
| 2026-07-14 | **Figma Banner and JSON bridge:** Added the token-bound Banner component set on the Banner page with all 15 combinations of inline/system/calendar × neutral/info/success/warn/error. It exposes editable Title, Eyebrow, Month, and Day text properties and an ordered Content Slot. The A1 JSON plugin imports, exports, and updates Banner variant/status/title/calendar data and content children; legacy `content.fallback` imports as a muted Paragraph child. Because Figma's plugin API cannot author native Slots or insert into an instance's ordinary frame, a Banner with JSON children is tagged and detached into an editable frame that still exports as `Banner`; rerender it for later visual-prop changes. Runtime action, dismissal, custom-icon, and announcement semantics report explicit Figma warnings. |
| 2026-07-14 | **Figma Definition List size bridge:** Definition List and its reusable Definition List Item now expose matching `sm` / `md` / `lg` Size variants. The A1 JSON bridge imports and exports `props.size`, applies it to both the parent list and every slot row, and uses the corresponding token-bound spacing plus Body/SM, Body/MD, or Body/LG typography. |
| 2026-07-14 | **Figma Figure bridge sizing contract:** compact Figma Figure `2xs` / `xs` / `sm` / `md` / `lg` / `xl` values are treated as outer maximum widths (128 / 192 / 320 / 480 / 640 / 800 px), not distinct media geometry. The JSON bridge enforces that cap on imported instances and locks the selected `16:9` / `4:3` / `1:1` / `3:4` / `9:16` ratio on the nested Image layer, matching the React Figure contract. |
| 2026-07-14 | **Figma Badge Material icons and Grid bridge:** Badge now exposes `sm` / `md` / `lg`, `Show icon`, and a directly editable nested Material icon instance—no custom icon asset. The JSON bridge imports/exports size, `icon: null`, and selected Material icon names. Authored Figma Grid auto-layout Frames now map to A1 `Grid`, preserving fixed columns, nearest A1-scale row/column gaps, cross-axis alignment, and child order; Grid JSON renders back to a native Figma Grid with automatic row flow. Responsive columns, custom tracks, manual placement, and spans warn when the compact interchange cannot represent them. |
| 2026-07-14 | **Figma Figure, Definition List, Blockquote, and Badge refinements:** Definition List now uses reusable row/column Definition List Item instances in its native slot; Figure replaces its Media Slot with a token-bound image fill and compact `sm`–`xl` / five-ratio variants; Blockquote feature/pull use display typography and feature has a token-colored opening quote mark; Badge has status-specific, token-bound default icons plus a `Show icon` control, and its subtle variants use readable foreground and status-border token bindings. Every Badge icon uses the exact same foreground binding as its label and a `spacing/4`-bound 4px gap, matching React `MessageBadge`. The Component JSON plugin imports/exports Figure's compact `size` / `aspectRatio` props, Definition List Item instances, and Badge `icon: null`; unavailable Material icon names warn rather than inventing a glyph. |
| 2026-07-14 | **Figma Component JSON stale slot export:** slot discovery now refreshes its outer instance and safely treats a Figma-replaced internal layer as unavailable. Exporting a slot-bearing component nested in a Stack/Section no longer fails with `get_name` when Figma transiently retains a stale instance sublayer; the plugin emits its existing missing-slot warning where relevant and continues exporting the composition. Figure now uses an image fill rather than a slot. |
| 2026-07-14 | **Figma Component JSON end-to-end fixture:** added `packages/figma/plugins/a1-json/examples/all-new-components.json`, a paste-ready Section composition that exercises Card (default/accent icon treatments and native content), MessageBadge, Figure metadata and image-fill variants, Definition List items, Blockquote, nested Stack layout, styled text, Buttons, and Button Container actions. The plugin README links to the fixture and explains that Figure source URL metadata and the Figma image fill are edited separately. |
| 2026-07-14 | **Figma Card, Badge, Figure, Definition List, and Blockquote bridge:** added compact token-bound Figma components for Card (default/accent, configurable inline icon in a tokenized action-surface tile with a legible text-foreground glyph, and Content Slot), Badge (status/subtle + label), Figure (source/alt/caption data plus image-fill variants), Definition List (row/column + reusable Label/Value item instances), and Blockquote (visual variant + quote/citation). The Component JSON plugin now imports, exports, and updates these public contracts without detaching native-slot components; documented React-only gaps warn instead of inventing unsupported Figma behavior. |
| 2026-07-13 | **Stack zero-gap rendering:** numeric `0` spacing now resolves to literal CSS `0` instead of the non-existent `--base-spacing-0` token. Figma auto-layout frames with `itemSpacing=0` therefore retain no gap in JSON Playground rendering, and Stack/Grid conversion preserves the explicit zero value. |
| 2026-07-13 | **Figma Live-view presentation mode:** Live handoffs now render the Playground outside A1 Web's `PageLayout`, leaving only the JSON composition—no header, navigation, sidebars, or editing controls. The Figma plugin contracts to a 220 × 72 px Live view control with the toggle as its only visible UI, then restores its normal workspace size when disabled. |
| 2026-07-13 | **Figma Live-view Playground focus:** local Playground handoffs now carry the plugin's Live view state. While Live view is active, A1 Web removes the JSON SideNav and its toggle for an unobstructed composition preview; turning it off restores the manual JSON panel. |
| 2026-07-13 | **Figma Live-view ancestor search:** exportable-container and topmost-ancestor inspection now catch stale Figma child/parent handles during document changes, preventing transient `get_children` errors while Live view chooses a screen-level export target. |
| 2026-07-13 | **Figma stale variable-mode reads:** Section export now refreshes its root instance before inspection and ignores unavailable internal frames/instances while reading explicit variable modes. This prevents transient `get_explicitVariableModes` errors after Figma replaces a component sublayer. |
| 2026-07-13 | **Figma stale Section sublayers:** component-set lookup and Section property-carrier discovery now tolerate Figma's short-lived stale internal instance handles after a variant/property replacement. Unavailable carriers are ignored rather than throwing `get_mainComponent` during selection inspection. |
| 2026-07-13 | **Figma Live view export resilience:** Stack/free-content traversal now guards against circular or stale Figma child references, skips only the inaccessible child with an explicit export warning, and reports Live-view export failures in the plugin UI instead of leaving an uncaught console error. |
| 2026-07-13 | **Figma live Playground view:** the Component JSON plugin now has an opt-in **Live view** mode. When enabled, it debounces document changes and streams the highest exportable ancestor of the active selection to the local Playground, refreshing the full composition without changing the designer's Figma selection. Disabling the mode stops all live sends. |
| 2026-07-13 | **Live Figma Playground updates:** an open JSON Playground now polls the local bridge for Component JSON plugin handoffs. Choosing **Open in playground** updates that existing page in place; the plugin opens a named Playground tab only when none is listening. |
| 2026-07-13 | **Figma Button Container fill width:** imported A1 `ButtonContainer` instances now fill their available width in auto-layout parents, while their `align` prop continues to position the contained actions. |
| 2026-07-13 | **Figma Open in Playground handoff:** large Component JSON payloads now use a short-lived localhost bridge handoff instead of an oversized query string. The Figma plugin stores valid JSON for five minutes and opens `/playground?handoff=<id>`; the Playground consumes and acknowledges it before rendering. Small payloads still use the direct URL path. |
| 2026-07-13 | **Figma Stack sizing defaults:** imported A1 `Stack` frames now default to Hug contents for height and fill their available width when placed in an auto-layout parent: they stretch across the cross-axis of vertical parents and grow across the primary width axis of horizontal parents. Explicit `grow: true` behavior remains available and is applied after the default. |
| 2026-07-13 | **a1-web Playground → Figma local handoff:** the JSON Playground now has **Send to Figma**. It queues valid JSON through the existing localhost-only A1 bridge on port `4318`, avoiding the A1 Jobs bridge's `4317` port; the manually opened A1 Figma plugin polls its development-only `localhost:4318` permission, imports each queued payload once onto the current page, and acknowledges it. The bridge accepts queue writes from the local a1-web origins only, limits payloads to 500 KB, keeps one payload for five minutes, and keeps Figma’s `null`/Figma origin access limited to read/ack handoff endpoints. A browser cannot launch a Figma plugin. |
| 2026-07-13 | **Code line-number gutter:** React `Code` now accepts `lineNumbers` for block presentation and `rows` to control an editable block's visible height. The optional non-interactive, `aria-hidden` gutter works in read-only and editable blocks, updates as logical lines are added or removed, follows vertical editable-code scrolling, and measures wrapped logical lines so each number stays aligned with its first visual row. The a1-web JSON Playground uses the shared editable Code editor with wrapping, copy, line numbers, a controlled editor height, and an inline A1 error Banner. |
| 2026-07-13 | **Figma Stack and Section layout bridge:** the Component JSON plugin now recognizes authored vertical/horizontal Figma auto-layout Frames as `Stack` nodes, preserving direction, A1-scale gap (including `0`), alignment, primary distribution, horizontal wrap, grow, and supported children without serializing component implementation layers. `Stack` JSON creates or updates an editable auto-layout Frame. The contextual one-click **AutoFix** reviews and normalizes off-scale Stack spacing, incompatible wrap spacing, and mixed stretch settings; it leaves padding review-only because A1 represents that with `Inset`. Section imports now populate the native `Section Content Slot` while retaining the Section instance, so Heading, Paragraph, and ButtonContainer children render in the intended content carrier; imported Heading/Paragraph layers stretch to fill the width of a vertical auto-layout parent and automatically resize their height for wrapping. Section property application refreshes internal carriers after each variant write so Figma does not read a replaced nested instance sublayer. Matching ButtonContainer JSON action counts now update the existing representative Button Slot instances and keep the container attached; only a legacy frame-based slot that needs structural add/remove work detaches. Selected Sections still inspect that carrier for arbitrary numeric spacing and AutoFix synchronizes it with an available Gap property/mode. Text AutoFix compares an unbound solid fill to local A1 text variables, binding the nearest semantic token (such as a light gray to `text/muted`) rather than emitting a hex value. It also detects local font-size overrides even when a Figma A1 text style is still attached, then applies the nearest allowed size in that style family; unstyled large bold text compares the Heading and Display scales so Display-only sizes resolve as Display. The plugin’s playground handoff preserves the current JSON string, and the Playground pretty-prints valid URL-loaded JSON before editing without reformatting user keystrokes. Stack’s TypeScript gap union now correctly includes the runtime-supported `0`. |
| 2026-07-13 | **a1-web JSON playground:** `/playground` is a lightweight live renderer for larger JSON experiments. An A1 SideNav rail accepts one component node, a `{ nodes }` bundle, or a full page definition; the main area remains a blank Canvas until input is valid, then previews it through the production page renderer. The JSON source now uses the shared editable `Code` block, including wrapping and copy controls; malformed input stays in place and is announced inline. |
| 2026-07-13 | **Figma Section child updates:** Component JSON **Update selection** now reconciles the existing ordered Heading, Paragraph, and Button layers in a selected Section’s `Section Content Slot`. Text edits include A1 text-style changes such as a Heading `size`; structural add/remove operations still require import/render because the parent remains a Figma component instance. |
| 2026-07-13 | **Figma Section content and Pattern handoff:** the Component JSON plugin now exports the ordered `Section Content Slot` tree of a Section, including standalone A1-styled Heading/Paragraph layers and registered child components such as Buttons. **Open in pattern editor** transfers the resulting component JSON (or a node bundle/Pattern definition) into a new local Pattern with a fresh ID, then opens the established Pattern editor. |
| 2026-07-13 | **Dialog footer action rendering:** `Dialog.props.footerActions` now reaches the a1-web Dialog adapter as direct A1 Button elements rather than being replaced by representative preview buttons. Each JSON action preserves its own Button props (including `variant`), while Dialog retains ownership of the one right-aligned ButtonContainer footer wrapper. |
| 2026-07-13 | **Figma component export precedence:** registered A1 instances now win over the generic frame/group traversal. Selecting a Dialog exports a `Dialog` node with body and footer actions rather than a neutral bundle of its internal Heading, Paragraph, and Button layers. |
| 2026-07-13 | **Figma Display Heading style lookup:** Heading JSON import/update now resolves its Figma text-style family from `props.type`: `type: "display", size: "xxl"` binds `display/xxl`; heading and display remain separate A1 hierarchies. A missing `heading/xxl` warns and falls back within the heading family rather than conflating the two. |
| 2026-07-13 | **Figma text color token namespace:** corrected the Component JSON bridge’s text-color lookup to recognize the actual `color/text/default`, `color/text/muted`, and `color/text/accent` variable paths. It still exports the portable component value (`color: "muted"`), then binds the matching Figma variable during import or text repair. Selected text is re-read and exported on the next debounced document change so manual Figma fill-token edits do not use a stale node handle. |
| 2026-07-13 | **Heading/Paragraph JSON and tokenized text bridge:** Heading and Paragraph now opt into the component configurator’s editable JSON view with safe default omission and JSON import. Their portable color model remains the underlying A1 prop enum (for example, `color: "muted"` represents the `text/muted` semantic token), never a resolved hex value; the Figma plugin binds the actual variable. Frame/group exports are now neutral `{ nodes: [...] }` bundles rather than invented Sections, so plain text imports without added canvas surface, padding, or layout formatting. |
| 2026-07-13 | **Figma Dialog actions and free-text bridge:** the Component JSON plugin serializes Dialog Footer Slot Button instances as `props.footerActions` and reconciles them during import/update; the page renderer turns the portable list into a native `ButtonContainer` footer. Standalone Figma text layers now export/import as A1 Heading or Paragraph from local `heading/*`, `display/*`, and `body/*` styles, text color variables, and alignment. Unsupported selected-text properties surface a review warning and can be normalized only through the explicit **Apply A1 text suggestion** action. |
| 2026-07-13 | **Figma Dialog body slot bridge:** `Dialog.props.body` now hides the inherited placeholder inside Figma's `body slot` and inserts a styled local text override. Export reads the slot override first, so JSON body content round-trips with the visible Dialog body rather than the disconnected `Body` component property. |
| 2026-07-13 | **Figma Menu divider bridge:** the Menu Item `Type=Divider` variant now maps bidirectionally to the a1-web `{ kind: 'divider' }` menu item. It consumes one of the existing five Figma Menu row slots and preserves its place during import, update, and export. |
| 2026-07-13 | **Figma group item reconciliation:** the Component JSON plugin now reconciles the live Figma option slots when `RadioGroup` or `CheckboxGroup` JSON items are added or removed, creating/removing real option instances before applying their properties. Radio Group accepts 2–20 rows and Checkbox Group accepts 1–20; counts outside the Figma component contract remain valid JSON but are clamped with an explicit warning. |
| 2026-07-13 | **Figma Radio Group default selection:** `RadioGroup.props.defaultValue` now maps to the matching Figma Radio Option `selected=true` variant on import. Canonical `options[].value` is used first, with `options[].id` accepted for a1-web editor-config compatibility; an unmatched default emits an explicit warning. |
| 2026-07-13 | **Editable Component JSON views:** Text Field, Menu, Dialog, Radio Group, and Checkbox Group now opt into the same page-definition JSON contract used by Button and Section (`jsonType`, `toJson`, `fromJson`). Their component pages expose the shared editable **Code \| JSON** view and accept valid `?json=` Figma-plugin handoffs. The group factory maps configurator option IDs/selection to `options`/`defaultValue`; Dialog maps title/body/footer/close/status; Menu maps its editable item configuration. |
| 2026-07-13 | **Figma Component JSON bridge coverage** (A1-1651): completed `packages/figma/plugins/a1-json/` for every public Figma asset. In addition to Button and Section, it now auto-exports, imports, and updates Text Field, Menu, Dialog, Radio Group, and Checkbox Group. It uses the live component variants/properties (including group density/inline layouts and Menu row state), documents deterministic option values derived from labels, and warns for fixed-slot, runtime-only, or otherwise unsupported values. The a1-web Dialog adapter now preserves exported body/footer configuration; the Menu adapter and configurator preserve active/disabled item state. |
| 2026-07-13 | **Components Figma coverage:** audited the live A1 Figma library and synchronized the registry and a1-web Components inventory. The public Figma assets are Button, Section, Text Field, Menu, Dialog, Checkbox Group, and Radio Group. Internal composition assets (Section Content, Menu Item, Checkbox Option, Radio Option, and Dialog Hero Icon) and imported Material icon sets are intentionally not listed as standalone A1 component coverage. |
| 2026-07-13 | **Figma to JSON plugin + configurator View JSON + Figma→a1-web handoff** (A1-1651): proof of concept bridging A1 Figma components and the page-definition JSON format. New Figma plugin `packages/figma/plugins/a1-json/` (manifest + code.js + ui.html + README) — **auto-export** of the selected instance as a page-definition `ComponentNode` (regenerates on selection/configuration changes, debounced; hand-edited JSON is never clobbered — the manual Export selection button overrides), **import/render** pasted JSON (single node, node array, or full page definition/project bundle) as Figma instances (runtime props warn as documented Figma gaps), and **Open in a1-web** — deep-links the exported node into the local configurator via a `?json=` query param (`A1_WEB_BASE` constant, localhost:5177 for now). Supported: **Button** (`Variant`/`Size`/`State`/`IconPosition`/`Label`/`Show icon`/`Icon`, defaults omitted, visual-only states warned) and **Section** — Figma's split Section model (the Section set + internal parts such as **Section Content** carrying the content-width/padding properties) is translated by scanning the instance and its internal part instances for properties by canonicalised name (case/spacing-insensitive; ContentWidth/Gap variable modes as fallbacks; explicit warnings when a core axis can't be located); `inverse` ↔ explicit Inverse/Dark Color mode, TEXT documentation props (Gradient/Align/Height/borders/Radius/background) round-trip, and registered descendant instances export as `children` (import detaches a Section with children so they can be placed in its `_content` slot; unsupported wrappers are flattened with warnings). An **Update selection** action applies pasted JSON to the currently selected instance in place via shared appliers (`applyButton`/`applySection`; properties only, children warned). a1-web: Button **and Section** configurators have the **Format: Code \| JSON** toggle — the JSON view is an **editable** code block (copy button, cleaned `utilities`; valid edits apply live with inline validation banners; `fromJson` rejects script-capable `href`/`backgroundImage` schemes; imported nodes with `children` show an info notice), and `ComponentDetailPage` applies a `?json=` node once on arrival then consumes the param. Generic opt-in via detail-module exports `jsonType`/`toJson`/`fromJson` (`detail/button.jsx`, `detail/section.jsx`) + `detail/index.js` passthrough, shared UI in `ComponentDetailPage.jsx`. New localized `label.app.configurator.*` labels (en + es/fr/de/pt/ja/zh/ar). No design-system component API, token, or CSS change. |
| 2026-07-13 | **Figma Radio Group component** (A1-1403): the redesigned Radio Group parent set on the Radio Group page (`node 283:1121`) now has `Size` variants for `comfortable`, `default`, and `compact`. Its `Radio Items` slot composes the separate Radio Option set (`node 269:1599`), which exposes matching independent `Size` variants plus `selected` and visual `state` values. Parent properties cover legend, helper, and required marker. Both sets bind scoped `radioGroup/*` geometry variables, A1 text styles, and shared Light/Dark Color roles. Documentation, realistic light/dark examples, and `packages/figma/code-connect/RadioGroup.figma.ts` were updated. Runtime selection, option value/count, inline layout, native radio/form semantics, callbacks, error/disabled parent states, and ARIA remain documented Figma gaps. |
| 2026-07-08 | **a1-web local Codex icon finder:** rewired the editor **Find an icon with AI** flow to use the localhost Codex bridge (`/codex/suggest-icons`) instead of browser-side Anthropic key setup or a separate local model runner. Suggestions are validated against `system/icons/material-symbols.json` plus active-project custom icons, annotated with icon-usage guidance when available, and report elapsed time plus token usage when Codex exposes counts in JSON events. The icon finder button is now available independent of `VITE_AI_ENABLED`. Added localized `label.app.editor.iconAi*` token-feedback labels, updated Help copy, and added a Blog article explaining usage, benefits, limitations, and API-extension fit. No design-system component API, token, or CSS change. |
| 2026-07-08 | **a1-web local Codex review** (A1-409): added a localhost-only Node Codex bridge (`npm run codex:bridge:a1-web`) that calls `codex exec --json --sandbox read-only --cd <repo>` with a structured page-review schema, plus an editor sidebar **Codex** tab that checks the bridge and reviews the current page definition for A1/schema/a11y/content findings. Added localized `label.app.editor.codex*` labels and documented the bridge port in quick orientation. No design-system component API, token, or CSS change. |
| 2026-07-08 | **a1-web project page routing** (A1-386): project `TopHeader` logos now receive a project-scoped `logoHref` when chrome is generated or injected into shared layouts. Published prototypes link the logo to `/p/{slug}`, standalone project previews link to the project’s first page, and editor chrome links to the project home (`/editor?project=…`) instead of the A1 app home. Added a renderer fallback for saved shared layouts that do not contain a `TopHeader`, so projects like imported ApiGrid still get generated project navigation instead of losing the header. No design-system component API, token, label, or CSS change. |
| 2026-07-08 | **a1-web Presentation primary slides only:** hid presentation speaker notes and demo placeholder screens by gating them behind local `SHOW_SPEAKER_NOTES` / `SHOW_DEMO_SCREENS` flags in `Presentation.jsx`. The content remains in source for later restoration. No design-system component API, token, label, or CSS change. |
| 2026-07-08 | **a1-web project publishing** (A1-1933): added project-level publish metadata and a project-home **Publish** dialog/action. Published projects get a stable workspace preview path (`/p/{slug}`) that routes into `EditorPreviewPage` as standalone chrome, starts on the first project page, and keeps in-prototype navigation under `/p/{slug}/{pageId}`. Publish state is preserved in the shared text export/import path and can be removed with **Unpublish** without deleting project content. Added localized `label.app.projects.*` publish labels. No design-system component API, token, or CSS change. |
| 2026-07-08 | **a1-web ticket page — in-page navigation** (A1-399): added a `PageNav` in-page nav to `pages/BacklogTicketPage.jsx`. The body sections (Details / Activity / Linked tickets / Build with AI / dev-only Virtual PO) now carry `id`s and are laid out in a two-column `Grid` — content (`GridItem` span lg:3) + a sticky `PageNav` sidebar (span lg:1) with `--a1-page-nav-top` set to `--component-top-header-height`; on mobile PageNav is its own fixed pill bar. New localized label `label.app.backlog.onThisPage` (en + es/fr/de/pt/ja/zh/ar) in `system/labels/app.json`. No design-system component API/token/CSS change — reused the existing `PageNav`, `Grid`/`GridItem`, and `Section`. Deviation noted: body `Section`s are nested inside a `Grid` column (they're no longer full-bleed and gain `radius="md"` where surfaced) — a deliberate two-column detail layout, accepted as local (see A1-399 standards review). || 2026-07-08 | **a1-web Editor screen reader report** (A1-383): added a page-editor **Screen reader report** toolbar action for project pages. The report analyzes the current page definition and shows a heuristic reading outline with nesting level, component, inferred role, likely screen reader announcement, pass/review/issue status, and notes for heading jumps, missing `aria-labelledby` targets, unlabeled buttons/icon buttons/fields/progress, and image-alt handling. Added localized `app.editor.screenReaderReport*` labels. No design-system component API, token, or CSS change. |
| 2026-07-08 | **a1-web Backlog ticket-only screenshots** (A1-411): ticket screenshots added by paste or file upload are now saved with an internal hidden-library marker, so they keep rendering as ticket attachments without appearing in the reusable Image library, editor Images panel, categories view, or image storage count. Existing legacy attachment refs still resolve. No design-system component API, token, label, or CSS change. |
| 2026-07-08 | **a1-web projects — sidebar (SideNav + TreeMenu) navigation option.** Added a per-project `navStyle: 'header' \| 'sidebar'` (`projectStore.ts` `Project` type + `createProject`; settable in `ProjectDialog` under "Primary navigation"). New `buildProjectTree` in `projectNav.ts` maps the page hierarchy to `TreeMenu` items + the ancestor ids to expand. In `EditorPreviewPage.tsx`, when `navStyle === 'sidebar'` the prototype renders real app-shell chrome — `PageLayout` (`viewportHeight`) with a `SideNav` whose children are a `TreeMenu` (`onSelect` → `navigateToScreen`, `selectedId` = active page, `defaultExpandedIds` = ancestors) — and the page renders in the main area (chosen "real chrome" approach, since the page-def renderer skips real PageLayout and the TreeMenu editor adapter's onSelect is local-only). Scope: prototype/preview only — the editor canvas still shows the TopHeader shared layout; the shared TopHeader/footer chrome isn't applied in sidebar mode; mobile SideNav open/close toggle is a follow-up. Also fixed archive-safety bugs: `createProject`/`updateProject`/`exportAllText` used the archived-filtered `loadProjects()` before saving (would drop archived projects) — switched to `loadProjectsRaw()`; and the `exportAllText`/`importAllText` PROJECT marker didn't carry `archived`/`navStyle`, so the projects export/import round-trip used by **cloud sync** un-archived projects on the next hydrate (the actual "archived projects don't disappear" cause when signed in) — both fields are now serialized and restored. |
| 2026-07-08 | **a1-web Vite warning cleanup** (A1-401): added a targeted a1-web Vite build warning filter for the known `@anthropic-ai/sdk` browser-externalization messages caused by unused credential fallback imports, and raised the chunk warning threshold to the current app-shell size. Production builds now complete without the accepted non-actionable warning noise. No design-system component API, token, or CSS change. |
| 2026-07-08 | **a1-web projects — archive instead of delete + pattern text-override handler fix.** (1) Project delete is now a **soft archive**: added `archived?` to the `Project` type, `archiveProject`/`unarchiveProject`/`loadArchivedProjects` in `projectStore.ts`, and `loadProjects()` filters archived out. `main.jsx` `handleDeleteProject` now archives (plus `handleRestoreProject` + `handleDeleteProjectPermanent`); `ProjectsList.jsx` relabels Delete→Archive and adds an **Archived** accordion (Restore / Delete permanently); `AllPagesView.jsx` delete copy aligned. Fixes deleted projects reappearing (sample re-seed / cloud sync). (2) Pattern **text/property overrides on instances** weren't persisting: the write handlers in `EditorPage.tsx` (`handleContentChange`, `handleContentKeyChange`, `handleItemTextChange`, `handleNodePropsChange`) early-returned on a **structural** lock (`lock.node`), silently dropping edits to *unlocked* props/text. They now gate only on `lock.content` (text) / `lock.props` (per-prop); `lock.node` stays structural (delete/move/add guards unchanged). |
| 2026-07-08 | **TopHeader menu search slot + a1-web Components menu search** (A1-408): added a React `TopHeader` `menuHeader` nav-item slot for compact submenu chrome, documented it in the registry, and added Storybook coverage. a1-web now uses the slot to place a localized compact `SearchField` in the main Components menu; typing switches the menu from category flyouts to ranked component matches, and Enter opens the top match. Added `app.nav.searchComponents` labels for supported locales. No token changes. |
| 2026-07-08 | **a1-web Backlog linked-ticket overflow menu** (A1-410): the similar-ticket card overflow/context menu in `TicketMergePanel` no longer duplicates the regular visible actions (Open, Link/Unlink, Merge). Those primary actions stay as explicit icon buttons on each card; the overflow menu now contains only the secondary **Copy reference** action. No design-system component API, token, or CSS change. |
| 2026-07-08 | **Lumen Card radius:** reduced Lumen's Card corner radius by one token step, changing `component.card.borderRadius` from `{base.radius.xl}` to `{base.radius.lg}` in the Lumen default override so dimensional cards read less rounded while retaining the theme's tokenized shape system. Rebuilt generated token/theme/Pure outputs. No component API changed. |
| 2026-07-07 | **a1-web patterns — host wrapper + structural-lock config fixes:** two editor fixes on top of governed propagation. (1) A **multi-root pattern instance** wraps its roots in an auto-generated `Stack` (`patternInstance`, no `patternNodeId`); selecting that wrapper showed Stack layout controls. It's now treated as a **pattern host** (`isPatternHost` in `EditorPropsPanel.jsx`) — banner-only (name, description, Edit pattern, Detach), no component config. Its child pattern nodes still configure normally. (2) A **structural lock** (`lock.node`, "Lock component — can't remove, move, or replace") was mapped to `fullyLocked`, which dimmed and disabled the *entire* configurator (all props + text) on instances. `lock.node` is now **structural-only**: `lockValue.fullyLocked` is always false in enforcing mode and the `a1-web-config-locked` wrapper was removed, so unlocked properties and text stay editable; only individually-locked props (`lock.props`) and locked text (`lock.content`) are read-only. Structural enforcement (delete/move/convert) is unchanged (`suppressConvert` still keys off `lock.node`). Known gap: `lock.content` is enforced on the canvas (InlineEditable) but not separately inside a configurator's own text field (pre-existing). |
| 2026-07-07 | **a1-web patterns — governed propagation + instance editing:** editing a pattern now propagates to every placed instance automatically, on a **governed** merge (structure + locked fields follow the pattern; unlocked per-instance values are preserved and never overwritten). Added `apps/a1-web/src/patterns/propagatePattern.js` (`propagatePatternToInstances` / `reconcileInstance`): on each debounced pattern save (`EditorPage.tsx`, after `savePattern`), it walks every project page (`loadProjects`/`loadPages`/`resolvePageJson`), and for each instance of that pattern freshly instantiates the pattern (new structure + pattern values + preserved `lock`), then overlays the old instance's **unlocked** prop/text values onto matching nodes (matched by `patternNodeId`), forcing **locked** props/text to the pattern value; nodes added/removed/reordered in the pattern are added/removed/reordered in the instance, and node ids stay stable. Persists via `commitPageJson`; pages not mentioning the id are skipped. **Instance editing:** selecting a placed instance (or a node inside one) shows a **pattern banner** (`PatternInstanceBanner` in `EditorPropsPanel.jsx` — name, description, **Edit pattern**, **Detach**) *above* the normal configurator, so unlocked properties stay editable per instance while locked ones render read-only via the existing lock mechanism. Threaded `onDetachPattern` through `EditorAsidePanel` → `EditorPropsPanel`. (Earlier the same day this was first built as full-sync/symbol with an info-only panel; changed to governed because unlocked per-instance edits were being overwritten.) Known gaps: instances embedding the edited pattern via a `PatternRef` inside *another* pattern aren't reconciled (only direct instances of the saved id); the synthetic Stack wrapper of a multi-root instance isn't a pattern node, so its own layout props reset to instantiate defaults. |
| 2026-07-07 | **a1-web editor — suppress autofill in config panels:** the editor's Configure/Data panels are internal tooling but their `<input>`/`<textarea>` fields were triggering browser + password-manager autofill (e.g. offering the signed-in user's email into a Stat "Description"). Added `apps/a1-web/src/editor/useSuppressAutofill.js` (a hook that sets `autocomplete="off"` + `data-1p-ignore`/`data-lpignore`/`data-bwignore`/`data-form-type="other"` on every field within a container, reapplied to later-mounting fields via a MutationObserver) and applied it to the `EditorAsidePanel` body so it covers all configurator/data/page-metadata fields across tabs without editing each configurator. |
| 2026-07-07 | **a1-web editor — SectionSeparator canvas selection:** `SectionSeparator` couldn't be selected by clicking it on the editor canvas (only via the layers tree). Its shipped root sets `pointer-events: none` (correct for a decorative divider between full-width sections — it must not intercept clicks in a rendered page), which also let editor clicks pass through so the node never entered the event path. Fixed in the a1-web editor stylesheet with `[data-editor-node].a1-section-separator { pointer-events: auto }` — `data-editor-node` exists only in edit mode, so rendered pages are unaffected and the shipped component is unchanged. |
| 2026-07-07 | **a1-web project/pattern editor — configurator audit:** every registered, addable component now has a working configurator in the editor Configure panel (previously 13 showed "No configurator is registered for this component type"). Wired `Stat`, `Autocomplete`, `InlineEditable`, `Inline`, `Dialog`, `Menu`, `ContextMenu`, `Snackbar`, `Notification`, `SideNav`, `BottomSheet`, and `Canvas` into `EditorPropsPanel.jsx` (`CONTROLS_BY_TYPE` + `propsToConfig` + `configToNodeUpdate`), reusing each component's existing detail configurator. `Stat`/`Autocomplete`/`InlineEditable` use explicit prop bridges (config's derived `showIcon`/`showBadge`/`badgeIconMode`, Autocomplete option `id`↔`value`, `ariaLabel`↔`aria-label`); the rest are config-as-props editor adapters (`SideNav` strips local `openItems`). `Stat.value` bound to `{{ dataset.column }}` is preserved instead of coerced to NaN on edit. Added a focused inline `Node` configurator (label/sublabel/shape/color/subtle) because the shared `node.jsx` configures a whole node/edge graph. Also fixed the **`Card`** bridge, which dropped `surface`, `heroSeparator`, and `heroSeparatorShape` (they now round-trip). Only `PageLayout` (page layout root) and `Outlet` (shared-layout placeholder) remain intentionally non-configurable. |
| 2026-07-07 | **a1-web ApiGrid data-source samples:** added `apps/a1-web/src/services/dataSources/apigridSamples.ts` with the five ApiGrid dashboard datasets (`ApiGrid APIs`, `ApiGrid consumers`, `ApiGrid activity`, `ApiGrid traffic`, `ApiGrid request logs`) transcribed from the `_tmp-design_handoff_apigrid` handoff `data.js`, and registered them in `SAMPLE_DATA_SOURCES` so they appear as one-click "Add sample" datasets on the Data sources page and can be bound into pages (`apigrid_apis`, `apigrid_consumers`, …). Array fields (`methods`, `scopes`) are flattened to comma-joined text, activity `<b>` markup is stripped to plain content, and the deterministic `traffic`/`logs` series are the exact prototype-generated values. No design-system component API changed. |
| 2026-07-13 | **a1-web product tour** (A1-83): added an app-local, first-visit guided walkthrough that spotlights the TopHeader navigation, its search/help controls, and the current workspace. It composes existing A1 Button, IconButton, Heading, Paragraph, Stack, ButtonContainer, and StepTracker components; traps keyboard focus, supports Escape, blocks background pointer interaction, remembers dismissal/completion locally, and can be restarted from Ask Help. The only local styling is the token-backed runtime spotlight/card geometry, which is not a reusable design-system component yet. New copy is in `system/labels/app.json` with all supported translations. No design-system component API changed. |
| 2026-07-07 | **a1-web project themes:** added optional project-scoped `theme` metadata to Projects. The project settings dialog exposes a theme selector, project cards show the selected theme, project JSON import/export and shared cloud sync preserve it, and the editor canvas plus launched prototypes apply the selected theme locally through a project theme scope rather than mutating the global a1-web app shell theme. |
| 2026-07-07 | **Card hero separator:** added `heroSeparator` and `heroSeparatorShape` to React Card hero-icon mode. The shaped SVG separator fills with the Card content surface, separates the hero icon area from the content, flips from horizontal to vertical when the hero becomes a left strip, and is exposed in Storybook plus the a1-web Card configurator as "Separator". |
| 2026-07-07 | **Card accent surface:** added `surface="accent"` to React Card. The accent surface uses the stronger action background step (`semantic.color.action.backgroundHover`, darker in light mode and lighter in dark mode), applies the primary-action foreground as local text/link/border/icon token aliases so bright-accent themes like Kong use dark text, disables status stripe rendering at runtime, and the a1-web Card configurator now clears/hides status controls, shows surface swatches, and warns that complex nested controls/explicit colour props still need contrast review. |
| 2026-07-07 | **Card status frame polish:** status Cards now color the full border with the status tone and size the inline-start stripe to `max(8px, card radius, component.card.statusStripe.width)`, so the stripe follows rounded card corners instead of reading as a separate second border. |
| 2026-07-07 | **SectionSeparator component:** added a React-only `SectionSeparator` for organic responsive transitions between neighboring Sections. It supports `topSurface` / `bottomSurface`, independent inverse scopes per side, seven SVG path shapes, responsive height sizes, optional Divider-token border highlighting, Storybook coverage, package exports/types, component registry docs, and an a1-web Layout & Display configurator. |
| 2026-07-07 | **a1-web Features page title:** converted the Features page header to the shared local `PageTitleArea` component for consistent breadcrumb, h1, and description treatment. No design-system component API changed. |
| 2026-07-07 | **Toolbar delayed tooltips:** Toolbar buttons, toggles, menu triggers, overflow controls, and icon-only group options now wrap their triggers in the shared Tooltip component with a 700ms delay instead of relying on native `title` attributes. Visible-label tools suppress the extra tooltip. |
| 2026-07-07 | **a1-web Accessibility report stats:** converted Accessibility report metric cards to compose the shared React `Stat` component with icons suppressed and semantic error highlighting for non-zero issue counts. No design-system component API changed. |
| 2026-07-07 | **Data Table radius token + Lumen treatment:** added `component.dataTable.borderRadius` so Data Table containers no longer inherit Card radius directly. Default output aliases the existing Card radius; Lumen overrides Data Table radius to `base.radius.lg` in light/dark modes so table surfaces are less rounded than Lumen cards. |
| 2026-07-07 | **a1-web Accessibility foundation title:** updated the Accessibility foundation page to use the shared local `PageTitleArea` component for its breadcrumb, h1, and description instead of hand-composing the header Section. No design-system component API changed. |
| 2026-07-07 | **Card navigation border tokens + Lumen treatment:** added `component.card.navigation.borderColor*` tokens and wired Card navigation state borders to them. Lumen maps navigation Card borders to subtle blue by default, with stronger blue hover/pressed states. |
| 2026-07-07 | **Lumen standard Tabs:** increased Lumen line-tab padding, font size, and indicator thickness while keeping the divider on `semantic.color.border.subtle`; softened Lumen's subtle border value in both dark/default and light/inverse modes to reduce contrast. |
| 2026-07-07 | **Lumen Card border width:** increased Lumen Card borders to `3px` in both default/dark and light/inverse overrides so dimensional cards have a stronger raised edge. Rebuilt generated theme/token outputs via `npm run build:a1-web`. |
| 2026-07-07 | **Button, Toolbar, SegmentedControl, and compact Field polish:** increased Button font weight one step across sm/md/lg sizes and the default Button typography token; made Toolbar tool radii derive from the bar radius minus toolbar padding so the inner controls follow the same arc; made SegmentedControl and Tabs `variant="segment"` use the same subtle border treatment as Toolbar with accessible/high-contrast escalation; added explicit segmented height tokens for sm/md/lg and normalized `size="default"` to `md` so the SegmentedControl size prop visibly changes the control; increased compact Field border radius one step and wired compact Field CSS to the existing token. Lumen tertiary Button/IconButton text now uses a brighter info-blue treatment in light and dark modes. |
| 2026-07-07 | **Lumen contrast + dimensional controls:** added reusable `component.field.borderColor` / `component.field.shadow`, `component.message.badge.subtleBorder*`, and `component.button.secondary.boxShadow*` tokens so themes can tune field-family controls, MessageBadge outlines, and secondary Button depth without local CSS. Lumen now softens MessageBadge borders by one step, softens Field/TextField/Select/Textarea borders with subtle inset/outset shadows, makes secondary Buttons subtler with blue borders/text and mode-aware white/dark surfaces, improves Card icon contrast, improves tertiary Button contrast, and lightens the light-mode page surface one ramp step. Rebuilt generated theme/token outputs via `npm run build:a1-web`. |
| 2026-07-07 | **Lumen theme:** added a selectable organic dimensional theme (`.a1-theme-lumen`) with explicit light/dark mode support, deep blue-violet dark surfaces, airy light surfaces, bright blue primary actions, Baloo 2 heading/display typography, Nunito body/Button typography, violet/info and rose/error pops, rounded card/control radii, stronger layered Card/Button glow shadows, subtle gradient/texture page styling, inverse-scope restores, and a1-web settings registration. Rebuilt generated theme/token outputs via `npm run build:a1-web`. |
| 2026-07-07 | **Chart examples — NFL scorigami canvas:** added a read-only Scatter Chart child example that uses real NFL Scorigami score-frequency data from `nflscorigami.com/data`, rendered as a pannable/zoomable A1 `Canvas` matrix of score-count nodes. The example keeps the main Scatter Chart configurator unchanged and uses the existing secondary example route pattern. |
| 2026-07-07 | **Chart examples + xs chart height:** added `height="xs"` for tiny embedded Recharts surfaces (`component.chart.height.xs` = `base.spacing.96`) and exposed it in React typings, Storybook controls, and a1-web chart configurators. Added read-only secondary chart example pages under the Bar Chart and Area Chart tree nodes: a compact live-updating API Gateway status card, a live API incident-recovery scenario that cycles success → warning → error → success, a wider Authentication API uptime timeline, and an API Monitoring Area Chart card. These examples use only A1 components, hide the non-applicable configuration panel, and route through the existing `componentExamples.json` child-page pattern; the main chart configurators remain unchanged. |
| 2026-07-07 | **a1-web Home dashboard link:** replaced the Home stats-strip dashboard promo card with a simple A1 `Link` using the `link` icon and "System dashboard" label. No design-system component API changed. |
| 2026-07-07 | **Heading accent background tokens:** added `component.heading.accent.*` tokens and CSS fallbacks so accent headings keep the semantic accent text color by default, while themes can opt into a contrast background. Kong maps accent headings to `accent.500` on `accent.1000` with small tokenized padding/radius for legibility. Rebuilt generated token/theme outputs via `npm run build:tokens`. |
| 2026-07-07 | **Kong dark-mode inverse restore:** narrowed Kong's dark/inverse theme selector and added an explicit light restore for `.a1-inverse` inside Kong dark mode. Inverse Sections now flip to the Kong light page surface (`neutral.50`) and restore light-mode Button/control tokens instead of inheriting dark-mode foregrounds. Rebuilt generated token/theme outputs via `npm run build:tokens`. |
| 2026-07-07 | **a1-web Components sidebar search autofill guard:** the Components navigation search now sets `autoComplete="off"`, a stable `name="a1-components-sidebar-search"`, and common password-manager ignore attributes so browser profile autofill does not inject personal data when navigating to Menu/Context Menu component pages. No design-system component API changed. |
| 2026-07-07 | **Kong dark-mode Link color:** Kong now overrides `component.link.inverse.*` in dark/inverse mode so Links render with `accent.500` by default, with nearby accent ramp values for hover and pressed states. Rebuilt generated token/theme outputs via `npm run build:tokens`. |
| 2026-07-07 | **Kong TopHeader logo + off Switch thumb:** added `component.topHeader.logoForeground` and `component.switch.thumb.background` tokens. Kong maps the TopHeader logo to black in light/default mode and `accent.500` in dark/inverse, and maps the off Switch thumb to `accent.500` so it matches the checked thumb. Rebuilt generated theme outputs via `npm run build:a1-web`. |
| 2026-07-07 | **Kong accent ramp + Slider detent:** added Kong `base.color.accent.25` so the theme no longer falls back to the default purple ramp for the 25 step, softened Kong `accent.600` to a closer shift from `accent.500`, and added `component.slider.activeColor` so Kong active Slider detent labels resolve to black while the track fill remains chartreuse. Rebuilt generated theme outputs via `npm run build:a1-web`. |
| 2026-07-07 | **Kong control color pass:** added Action Tile icon color tokens, Chip selected state tokens, Switch checked track/border tokens, and a Slider thumb color token. Kong maps Action Tile icons to black in light mode and `accent.500` in dark/inverse, selected Chips to primary Button colors, checked Switch tracks to black with an `accent.500` thumb, and Slider thumbs to black. Rebuilt generated theme outputs via `npm run build:a1-web`. |
| 2026-07-07 | **Kong Card icon colors:** added Card icon and hero icon color tokens (`component.card.icon.background` / `foreground` and `component.card.hero.background` / `foreground`) and updated Card CSS to consume them. Kong light mode maps small Card icon blocks to `accent.0` with black icons and hero icon bands to `accent.500` with black icons. Rebuilt generated theme outputs via `npm run build:a1-web`. |
| 2026-07-07 | **Kong selected-control states:** added selected hover/pressed tokens for Tabs and Pagination (`component.tab.selectedBackgroundHover` / `selectedBackgroundPressed` / selected foreground state tokens, plus matching Pagination selected background/border/foreground state tokens) and extended TreeMenu selected foreground state tokens. Kong selected Tabs, Pagination, and TreeMenu now use `accent.500` + `accent.1000` by default, `accent.0` on hover, and flip to `accent.1000` background with `accent.500` text on active. Kong secondary Button now uses `accent.500` border, `accent.0` background, and `accent.800` text, with active flipped to `accent.1000` background and `accent.500` text. Rebuilt generated theme outputs via `npm run build:a1-web`. |
| 2026-07-07 | **Kong tertiary button foreground:** darkened Kong tertiary Button text by two accent-ramp steps in light/default mode (`accent.800`/`900`/`1000`) and added a dark/inverse mode override (`accent.400`/`300`/`200`) so color-mode rules do not replace the theme treatment. Rebuilt generated theme outputs via `npm run build:a1-web`. |
| 2026-07-07 | **Kong theme refinements:** Kong now uses `neutral.50` for page surfaces and `neutral.800` in dark/inverse contexts, rounds IconButton via `component.iconButton.borderRadius`, uses filled very-dark neutral secondary Button colors in light/dark/inverse contexts, and sets selected pill/segment Tabs to `accent.500` background with `accent.1000` foreground. Added `component.tab.selectedBackground` / `selectedForeground` tokens so tab selected colors can be themed without custom selectors. Rebuilt generated theme outputs via `npm run build:a1-web`. |
| 2026-07-07 | **Kong primary button foreground:** Kong theme primary Button foreground, hover foreground, and pressed foreground now alias `base.color.accent.1000` over the chartreuse `accent.500` action surface in both default/dark and explicit light mode. Rebuilt generated theme outputs via `npm run build:a1-web`. |
| 2026-07-07 | **Kong theme:** added a selectable system theme (`.a1-theme-kong`) with a neutral ramp from `#717A74`, chartreuse accent ramp from `#BEFF00`, Funnel Sans typography, and pill-shaped Button/Text Field controls through theme token overrides. Rebuilt generated theme outputs via `npm run build:a1-web`. |
| 2026-07-07 | **a1-web Theme editor JSON view:** added a direct **View JSON** toolbar action for open themes, renamed the theme workspace sidebar's Code category to JSON, made the saved theme JSON editable/applyable from the editor, and hid the theme-generation AI panels/copy from the Theme editor. No design-system component API changed. |
| 2026-07-07 | **StickyActions dark-mode surface:** `--component-sticky-actions-background` is now part of the generated light/dark color-mode contract and aliases `--semantic-color-surface-page`, so the fixed action bar follows the active page surface in system dark, explicit dark, inverse, and light restore scopes instead of staying pinned to the light token value. Rebuilt generated React/Pure color outputs via `npm run build:tokens`. |
| 2026-07-07 | **a1-web Data Viz color controls:** hid the Recharts chart Color configurator control while the subtle treatment is paused, and switched the Explore dashboard chart cards back to the standard semantic series colors instead of opting into `variant="subtle"`. The underlying React chart `variant` prop remains available for future iteration. |
| 2026-07-07 | **Color mode light contract restore** (A1-1436): `system/build-themes.mjs` now emits a generated `:root` light-mode baseline from `LIGHT_MODE_DECLARATIONS`, so Auto/System mode on a light OS resolves status, action, surface, and component aliases the same way as forced Light mode. It also generates theme-specific restore blocks after the generic `html.a1-theme-light` mode declarations, limited to declarations that light mode can overwrite. This keeps selected theme surfaces/action/status aliases intact when users force Light mode. Fresh also has an explicit-light body gradient restore so its mint gradient stays light even on systems that prefer dark. Rebuilt `color-scheme-modes.css`, bundled `color-scheme.css`, React token copies, and React Native generated token outputs via `npm run build:themes`. |
| 2026-07-07 | **a1-web Explore dashboard:** added `/dashboard` under the Explore navigation, linked from the Home stats strip. The page composes A1 cards, controls, stats, and Recharts wrappers to visualize backlog health, component coverage, token output, system flow, rules, and labels, with per-card segmented views and links into each detailed area. |
| 2026-07-06 | **Data Viz / Recharts chart components:** added Recharts 3.9.2 as the React package charting dependency and introduced React-only wrappers for `LineChart`, `BarChart`, `AreaChart`, `ComposedChart`, `PieChart`, `ScatterChart`, `RadarChart`, `RadialBarChart`, `FunnelChart`, `TreemapChart`, `SankeyChart`, and `SunburstChart`, with `Chart` retained as a generic Cartesian compatibility export. Added component tokens (`system/tokens/component/chart.json`) for axes, polar grids, legend, tooltip, focus ring, chart heights, line weight, area opacity, and semantic/status series tones; added `system/rules/chart.yaml`; added Storybook coverage; and surfaced each chart type in a1-web's Data Viz category alongside Node, with live configurators, generated prop table entries, editor Add catalog entries, page-renderer support, and editor props controls. Recharts animation is disabled by default so chart motion does not bypass A1 motion tokens. |
| 2026-07-06 | **Figma Dialog component** (A1-1418): created the Dialog Figma assets on the Dialog page. Final structure: `Dialog` is a grid auto-layout component set (`node 228:1628`) with 24 variants across `Size` (`sm`, `md`, `lg`, `xl`) and `Status` (`none`, `success`, `error`, `warn`, `info`, `neutral`), ordered so the default is `Size=md, Status=none`. `Dialog Hero Icon` is a separate child component set (`node 228:1013`) for status icons. Dialog owns editable `Title` / `Body`, `Show close`, `Show footer`, `_body` and `_footer` slot frames, variable-bound panel/border/status colors, Components collection Dialog FLOAT variables, locked row/column labels, docs/examples, and repo-side Code Connect (`Dialog.figma.ts`). Runtime-only behavior remains documented as Figma gaps: open-state ownership, native `<dialog>`, Escape/backdrop dismissal, focus trap/restore, `onClose` callback semantics, `icon` override, refs, and arbitrary native dialog attributes. |
| 2026-07-06 | **Figma Menu component** (A1-1420): created and repaired the Menu Figma assets on the Menu page. Final structure: `Menu` is a single vertical auto-layout shell component (`node 218:1177`) with a section label and `_items` slot frame; `Menu Item` is a separate child component set (`node 218:1176`) with 7 `State` variants (`default`, `hover`, `focus`, `pressed`, `active`, `disabled`, `destructive`). Menu Item owns editable `Label` / `Shortcut`, icon instance swap, icon/shortcut visibility, disabled opacity, active fill, destructive tone, focus treatment, and variable-bound icon vectors. Menu owns `Aria label`, `Section label`, item slot instance swaps, and item slot visibility toggles. The Menu Item set uses grid auto layout with 64px component-set padding; the Menu shell intentionally does not use grid layout. Added locked auto-layout row/column labels, shared Color bindings, Menu FLOAT variables in the Components collection, `Menu/Section label` typography, docs/examples, dark-mode validation, and repo-side Code Connect templates (`Menu.figma.ts`, `MenuItem.figma.ts`). Live Code Connect metadata remains blocked by Figma's Organization or Enterprise plan requirement. |
| 2026-07-06 | **a1-web Presentation walkthrough** (A1-1434): added a focused `/presentation` Explore page that renders outside the normal app `PageLayout` chrome so the TopHeader, sidebars, sheets, search, help, and settings UI are hidden while presenting. The deck now uses six direct-markup content slides with alternating demo placeholder screens and speaker-note paragraphs below each slide card, plus A1 components/tokens and a small token-bound page stylesheet for viewport-contained layout, slide transitions, and visual framing, with Arrow key navigation and Escape-to-close. No design-system component API changed. |
| 2026-07-06 | **Error border token darkened:** `--semantic-color-status-error-border` now aliases `error.400` instead of `error.300` for default/light color modes and theme overrides, with generated React, React Native theme color, and Pure CSS outputs rebuilt. The A1 Figma file's `color/status/error/border` Light mode variable was synced to alias `error/400`; Dark mode keeps its existing darker alias. |
| 2026-07-06 | **Text Field error border + Figma compact typography:** React TextField/Textarea error states now use `--semantic-color-status-error-border` for both the side accent and the rest of the border, instead of mixing error background on the accent with error border on the outline. The Figma Text Field component (`node 148:1360`) now mirrors this by keeping error input strokes and error accents bound to `color/status/error/border`, and compact `Value`, `Hint`, and `Error` layers use the smaller `body/xs` text style so compact variants match the React visual scale. |
| 2026-07-06 | **Figma Text Field component** (A1-959): created and validated the Text Field component set in the A1 Figma file on the Text Field page (`node 148:1360`) with 21 variants covering `Size` (`comfortable`, `default`, `compact`) and aggregate `State` (`default`, `hover`, `focus`, `required`, `error`, `readOnly`, `disabled`). `LabelPosition` was intentionally removed from the Figma variant API; the component represents the label-above pattern only. Added editable text component properties for `Label`, `Value`, `Hint`, `Error`, and `Required label`, boolean `Show label` / `Show hint` toggles, token-bound fills/strokes/radius/dimensions, field-specific Light/Dark Color aliases, Field FLOAT variables for exact React field dimensions, semibold Field label text styles, documentation/example frames (`node 148:1361`), and a repo-side Code Connect template (`packages/figma/code-connect/TextField.figma.ts`). Follow-up validation captured a1-web React screenshots for all 21 size/state combinations and overlaid live Figma instances on the validation board (`node 184:1334`), then corrected focus ring/focus border bindings, default/comfortable radii, and error text colour binding to `color/status/error/text` / `--semantic-color-status-error-text`. Live Code Connect metadata remained blocked by Figma's Organization or Enterprise plan requirement. |
| 2026-07-06 | **a1-web Backlog table filters:** the All tickets DataTable view now has native filter chips for Type, Status, Priority, Size, and Scope. These table-local filters stack with the shared Backlog panel filters/search, keep display cells formatted, and use hidden row filter fields for stable matching. No design-system API change. |
| 2026-07-06 | **a1-web Backlog Figma component tickets:** added an automatic backlog seed that creates one M-size feature ticket for every component and registry-only component row not yet marked available in Figma, then repairs generated tickets if the evaluated priority/size changes. Priorities are based on Figma design criticality (P0 foundational primitives/core controls, P1 common app structure/forms/feedback, P2 specialized components, P3 experimental/niche rows). Tickets are scoped as `component` with a stable `figma:<component-id>` reference, so seeding is idempotent across reloads, and each description includes the first-pass Figma creation checklist: read the workflow, mirror React props, bind variables/styles, verify light/dark modes, document mapping gaps, update Code Connect where possible, and mark Figma coverage only after the asset is usable. |
| 2026-07-06 | **Component registry Figma coverage:** added **Figma** to the `components.md` package key and every component coverage table so the registry can show which components exist in the A1 Figma design system file. The a1-web Components inventory and component detail package grid now include the same Figma package option. Button is currently marked implemented, matching the A1-419 Figma component set and Code Connect template; other components remain unmarked until their Figma assets are created and documented. |
| 2026-07-06 | **Figma Button component** (A1-419): created the Button component set in the A1 Figma file on the Button page (`node 123:701`) with 180 variants covering `Variant` (`primary`, `secondary`, `tertiary`, `destructive`, `success`), `Size` (`sm`, `md`, `lg`), `State` (`default`, `hover`, `focus`, `pressed`, `disabled`, `loading`), and `IconPosition` (`start`, `end`). Added label, icon visibility, and icon instance-swap component properties, token-bound colors/size variables, dark-mode variable coverage, focus-ring and loading visual states, prototype hover/press/focus reactions, documentation/examples frames, and a repo-side Code Connect template under `packages/figma/code-connect/`. Follow-up repair bound nested icon vectors to Button foreground variables (removing black literal fills), tied labels to Button-specific text styles matching React weights (`sm` medium, `md` semibold, `lg` bold), changed focus to an absolute outer ring, refreshed paint fallback colors so Figma previews resolve variables correctly, ordered the set so Figma asset search inserts the secondary/md default, refactored dark-mode ownership so Button color variables are single-mode aliases to shared `color/button/*` variables in the Color collection, added destructive/success component color roles so hover and pressed states match React, and changed dark-mode primary Button background/border to accent-300. Live Figma Code Connect publishing was blocked by Figma's Organization or Enterprise plan requirement, so the checked-in template documents the React mapping until the component is published from a supported team library. |
| 2026-07-05 | **Overlay component** (A1-417): added the React `Overlay` component for rare full-screen status announcements with `open`, `onClose`, `status`, `icon`, `title`, `body`, `actions`, `dismissLabel`, and `children` props. It uses native `<dialog>` modal semantics, a translated dismiss label (`system/labels/overlay.json`), status colour tokens, tokenized full-screen transparency/backdrop/icon sizing (`system/tokens/component/overlay.json`), governed rules in `system/rules/overlay.yaml`, Storybook stories, and an a1-web configurator under the Overlay category. React only; no Pure/Native/Web Component coverage added. |
| 2026-07-05 | **a1-web Release notes package coverage:** the Releases page now imports package changelogs alongside the app changelog: React has both detailed (`packages/react/CHANGELOG.md`) and simplified (`apps/a1-web/src/pages/publicReactReleaseNotes.md`) notes, while React Native, Web Components, Pure CSS, and the ESLint plugin now have starter package changelogs. Release sources switch with an A1 `ChipGroup` filter in the sidebar independent of the Simplified/Detailed mode, release versions remain in the `TreeMenu`, Simplified/Detailed moved from the sidebar into the page header, and the header now uses `contentWidth="md"` to align with the release-note cards. The main pane labels each release section with its source, search follows the active source, and new package/source labels live in `system/labels/app.json` with supported translations. No design-system component API changed. |
| 2026-07-05 | **a1-web Backlog swimlane controls:** the board no longer renders the swimlane visibility toolbar once the board uses its small-screen tab-per-lane layout, avoiding duplicate lane switching controls. At larger breakpoints the same A1 `Toolbar` now opts into `overflow` with a localized `moreSwimlanes` label in `system/labels/app.json`, keeping the lane toggles on one row instead of wrapping. No custom CSS or design-system API change. |
| 2026-07-05 | **a1-web Backlog filter rail placement:** Backlog's view/search/filter panel now uses `PageLayout`'s fixed-width sidebar slot at non-mobile breakpoints, moving the rail from the right side to the left without the proportional aside-grid gap. Added a start-side config-rail modifier so the left rail uses the correct right border while preserving the existing xs/sm BottomSheet behavior. No design-system component API changed. |
| 2026-07-05 | **a1-web public Release notes** (A1-407): Releases now open on `apps/a1-web/src/pages/publicReleaseNotes.md`, a curated public-facing Markdown source with simpler summaries and less internal implementation detail than the app changelog, and include an A1 `SegmentedControl` for switching between Simplified and Detailed notes. The release navigation moved from tabs into the app shell's left `SideNav` using A1 `TreeMenu`, with search and the segmented control in the sidebar header; the main pane renders each changelog item as its own A1 `Card`. Search now shows all matching releases in the main pane separated by release version and highlights matching text with semantic inline `<mark>` elements. The release `SideNav` is controlled as expanded on large breakpoints (`collapsed={false}`, no collapse button) and opens from a localized page action on overlay breakpoints. New segmented-control labels, tree/open labels, page descriptions, and empty-state copy live in `system/labels/app.json` with supported translations and are consumed through `useLabel`. No design-system component API changed. |
| 2026-07-05 | **a1-web Release notes search + inline Markdown:** Releases page now filters generated changelog tabs and entries with a compact `SearchField`; empty-state copy and the field label live in `system/labels/app.json` with supported translations and are consumed through `useLabel`. Release-note lines render common inline Markdown (`**bold**`, `*italic*`, backtick code, and Markdown links) into semantic inline markup using native emphasis plus A1 `Code`/`Link`. No design-system component API changed. |
| 2026-07-05 | **Wireframe redacted theme removed** (A1-403): removed the `system/themes/wireframe-redacted/` source theme, the generated `.a1-theme-wireframe-redacted` React theme output, the a1-web redaction stylesheet/import, the Settings theme option, and the Priority Guide **Preview redacted** action. Priority Guide preview now targets only the standard Wireframe theme. No replacement theme or redaction utility was added. |
| 2026-07-05 | **TreeMenu collapsed variant** (A1-377): added `variant="collapsed"` to render root items as icon-only triggers with each branch's children shown in an anchored A1 `Menu` flyout; leaf root items still select or navigate directly. Collapsed mode reuses the existing TreeMenu for flyout children so nesting, selected state, expansion, inline rename, and drag hooks stay on the component contract. Added root-icon roving keyboard behavior (Arrow Up/Down/Home/End, Arrow Right opens, Arrow Left closes), active ancestor highlighting, tokenized collapsed item sizing (`component.treeMenu.collapsed.itemSize`), matched collapsed root icon color/size to expanded item icons, a TreeMenu disabled-opacity token, localized flyout labels in `system/labels/tree-menu.json`, Storybook collapsed/inverse/narrow stories, and the a1-web configurator Variant control/snippet/editor bridge (defaulting the page to collapsed so the menu flyout is visible immediately). React only; no Pure/Native/Web Component coverage added. |
| 2026-07-05 | **Snackbar visibility in dark mode** (A1-313): the default Snackbar surface now inverts with the document colour scheme instead of being pinned to fixed neutral literals — a dark toast on a light page (unchanged) and a **light** toast on a dark page, so it no longer disappears against a dark background. React (`packages/react/src/components/snackbar/snackbar.css`): `.a1-snackbar--default` reads `--semantic-color-surface-page` / `--semantic-color-text-default`, which resolve inside the snackbar's own `.a1-inverse` scope (so they flip by document mode), with `--component-snackbar-default-*` kept as the out-of-scope `var()` fallback. Web Component (`packages/web-components/src/components/snackbar/a1-snackbar.js`): defines local `--a1-snackbar-surface`/`-border`/`-foreground` in its shadow styles, defaulting to the component tokens and flipping to the light neutral ramp (`--base-color-neutral-0` surface / `-900` text) under `@media (prefers-color-scheme: dark)` and `:host-context(html.a1-theme-dark)` (explicit `.a1-theme-light` restores the dark toast); the action and close controls now read the flipped foreground. React Native already inverted (`colors.textDefault` surface / `colors.pageBg` text). The `system/tokens/component/snackbar.json` `default.*` values are unchanged (still `neutral.900`/`neutral.0`); only their `$description`s were updated to note they are the out-of-scope fallback. Verified in Storybook (React) and a Vite harness (Web Component): white toast + dark text on the dark page, dark toast + light text on the light page; focused axe run clean in both modes. CSS/token-doc only; no public prop change. |
| 2026-07-05 | **Snackbar multiple stack + auto dismiss** (A1-313): React Snackbar now supports `autoHideDuration` (paused while hovered/focused), `dismissible`, and direction-aware off-screen close animation; new React `SnackbarStack` renders intentional multiple snackbar stacks with tokenized spacing, width, offsets, and the existing border/shadow separation. Storybook and the a1-web Snackbar configurator gained single/multiple mode and auto-dismiss controls. Web Component Snackbar gained `auto-hide-duration` and matching close animation; React Native already had `duration`. |
| 2026-07-05 | **a1-web Backlog ticket creation snackbar** (A1-179): ticket creation now uses the existing Snackbar action slot to offer the translated **Open ticket** label, routing directly to the newly-created `/backlog/A1-{n}` page without adding Snackbar API or styling. Build with AI plans now require new user-facing copy to live in `system/labels/` with supported translations and label-resolver consumption. No design-system component API changed. |
| 2026-07-05 | **Typography overflow wrapping** (A1-398): React Paragraph, Heading, and List item content now allow emergency wrapping for long unbroken prose fragments such as paths, props, and token names, with matching HTML/CSS typography classes in Pure. This fixes the a1-web Releases page horizontal overflow without a page-specific CSS override. CSS-only; no component API changed. |
| 2026-07-05 | **Dialog vertical-only scrolling** (A1-397): `.a1-dialog__body` now sets `min-width: 0` and `overflow-x: clip` alongside the existing vertical scroll, preventing wide child content such as ticket detail toolbars/tabs from creating horizontal scroll inside dialogs. Added governed rule `dialog-vertical-scroll-only` in `system/rules/dialog.yaml` and documented it in the Dialog registry notes. CSS-only; no Dialog API change. |
| 2026-07-05 | **a1-web Backlog ticket mobile toolbars:** the ticket dialog and standalone ticket page now set ticket triage Toolbars to `fullWidth` and enable existing Toolbar/ToolbarGroup overflow for type, status, priority, and size controls. This uses the design-system overflow API instead of local CSS; no design-system component API changed. |
| 2026-07-05 | **a1-web Backlog Build with AI in production:** the ticket detail page now renders the Build with AI section in production builds. The feature still opportunistically uses a local Ollama runner when one is reachable, but the built-in deterministic planner keeps the deployed tab useful without browser API keys or local-only setup. Added a required end-of-ticket CSS review rule to `project-workflows.md`; no design-system component API changed. |
| 2026-07-05 | **Menu embedded label styling:** `.a1-menu` now overrides embedded field and choice-group labels to the menu's compact muted section-label treatment, and section labels use semibold rather than the bold token. This keeps settings-style menus from rendering full-form label emphasis inside compact menu panels. CSS-only; no public prop change. |
| 2026-07-05 | **BottomSheet mobile height cap:** `.a1-bottom-sheet` now caps its expanded height to the viewport area below the sticky `TopHeader` by default: `100dvh - --component-top-header-height - safe-area-inset-top`. Consumers without top chrome can override `--a1-bottom-sheet-block-start-offset`. CSS-only; no public prop change. |
| 2026-07-05 | **Button loading spinner reuse** (A1-308): `Button loading` now renders the shared React `CircularProgress` component instead of a custom `.a1-button__spinner` border/keyframe spinner. Button keeps the same API, inert behavior, `aria-busy`, and icon-slot sizing; Button CSS only overrides CircularProgress CSS variables for icon-sized `currentColor` fill with a transparent track. |
| 2026-07-04 | **Menu — item text + icon bumped one step.** Menu item text went from `--semantic-font-size-body-sm` (0.875rem) to `--semantic-font-size-body-md` (1rem) in `menu.css`, and `component.menu.itemIconSize` from `18px` to `20px` in `system/tokens/component/menu.json` (rebuilt to `--component-menu-item-icon-size: 1.25rem`). Section labels and item shortcuts stay at `body-xs` (secondary text). Token/CSS-only — no Menu prop, story, or configurator API change; the a1-web Menu configurator and stories read the token and pick up the new size automatically. |
| 2026-07-04 | **Priority Guide editor (a1-web) + Wireframe theme.** New a1-web editor at `/priority-guide` (under **Editors**): `apps/a1-web/src/priorityGuide/` holds the store (`priorityGuideStore.ts` — localStorage `a1-priority-guides`, seeded from the four bundled example guides copied into `seeds/`, extended with `projectId`/`pageId`) and the guide⇄PageDefinition conversion (`convert.ts` — `guideToPageDefinition` emits real A1 nodes in priority order and stashes the source guide in `page.meta.__priorityGuide` for a lossless `pageDefinitionToGuide` round-trip). The page component is `apps/a1-web/src/pages/PriorityGuideEditor.jsx` (guide library, three-pane editor with inline-editable fields, ranked-item CRUD/drag-reorder, group support, two-way JSON). Wired into `main.jsx` (PAGES/titles/nav/render) and folded into the cloud-sync envelope (`cloudSync.js` — `guides` key, additive/back-compatible). "Convert to page" writes a project page via `projectStore.commitPageJson`; "Preview wireframe" opens the page standalone with a new `?theme=wireframe` param on `EditorPreviewPage` (the app's authoritative theme effect seeds `theme` from it in standalone mode; not persisted). |
| 2026-07-04 | **Wireframe theme** (`system/themes/wireframe/`, selector `.a1-theme-wireframe`): a deliberately un-designed low-fidelity theme in black/grey/white only — a single hueless neutral ramp with every accent/status collapsed to grey, all shadows (`--semantic-shadow-*`, card/dialog/switch) set to `none`, zero-radius corners, and a monospace stack for body/heading/display/button type. Because Style Dictionary bakes accent literals into component-tier tokens at build time, overriding the base ramps alone leaves buttons/links/status chrome coloured; the theme therefore also overrides the accent-derived component tokens directly: `component.button.{primary,secondary,tertiary,focusRing}`, `component.link.*`, `component.notification.neutral.background`, `component.card.status.*`, `component.statusBar.{fillBackground,borderColor}`, `component.circularProgress.fillColor`, `component.field.focusRingColor`, `component.actionTile.focusRing`, `component.heading.mark.{underline.color,highlight.backgroundColor}`, `component.segmented.inactive.color`, `component.stepTracker.dot.color`, `component.section.gradient.highlight`, and `semantic.color.status.{error,warn}.text`. Verified with a live-DOM audit: zero saturated colours across the rendered kitchen-sink page. Registered in a1-web (`themeOptions` + the `classList.toggle` theme effect in `main.jsx`); light-mode only, web only (no React Native). Built via `npm run build:themes`. This is the target theme for the a1-web "convert a Priority Guide to a wireframe" feature. |
| 2026-07-03 | **Consumer-audit follow-ups** (`@gtivr4/a1-design-system-react` 0.26.0): added `license`, `repository`/`bugs`/`homepage` to package.json and shipped `CHANGELOG.md` in the tarball; excluded the unexported experimental `data-grid` wrapper (its `react-data-grid` dep is dev-only); rewrote `src/index.d.ts` to mirror `index.js` (was 20 exports behind) and added nine missing `.d.ts` files (Date/Time/Phone/Zip/CreditCard fields, `Labels`, `TokenSelect`, `TopHeader`, `DataTableFilters`) — `pack:check` now blocks declaration drift; `TextField` warns once in dev when `placeholder` is passed and `TextareaField` now strips it too (it previously leaked to the DOM against the no-placeholder contract); Snackbar's ignored `variant`/`inverse` marked `@deprecated` in its `.d.ts`; documented the `onChange` event-vs-value convention in `guidelines/components.md`. SideNav embedded variant, `onChange` unification, and generated declarations triaged into `TODO.md`. |
| 2026-07-03 | **npm packaging fix + shipped guidelines** (`@gtivr4/a1-design-system-react` 0.26.0): the `files` whitelist only shipped `src/index.js`, so the four runtime `.js` modules (`structure-utils.js`, `FieldsetContext.js`, `maskUtils.js`, `customIconRegistry.js`) were missing from every published tarball and 15 shipped components had unresolvable imports — a fresh install could not bundle (consumers needed a postinstall patch). Whitelist now ships `src/**/*.js` (excluding `src/storybook/**`). Wrote the five companion guideline files `Guidelines.md` had always promised (`setup.md`, `components.md`, `icon-discovery.md`, `tokens.md`, `styles.md`), refreshed the hub's stale export list and theming table, rewrote the package README (correct package name, required `tokens.css` import, fonts, PostCSS custom-media), and added `./utilities/sr-only.css` to the exports map. New repo-root guard `npm run pack:check` (`scripts/verify-pack.mjs`) dry-packs the tarball and fails on unresolvable imports, missing entry points, missing guideline files, or Guidelines/`src/index.js` export drift. |
| 2026-07-02 | **Chip component** (A1-390): added React `Chip` and `ChipGroup` for compact filters, selectable tag rows, menu-trigger filters, and navigation chips. `ChipGroup` supports no selection, single selection, multiple selection, controlled/uncontrolled values, size, and wrap on/off; `Chip` supports icons, short titles, selected/disabled states, anchors via `as`/`href`, and composed A1 `Menu` content. DataTable filters now use Chip instead of local chip CSS. Added tokens in `system/tokens/component/chip.json`, rules in `system/rules/chip.yaml`, Storybook stories, a1-web Actions & Controls registration/configurator, and editor Add panel support through a configured `ChipGroup` node. |
| 2026-07-02 | **a1-web project overview pages:** project overview page lists now render each page as an interactive `ActionTile` inside an `ActionTiles` stack, preserving the page icon, title, nesting-level badge, description, and direct page navigation. The overview content width was reduced from `xl` to `lg` so page tiles read as a narrower working list. |
| 2026-07-02 | **a1-web Editor add catalog:** page and pattern editor Add panels now expose every component from the a1-web Components registry. Added missing editor `ComponentType`s, renderer registrations, and add templates for Search Field, Action Tiles, Page Layout, Canvas, Side Nav, Autocomplete, Inline Editable, Notification, Snackbar, Dialog, Menu, Context Menu, Bottom Sheet, Stat, Node, and Inline. Search Field is now in the Common set. |
| 2026-07-02 | **Icon loading layout:** restored the shared React `.a1-icon` layout reservation with a `1em` inline/block box, nowrap ligature text, and clipping so Material Symbols names do not widen controls while the icon font is loading. |
| 2026-07-02 | **Action Tiles component** (A1-389): added a React `ActionTiles` component for grouped icon-led action surfaces with rounded corners, `title` + `subtitle`, responsive icon placement (`iconLayout="auto"` shifts from top to side with container width), optional `accessory` and `footer` slots, plus optional whole-tile interactive semantics via item-level `as` / `href`. Follow-up polish on the same date: removed the body slot, made scale container-driven instead of prop-driven, tightened padding relative to Card, removed shadows, matched the interactive border treatment to navigation Card, converted the public component into an always-grouped `items` API (single-item groups allowed) with grid/stack + gap on/off, reduced the title/subtitle typography scale, and made interactive tile items automatically drop nested accessory/footer controls. Tokenized via `system/tokens/component/action-tile.json`, governed by `system/rules/action-tile.yaml`, exported from the package index (`ActionTile` kept as an alias), documented in Storybook, and registered in a1-web under **Actions & Controls** with a live configurator. |
| 2026-07-02 | **a1-web Help shortcut:** the global `?` keyboard shortcut now opens the anchored Help assistant menu instead of navigating directly to the full Help page. Updated the shortcuts Help article to match. No design-system component API changed. |
| 2026-07-02 | **a1-web Help chat bot** (A1-157): the top-header Help icon now opens an anchored local help assistant instead of navigating directly to the Help page. The assistant accepts plain-language questions, ranks answers against the shared Help content model, shows matched Help topics with summaries, and can open the full Help page with the selected query prefilled. Implemented with `apps/a1-web/src/help/helpAssistant.js` + `HelpAssistantMenu.jsx`; intentionally non-AI in this slice, but the ranking service is isolated so a future AI-backed answerer can replace or augment it. No design-system component API changed. |
| 2026-07-02 | **a1-web Help data model** (A1-338): extracted the monolithic Help page content into `apps/a1-web/src/help/helpContent.jsx`, backed by `helpModel.jsx` utilities that normalize keywords, derive search text, and export an AI-friendly plain-text catalog (`HELP_AI_CONTENT`). `pages/Help.jsx` is now a thin renderer over the shared model, so future Help updates should be added to the help-content module rather than embedding articles directly in the page component. No design-system component API changed. |
| 2026-07-01 | **Color ramps 25 step** (A1-342): added `25` to every base color ramp. `neutral.25` already existed; added `accent.25`, `highlighted.25`, `info.25`, `error.25`, `warn.25`, and `success.25` as OKLCH midpoints between each ramp's existing `0` and `50` colors. Rebuilt token CSS/JSON and Pure/React token outputs. |
| 2026-07-01 | **Section background image** (A1-345): added `backgroundImage` (decorative image URL, applied via the `--a1-section-bg-image` custom property), `backgroundFit` (`cover` default / `contain` / `tile`), `backgroundPosition` (9 focal points, Figure `crop` vocabulary), `backgroundOverlay` (`darken` / `lighten` contrast scrim) and `backgroundOverlayStrength` (`sm`/`md`/`lg`). One background stack — overlay scrim → image → surface (no extra DOM); new `component.section.backgroundOverlay.*` tokens (darken/lighten colours + strength numbers) mixed via `color-mix`. `backgroundImage` suppresses `gradient`. Pure `a1-base.css` got matching `.a1-section-bg-*` classes — added to the `componentCss()` template in `scripts/build-html-css.mjs`, since `a1-base.css` turned out to be **generated** by that script (the ai docs wrongly called it hand-authored; `project-workflows.md`, `quick-orientation.md`, and `packages/pure/ai/project-context.md` corrected in this change). React Native not covered. New a11y rules `section-background-image-contrast` / `section-background-image-decorative` in `system/rules/section.yaml`. Updated `.d.ts`, registry note, figma-workflow gap table, a1-agent-brief, three new Storybook stories, and the a1-web Section configurator (library/URL image picker, fit, 3×3 focal grid, overlay + strength slider). |
| 2026-07-01 | **a1-web Projects definition dialog — editable JSON**: the project "definition" dialog now renders the bundle with `Code variant="block" copyCode editable wrapping rows={18}` (scrollable, vertically resizable textarea; copy reflects live edits) instead of a read-only `collapsible` block. No design-system component API changed. |
| 2026-07-01 | **TreeMenu rename via double-click / F2 + input contrast** (A1-22): added `onRenameStart(id)` — fired when the user double-clicks an item's label or presses F2 on the focused item (disabled items never fire it), so consumers can enter edit mode without a context menu. The a1-web editor Layers tree wires it to `editingId`. Fixed the rename input's text colour: it now uses `--semantic-color-text-default` (the field family's input colour) instead of `color: inherit`, which picked up the selected row's inverse text and was unreadable on the field surface. Updated `.d.ts`, registry note, the `Inline rename` story, and the editor Help page. |
| 2026-06-30 | **TreeMenu inline rename** (A1-22): added `editingId` / `onRenameCommit(id,label)` / `onRenameCancel()` props so tree items can be renamed in place. When `editingId` matches an item, its label `<span>` is replaced by an auto-focused/selected `<input aria-label="Rename item">` (rendered outside the label `<button>` to avoid nested interactive HTML), styled as an A1 field (`--component-field-*` border + focus-ring tokens, `.a1-tree-menu__rename-input`); Enter/blur commits, Escape cancels, keydown is `stopPropagation`'d so tree navigation doesn't fire, and drag is suspended while editing. Consumer-driven (consumer enters edit mode and owns the `items` update). Updated `.d.ts`, registry note, and added an `Inline rename` story. Used by the a1-web editor Layers tree (A1-22). |
| 2026-06-30 | **SplitButton secondary styling** (A1-380): removed the adjoining internal borders between the main action and caret toggle so secondary split buttons render as one shared pill outline with the existing hairline divider, rather than showing a doubled seam. CSS-only; no API change. |
| 2026-06-30 | **a1-web Backlog consistent ticket card variants** (A1-353): board and queue backlog views now share one ticket card pattern with view variants. Queue cards use the same badge metadata structure and a default-size `IconButton` for opening a ticket. No design-system component API changed. |
| 2026-06-30 | **Form fields read-only autofill guard** (A1-51): `TextField` and `TextareaField` now force autocomplete off in read-only mode, add common password-manager ignore hints, and restore the existing value if an autofill/input event attempts to mutate the control. `SearchField` also hides its clear button while read-only. Storybook read-only/autofill examples updated. |
| 2026-06-30 | **a1-web Editor pattern creation flow** (A1-231): selected page elements can now be turned into patterns from the right-side Configure panel. The source element is replaced with a linked pattern instance, then the app opens the new pattern editor with a back link to the source page. No design-system component API changed. |
| 2026-06-30 | **a1-web Backlog remembered swimlanes:** visible swimlane selections now persist in the existing local backlog filter storage with validation against known statuses. No design-system component API changed. |
| 2026-06-30 | **a1-web Backlog next/previous review** (A1-243): the ticket detail dialog footer now includes left-aligned previous/next `IconButton`s that navigate through the active review sequence (board, all tickets, or queue). No design-system component API changed. |
| 2026-06-30 | **a1-web Editor Figure radius fix** (A1-346): the Figure editor bridge now writes `radius="none"` explicitly so changing a rounded Figure back to None clears prior rounded props during node updates. No design-system component API changed. |
| 2026-06-30 | **DataTable icon-only row actions:** row action objects can now set `iconOnly: true` to render as an accessible `IconButton` using the action `label`. The a1-web Rules table uses this for its Edit action. No breaking API change. |
| 2026-06-30 | **DataTable search uses SearchField:** the shared DataTable filter search control now composes the official `SearchField` component instead of hand-rolled input/icon markup, while using a generated neutral `name`, `autoComplete="off"`, password-manager ignore hints, `autoCorrect="off"`, `autoCapitalize="none"`, and `spellCheck={false}` autofill guards. No API change. |
| 2026-06-30 | **a1-web Rules editor examples/editing** (A1-57): rules can now be edited from the table, including built-in rules by saving a local user override with the same rule id. The rule dialog supports code examples, uploaded image examples, and pasted image examples stored through the existing image library; the table exposes an Examples preview column. No design-system component API changed. |
| 2026-06-30 | **a1-web Backlog paste images** (A1-160): ticket creation and editing dialogs now accept pasted images anywhere inside the dialog, attach them through the existing local image library, announce success with `aria-live`, and keep text paste untouched unless the clipboard contains an image. The ticket edit dialog also exposes removable screenshot thumbnails. Help updated; no design-system component API changed. |
| 2026-06-30 | **Stat component** (A1-375): added a React `Stat` component (single highlighted metric — muted `title` + optional `icon`, large tokenized `value` with numeric formatting / `prefix` / `suffix`, optional `description` and status `badge`; `size` `xs`–`xl`, `align` start/center/end). Tokenized via `system/tokens/component/stat.json` (`--component-stat-*`), exported from the package index, with Storybook stories. Registered under the Data category. **Shared with A1-375** (the component originates on that ticket's branch and was brought onto this branch to use on the a1-web Components overview page); when A1-375 lands, reconcile the duplicate component files. |
| 2026-06-30 | **Switch — fix page scroll-jump on toggle**: made `.a1-switch__row` `position: relative` so the visually-hidden (`position: absolute`) `role="switch"` input is contained within the switch instead of resolving against a distant positioned ancestor. Previously, clicking the switch moved focus to that far-off input and the browser scrolled it into view, jumping the page (a sticky header could scroll out of view); reported on the Kitchen sink page. Focus still lands on the input for keyboard/SR users — it just no longer scrolls. CSS-only; no API change. |
| 2026-06-30 | **Toolbar label — tied to form-label tokens** (A1-165): the Toolbar's visible `label` caption (`.a1-toolbar-field__label`) now uses the same tokens as a compact form label — `--semantic-font-size-form-label-compact` (size) + `--component-field-compact-label-font-weight` (weight); colour was already `--semantic-color-text-default`. It previously used `--semantic-font-size-body-xs` (same value) with the *default* field-label weight (600), an inconsistent compact-size/default-weight mix. Now renders identically to a compact Field/Fieldset legend label beside it (12px / weight 500 / text-default). CSS-only; no API change. Updated the JSX docblock, `.d.ts`, and the registry note. |
| 2026-06-30 | **Toolbar — subtle border by default** (A1-220): `Toolbar` now carries a hairline subtle border by default (`--a1-toolbar-border-width` = `--component-divider-size-xs`, `--a1-toolbar-border-color` = `--semantic-color-border-subtle`) for definition against the page surface, replacing the previous borderless default. The accessible theme and OS high-contrast (`prefers-contrast: more` / `forced-colors`) step it up to the thicker `--component-divider-size-sm` width and the stronger `--semantic-color-border-default` colour. Also fixed the accessible-theme escalation selector — it targeted `[data-theme='accessible']`, which never matched the runtime theme class `.a1-theme-accessible`, so the high-contrast step was previously dead. CSS-variable only; no API change. The `overlay` variant keeps its own card-on-page border. |
| 2026-06-30 | **Toolbar overflow — no menu-from-a-menu** (A1-354): when a `ToolbarMenu` is moved into the toolbar's More (overflow) menu, its items now render **inline** in that menu as a labelled section instead of a button that opens a nested submenu. The overflow panel keeps original order — consecutive non-menu tools render as a compact horizontal strip and each `ToolbarMenu` becomes an inline labelled run in place — preserves the active selection, and closes the More menu when an inline item is chosen. The selected inline item uses the Toolbar's subtler selected style (default text on a darker neutral fill, `.a1-toolbar__overflow-item`) instead of the bold action-background `MenuItem` highlight. Driven by internal-only `inline` / `onInlineSelect` props on `ToolbarMenu`; no public API change. |
| 2026-06-30 | **a1-web Toolbar configurator Edit / Preview mode:** added a Toolbar-only preview mode switch. Edit mode keeps click-to-select behavior for configuring individual tools; Preview mode lets toggles, menus, and groups behave interactively without selecting tools. The Toolbar Overflow example starts in Preview mode. No design-system component API changed. |
| 2026-06-30 | **a1-web component examples / sticker sheets** (A1-102): added JSON-backed focused example child pages to component detail pages. Examples apply preconfigured component configs into the live configurator state, render the same component preview/snippet path as Configure, can define preview constraints such as a narrow width, and have direct routes such as `/components/toolbar/overview`. Added Overflow under Toolbar as the first example with a denser WYSIWYG-style editor toolbar. No design-system component API changed. |
| 2026-06-30 | **Toolbar overflow pattern** (A1-354): added Toolbar-level `overflow` and `overflowLabel` props. `overflow` keeps direct toolbar children on one row, measures the available inline space, and moves trailing tools into a tokenized More menu when the toolbar is narrower than its contents. The More panel wraps overflowed tools into horizontal grouped rows, with dividers as full-width group breaks, instead of forcing every direct child into a vertical stack. Added the `Overflow toolbar` story, documented the distinction from `ToolbarGroup overflow`, and exposed the setting in the a1-web Toolbar configurator/snippet. |
| 2026-06-30 | **a1-web Motion foundation examples:** added an Examples tab with replayable duration and easing demos that animate with semantic motion tokens and respect reduced-motion settings. No design-system component API changed. |
| 2026-06-30 | **a1-web Get started package setup:** added Web components to the Package setup tabs with `@gtivr4/a1-design-system-web`, Lit peer dependency guidance, Button/Snackbar custom-element imports, token/theme CSS requirements, AI context files, and a framework-agnostic example. No design-system component API changed. |
| 2026-06-30 | **a1-web Image library standalone feature:** moved Image Library from `projects/ImageLibraryView.jsx` to `image-library/ImageLibraryView.jsx`, removed Projects from its breadcrumb, and switched its header to the shared `PageTitleArea` pattern wrapper. No design-system component API changed. |
| 2026-06-30 | **a1-web Patterns page title area** (A1-355): added the built-in `page-title-area` governed pattern for breadcrumb/title/description/action page tops, added a shared app `PageTitleArea` wrapper, migrated Patterns/Blog/Projects page headers to it, and updated `page-header` to compose the pattern via `PatternRef`. No design-system component API changed. |
| 2026-06-30 | **Color ramps typo** (A1-371): renamed the highlighted base color ramp to `base.color.highlighted` / `--base-color-highlighted-*`, updated heading and section component-token aliases, and updated the a1-web Section gradient swatch. |
| 2026-06-30 | **a1-web page search shortcut:** `'` now focuses the first visible on-page search target, while `/` remains global search. a1-web marks Backlog, Help, Blog, Components, and Label editor searches; `DataTableFilters` marks its search input so every DataTable search participates. No design-system component API changed. |
| 2026-06-30 | **a1-web Backlog Paused swimlane:** added `paused` as a first-class workflow status between In progress and Done. The board shows Paused by default, ticket status controls can set it, local backlog sync groups it, Supabase validation accepts it, and the backlog labels include translations. No design-system component API changed. |
| 2026-06-29 | **a1-web Settings menu cleanup:** hid Crochet and Marshmallow from the Settings theme picker, moved Account controls to the end of the Settings menu, and removed Editor export/import actions from Settings. No design-system component API changed. |
| 2026-06-29 | **a1-web account settings consolidation:** removed the separate user/account top-header menu and moved Account, sign-in, and sign-out actions into the existing Settings menu when Supabase auth is configured. No design-system component API changed. |
| 2026-06-29 | **a1-web Virtual Team navigation:** moved the dev-only Virtual Team entry from the icon-only top-header action group into the standard Explore menu list in dev builds. No design-system component API changed. |
| 2026-06-29 | **a1-web Backlog navigation:** moved Backlog from the icon-only top-header action group into the standard Explore menu list. Backlog remains addressable at `/backlog` and through keyboard shortcuts; no design-system component API changed. |
| 2026-06-29 | **a1-web route scroll reset:** central navigation now resets both `window` scroll and the A1 `PageLayout` main scroll container after route changes. Opening a Blog article from the Blog index starts at the top of the article instead of preserving the archive scroll position. No design-system component API changed. |
| 2026-06-29 | **a1-web Blog article depth:** release article pages now render multi-paragraph sections, add user-facing release context for why each update matters in daily A1 use, and append a “What to try next” section to every article. The Blog data source and page-definition renderer path remain shared; no design-system component API changed. |
| 2026-06-29 | **a1-web local auth gate bypass:** split Supabase configuration from full-page auth gating. The app still creates the Supabase client locally when env vars are present, but `AuthGate` only requires sign-in on live/non-local hosts; localhost, `127.0.0.1`, `::1`, and `file:` runs pass through for local development, Help screenshot capture, and walkthrough generation. No design-system component API changed. |
| 2026-06-29 | **a1-web video walkthrough pipeline** (A1-107): added a first reproducible prompt-to-MP4 slice. `walkthroughs/*.walkthrough.yaml` specs can now drive Playwright capture via `npm run walkthrough:capture`, producing browser video, per-step screenshots, event timings, and `remotion-input.json`. Added `npm run walkthrough:prompt` for deterministic starter specs from a prompt, `npm run walkthrough:narration` for narration scripts / estimated word timings / SRT captions / optional macOS local voiceover, `npm run walkthrough:render` for narration-paced Remotion MP4 output, and `npm run walkthrough:demo` for the sample end-to-end flow. Generated walkthrough artifacts are gitignored; `packages/react/ai/walkthrough-pipeline.md` documents the contract and follow-up TTS adapter. No design-system component API changed. |
| 2026-06-29 | **a1-web global keyboard shortcuts:** added an Amazon-style first-tab keyboard navigation trigger backed by the A1 `Menu` component, with grouped actions for main content, global search, primary pages, tools, and Help. Also added app-level shortcuts for high-use navigation and main-content focus: `/` opens global search, `Cmd/Ctrl+K` opens global search unless a page-specific handler already consumed it, `Cmd/Ctrl+/` shows all shortcuts, `?` opens Help, `Alt+M` or `g m` focuses the PageLayout main scroll container, and `g` chords navigate to Home, Components, Foundations, Editor, Patterns, Backlog, Rules, Labels, Data, Theme editor, and Accessibility. Shortcuts ignore input/select/textarea/contenteditable targets. `Menu` gained `trapFocus` and `modalOnMobile` opt-out props so non-modal discovery menus can use the shared component without trapping tab focus. |
| 2026-06-29 | **a1-web Global search** (A1-265): added a dedicated `GlobalSearchDialog` app component launched from a new top-header search action or `/` / `Cmd/Ctrl+K` keyboard shortcuts. It uses a native top-layer scrim without the standard Dialog surface, opens with a focused floating comfortable SearchField, and reveals a separate grouped results panel only after input. The index covers pages, foundations, component categories/components, projects/project pages, patterns, rules, workspace labels, and backlog tickets, with related-term and typo-tolerant scoring. No design-system component API changed. |
| 2026-06-29 | **A1-349 — a1-web Project deletion persistence:** fixed deleted Projects reappearing after shared cloud sync by letting cloud project hydration replace the local project set instead of merge-only upserting. Project deletion now removes the project page-list and shared-layout storage through one purge helper and filters the raw project list so sample seeding cannot run during the delete path. |
| 2026-06-29 | **a1-web Backlog CSV export:** added an Export CSV action to the Backlog panel that downloads the complete ticket list from the source backlog state, independent of the active view, filters, sort, or DataTable pagination. |
| 2026-06-29 | **Icon font loading stability:** constrained `.a1-icon` ligatures in React and Pure CSS to a fixed 1em square with clipped overflow so Material Symbols fallback text cannot expand layout while the icon font loads. Also applied the same treatment to a1-web editor pseudo-element lock/pattern badges that use Material Symbol ligatures directly. |
| 2026-06-29 | **a1-web Components sidebar A-Z view:** added an icon-only toolbar toggle beside the Components sidebar search so users can turn the flat A-Z component list on and off. The A-Z list keeps the same smart search matching while sorting visible component results alphabetically, and the selected view persists per browser in localStorage. |
| 2026-06-29 | **DataTable search matcher + a1-web component smart search:** DataTable searchable columns now accept an optional `searchMatcher(row, query)` hook for alias/fuzzy/domain-specific matching while preserving the default substring search. The a1-web Components sidebar and all-components table use curated component keywords plus typo-tolerant scoring, so searches like `CTA` find Button and `grid` ranks Grid ahead of related Data Table results. |
| 2026-06-28 | **a1-web automated accessibility report data:** added `scripts/a11y/generate-component-reports.mjs` plus `npm run a11y:reports` / `npm run a11y:reports:check` to derive component report rows from the built React token CSS. Button's Accessibility tab imports generated contrast and target-size rows as build-time fallbacks, then resolves active CSS variables in the browser so the report recalculates when theme or color mode changes. `.github/workflows/accessibility-reports.yml` regenerates the fallback report data weekly or on demand and opens a PR when values change. |
| 2026-06-28 | **a1-web component accessibility report scaffolds:** extended the accessibility report structure beyond Button to every component detail page. Non-Button reports now classify components as content, input, navigation, overlay, or interactive; resolve active-theme contrast rows live; show known target-size token rows where available; and include keyboard, screen reader, WCAG, test matrix, and manual checklist sections without placing content blocks beside data tables. |
| 2026-06-28 | **a1-web Accessibility page component scan:** `/accessibility` now imports a generated component scan summary and shows the latest grouped Storybook axe results in a searchable/filterable DataTable, with stats for component groups, stories scanned, affected components, and total violations. |
| 2026-06-28 | **Shared focus ring color:** standardized visible focus indicators on `--component-button-focus-ring` as the shared focus-ring color token. The source token now resolves to `#5916b5` in the default bundle, and field focus continues to alias it. Accordion, BottomDrawer, BottomSheet, Breadcrumb, Card, Code, ContextMenu, DataTable filter chips, Link, Menu, PageNav, Pagination, SegmentedControl, SideNav, Tabs, TokenSelect, TopHeader, TreeMenu, and Canvas focus states now use the shared focus color while preserving component-specific width and offset. |
| 2026-06-28 | **a1-web Button accessibility report showcase:** Button's component detail Accessibility tab now demonstrates the intended report format with summary cards, contrast evidence, generated target-size evidence for WCAG 2.5.5 / 2.5.8, keyboard controls, screen reader expectations, WCAG mapping, test matrix, manual checklist, implementation notes, and release-gate guidance. Other component accessibility tabs continue to show the generic placeholder until filled in. |
| 2026-06-28 | **a1-web component details — focused tabs and current properties:** component detail pages now hide the Overview and Anatomy tabs, normalizing old tab state to Configure. The Properties tab prefers generated rows from React package `.d.ts` declarations, including child prop interfaces, with local fallback rows for surfaces without declarations. Date, time, phone, ZIP, and credit-card field pages alias the Text Field prop table so specialized field docs stay aligned with the package surface. Bleed's preview now applies the inverse text token to the paragraph inside its action-colour bleed band. |
| 2026-06-28 | **Menu — Safari fallback anchor fix:** `Menu` now ignores `body`, `html`, and `dialog` as fallback anchors when no explicit `anchorRef` is provided. Safari can leave focus on `body` when an icon button opens a non-modal `<dialog>`; using that as the anchor placed the menu near the bottom-left of the page and made outside-click dismissal persistent because `body.contains(target)` is always true. Unanchored menus now fall back to viewport placement and outside-click close works normally. a1-web also passes an explicit `anchorRef` for the Settings menu so it opens below the settings icon. |
| 2026-06-28 | **a1-web — Component proposals workspace** (A1-326): added a dormant component-proposals workspace for the first in-app component creation slice; it is intentionally hidden from navigation and routing for now. The workspace generates local draft proposals from a component/property brief, using a local Ollama model when available and falling back to the built-in deterministic generator when no model is reachable or valid JSON is not returned. It captures approval metadata (draft/proposal/review/accepted/released, creator, visibility, description, problem, dates, updates), renders a live preview through the existing A1 page-definition renderer, generates buildable React component source from the validated definition, and includes developer-mode JSON editing that rebuilds the component source. Proposals persist in localStorage for this slice; publishing to packages, direct project insertion, and re-exposing the page are follow-ups. No design-system component API changed. |
| 2026-06-27 | **TopHeader — action divider** (A1-366) — `TopHeader` actions array now accepts `{ divider: true, id }` items that render a 1px vertical separator (`<div class="a1-top-header__action-divider" aria-hidden="true" />`). CSS: `align-self: stretch; width: 1px; margin-block: var(--base-spacing-8); background: var(--semantic-color-border-subtle)`. No breaking change — all existing action items are unchanged. |
| 2026-06-27 | **DataGrid — A1 checkbox styling** (A1-366) — replaced native browser checkbox in `.a1-data-grid .rdg-checkbox-input` with A1 token-styled appearance: `appearance: none`, 16×16px, `var(--semantic-color-border-default)` border, `var(--base-radius-xs)` corners, checked/indeterminate states fill `var(--semantic-color-action-background)` with `var(--semantic-color-action-foreground)` icon via mask, focus ring via `--component-field-focus-ring`, disabled softens surface and border. CSS only — no React API change. |
| 2026-06-27 | **system/labels/app.json — app.labelEditor namespace** (A1-366) — added 44 DTCG label keys under `app.labelEditor.*` with English `$value`, `$description`, and translations for `es`, `fr`, `de`, `pt`, `ja`, `zh`, `ar`. Covers all user-visible strings in the Label editor page and AddLabelDialog. Added `apps/a1-web/src/labels/useT.js` exporting `TContext / TProvider / useT()` for in-app string resolution through the label stack. |
| 2026-06-27 | **A1-web app translation slice (A1-366)** — added `system/labels/app.json` for app-shell navigation, page titles, header actions, settings, accessibility toggles, locale controls, and editor import/export actions. A1-web now resolves these chrome strings through the existing system/workspace/project label stack, and the Label editor/label picker include the new app namespace. Added `apps/a1-web/TRANSLATION_REVIEW.md` for same-vs-different terminology decisions; projects, patterns, and page-definition content stay untranslated. |
| 2026-06-27 | **a1-web Editor labels — context-menu picker** — right-clicking Heading, Paragraph, and Button nodes in the page/pattern editor now offers “Choose text label”, opening the shared searchable single-select label DataTable dialog and writing the selected key to `content.textKey` with the selected English value as fallback. The side-panel text label control is now a `more_vert` menu with Choose label and Add label; Add label suggests a key from the selected text, saves to the shared workspace label dataset, and can auto-translate enabled locales. Labeled text displays as `<<label.key>>` in text/label fields while preserving the stored fallback. |
| 2026-06-25 | **Icon + a1-web custom icon pipeline** — React `Icon` now reserves `custom:<name>` for glyphs registered through `registerCustomIconFont`, while all unprefixed names continue to render through Material Symbols. a1-web adds a project-scoped IndexedDB icon library, strict 24×24 filled-path SVG validation, in-browser TrueType compilation, active-project font registration, a Custom icons editor with project filtering/scope editing, and scoped custom options in component icon pickers. The supplied engine SVG seeds as `custom:engine` for Northstar Motors. |
| 2026-06-25 | **a1-web — Northstar Motors sample project:** added a seeded Inventory + Vehicle details project for the Vehicles dataset. Inventory repeats a bound Card template and links each row by `__id`; Vehicle details uses `detailDataset: "vehicles"` and exercises image, text, numeric, boolean, and item-specific bindings across comprehensive specification groups. Fixed shared-layout composition to preserve the content page's identity, `detailDataset`, and `detailPreviewId`, allowing standalone preview links to resolve the selected row. No design-system API changed. |
| 2026-06-25 | **a1-web — Vehicles sample dataset:** added a synthetic 120-row, wide-schema vehicle catalog to Data sources with detailed identity, price, ownership, ICE/electric powertrain, engine bore/stroke, charging, performance, efficiency, dimension/wheelbase, chassis, wheel/tire/brake, comfort, infotainment/speaker, airbag/driver-assistance, warranty, and eight-image fields. Built-in samples now carry their own Material Symbol icons. No design-system API changed. |
| 2026-06-25 | **a1-web — Virtual data teammate** (A1-344): added a dev-only Virtual team member that previews and applies project data connections. An optional local Ollama model selects relevant page-to-dataset matches; a deterministic fallback and validator use the existing `repeat`, `collections`, and `{{ dataset.column }}` contracts to connect single Card/ListItem templates, matching text/props, and DefinitionList fields. Apply writes named editor-history snapshots through `projectStore.commitPageJson`; no design-system component or token API changed. |
| 2026-06-25 | **Page-definition registry documentation reconciled:** updated `page-definition-standard.md` from the obsolete seven-component proof-of-concept list to the actual 60-type contract in `pageTypes.ts` / `componentRegistry.ts`, including `Grid`, media, data, navigation, feedback, and the full field family. Documented direct component mappings versus editor adapters, replaced the false “no Grid” guidance and row-Stack workaround with the correct Grid pattern, updated the canonical example, and synchronized `project-context.md` / `a1-agent-brief.md` (including the previously omitted `Outlet`). Confirmed and documented the serializable `DefinitionList` item contract as `{ label, value }`: `label` is required, `term` is not supported, and page definitions should prefer `value` over the React-only alternate `children` source. Added the primary-section `contentWidth` rule and its intentional edge-to-edge exception so the standard matches `system/rules/section.yaml`. |
| 2026-06-24 | **Color token Phases 4–5 — inverse and brand contracts:** inverse is now explicitly defined as opposite the document mode, not a recursive toggle. `Inverse` and `Section inverse` expose `data-a1-color-scope="inverse"`; the computed contract covers nested scopes and a native top-layer dialog inside the scope across all 16 theme/mode fixtures. Removed the unused `brand.a1.*` color and typography alias tier after confirming no runtime/package consumers, removed its Color foundation tab and System map node, and added a blocking audit check preventing brand token definitions from returning. Fresh gradient/dark-surface colors moved from raw authored CSS into structured theme custom properties. |
| 2026-06-24 | **Color token Phase 3 — component contracts:** replaced all 10 direct `--base-color-*` uses in component CSS with component tokens. New tokens: `component.link.inverse.*`, `component.notification.neutral.*`, `component.section.gradient.highlight`, `component.segmented.inactive.*`, `component.snackbar.default.*` (new file), `component.switch.thumb.on.background`. Removed raw rgba() fallback from snackbar shadow. `componentBaseColorReferences` is now a blocking audit check — enforces 0 base-color refs in component CSS. `tokens:audit:check` and `tokens:contract:check` both pass clean. |
| 2026-06-24 | **Color-mode CSS generation (Phase 2 complete):** all 7 runtime light/dark/inverse selector blocks are generated from `system/color-modes.mjs` via `system/build-themes.mjs`. `LIGHT_MODE_DECLARATIONS` added to `color-modes.mjs` (68 CSS custom properties, all aliasing tokens). `color-scheme-modes.css` contains generated selectors and `color-scheme-static.css` contains hand-authored reset/typeface/contrast/Fresh content; the published `color-scheme.css` is generated as a self-contained bundle of both to avoid consumer CSS `@import` resolution failures. `component.scrim.colorLight` token added to eliminate the only raw `rgba()` in generated output. Audit extended with `reactLightContractDrift` check. Fixed two pre-existing CSS bugs: `--semantic-color-surface-inverse` not set in `html.a1-theme-dark .a1-inverse`, `--semantic-color-surface-field` not set in `@media dark .a1-inverse`. Contract baseline updated. Both `tokens:audit:check` and `tokens:contract:check` pass with 0 findings. |
| 2026-06-24 | **Color-token remediation, structured themes:** completed Phase 1 of the remediation plan. Moved 926 raw custom-property declarations out of `system/themes/*/theme.json` into typed DTCG override files, added the shared `system/theme-config.mjs` loader/validator and theme JSON schema, separated non-token selector styles, and removed all namespaced `system/themes/*/tokens` sources. Style Dictionary now builds only canonical `system/tokens`; default font families live at `base.font.family.*`; React, Pure CSS, and React Native generation consume the same validated theme configuration. Native color/typography output remains byte-identical, and the a1-web System map now shows the structured theme flow. |
| 2026-06-24 | **Color-token remediation, slice 1:** introduced `system/color-modes.mjs` as the shared light/dark role contract for Pure CSS and React Native generation; removed their duplicate dark maps; added a blocking color-token inventory (`tokens:audit:check`, generated `color-token-audit.md`) and a computed-style theme/mode/inverse contract (`tokens:contract:update/check`, 16 Storybook fixtures). The audit verifies aliases, duplicate paths, CSS references, theme structure, and React explicit-dark parity. Fixed InlineEditable's nonexistent radius/focus variables by using `--base-radius-sm` and component field focus-ring tokens. Repaired `a11y:component`/`a11y:changed` for Storybook 10 by replacing the removed `--testNamePattern` option with direct index-based Playwright + axe filtering. Generated outputs stayed byte-stable; 573 visual stories passed with no regressions; all six InlineEditable stories pass focused axe checks. |
| 2026-06-24 | **Color-token remediation baseline:** added focused Storybook visual-regression matrices for all current themes in explicit light, explicit dark, inverse, and nested-inverse contexts. The fixtures exercise semantic surfaces, default/muted text, primary and secondary actions, status badges, and compact fields so the token-architecture migration can be compared against a committed pre-migration reference. Figure stories now use repository images instead of remote Unsplash responses so consecutive visual runs are deterministic. No component or token behavior changed. |
| 2026-06-24 | **a1-web Foundations — System map color architecture:** expanded the System map to distinguish authored token tiers and Brand A1 aliases from namespaced theme tokens, raw selector overrides, Style Dictionary, the custom theme/HTML builders, generated CSS/JSON artifacts, hand-authored color-scheme modes, runtime document classes, and inherited inverse islands. This documents the actual parallel build/cascade paths before packages and components consume color. No design-system component or token API changed. |
| 2026-06-24 | **a1-web Foundations — Color visualization:** added a lazy-loaded `/foundations/color-visualization` quality tool built with React Three Fiber, Three.js, and Culori. Live `--base-color-*` values from the selected site theme are converted to OKLCH and reused across a 3D token-space viewer, selected-ramp diagnostics, and cross-ramp step alignment; all recompute when theme or color mode changes. Controls live in the shared component-style right rail as A1 Toolbars: ramp selection is a four-column swatch-only Toolbar Group, token steps use a six-column swatch-only Toolbar Group, and a Tree Menu selects the Step alignment ramp step. The scene supports ramp isolation, connected paths, token labels, lightness guides, point selection, a sampled sRGB gamut boundary, and explained/toggleable wireframe out-of-gamut warnings; a numeric conversion tolerance prevents false warnings on valid hex tokens. No design-system component or token API changed. |
| 2026-06-23 | **Canvas — elbow edge style, trackpad zoom fix; Node — own a1-web page** — (1) **Elbow edge style**: `edgeStyle="elbow"` routes edges orthogonally (exits from cardinal face center, horizontal → vertical → horizontal with 16px rounded corners). New `getElbowBoundaryPoint()` computes cardinal exits; `edgePath()` now takes a style string (`"straight"` | `"curved"` | `"elbow"`) instead of a boolean. `CanvasEdgeStyle` type updated. Storybook `ElbowEdges` story added. Configurator "Edge shape" gains an Elbow option (`turn_right` icon). (2) **Trackpad zoom speed**: `onWheel` now uses `e.deltaMode` to distinguish trackpad (pixel mode, `deltaMode === 0`) from mouse wheel (line mode, `deltaMode === 1`). Trackpad applies `ZOOM_TRACKPAD_SENSITIVITY = 0.003` (capped ±15%) so a fast two-finger scroll doesn't rocket past the target zoom; mouse wheel keeps the existing ±15% per click step. (3) **Canvas → Layout & Display** category in a1-web (was Data). (4) **Node a1-web page** (`/components/node`) — standalone configurator with label + sublabel text fields, shape, color, subtle toggle; no Canvas required; registered in the Data category in `data.js` / `detail/index.js`. Status: experimental. |
| 2026-06-23 | **Canvas — `CanvasNode` renamed to `Node` (standalone component)** — `CanvasNode` is removed; replaced by `Node`, a standalone visual component that works anywhere without a Canvas parent. When used outside Canvas it renders inline at natural dimensions (no absolute positioning, no context dependency). When used inside Canvas it reads `CanvasCtx` for positioning (`x`/`y`/`id` required for edge routing), drag handling, selection, and context menu. `id`/`x`/`y` props are now optional (required only inside Canvas). Package export changed: `CanvasNode` → `Node` in `src/index.js`. TypeScript: `CanvasNodeProps` → `NodeProps` with `id`/`x`/`y` marked optional. Storybook stories, configurator, and components.md updated. |
| 2026-06-23 | **Canvas — surfaces + inverse; remove data-driven API** — (1) **`background` trimmed to `panel | page | raised`** — the `card` value and `.a1-canvas--card` CSS class are removed. (2) **`inverse` boolean prop** added (default `false`) — applies `--semantic-color-surface-inverse` background and `--semantic-color-text-inverse` text, mirroring Section's inverse treatment. CSS: `.a1-canvas--inverse`. TypeScript: `CanvasBackground` updated; `inverse?: boolean` added to `CanvasProps`. (3) **Data-driven API removed** — `nodes`, `edges`, and `onNodesChange` props are gone. Canvas is a pure compositional surface; use `<CanvasNode>/<CanvasEdge>` children. Removed `CanvasNodeData` and `CanvasEdgeData` interfaces from types. Updated Storybook (removed `card` from background argType, added `Inverse` story), configurator (removed `card` BG option, added `inverse` toggle in Options FieldState, wired to Canvas + snippet), and components.md. |
| 2026-06-23 | **Canvas — context menu, snap-to-grid, curved edges** (A1-312 follow-up 2) — (1) **Context menu**: right-clicking a node opens a `ContextMenu` with consumer-provided `nodeMenuItems(id)` items + a "Delete" item in `mode="edit"` (requires `onDeleteNode` prop). Right-clicking the canvas background opens a canvas context menu with Zoom in / Zoom out / Fit to screen / Reset zoom; consumer-provided `canvasMenuItems` are appended. CanvasNode calls `e.preventDefault()` + `e.stopPropagation()` on `onContextMenu` and calls `ctx.onNodeContextMenu`. (2) **Snap to grid**: `snapToGrid` boolean prop (default `false`) on Canvas; when enabled, dragged node positions are rounded to the nearest 24px grid increment. (3) **Curved edges**: `edgeStyle="curved"` on Canvas routes all edges as quadratic Bézier curves (control point offset perpendicular to the midpoint, capped at 80px); per-edge `curved` boolean on `CanvasEdge` overrides the canvas-level default. All edge rendering uses `<path>` instead of `<line>`. New Canvas props: `onDeleteNode`, `nodeMenuItems`, `canvasMenuItems`, `snapToGrid`, `edgeStyle`. Updated TypeScript types; added `ContextMenuAndSnap` Storybook story; configurator gained "Edge shape" + "Snap to grid" controls. |
| 2026-06-23 | **Canvas — UX + compositional API** (A1-312 follow-up) — (1) **Left-click drag** on the canvas background now pans (previously required spacebar or middle-click). (2) **Touch support**: single-finger drag pans, two-finger pinch zooms toward the pinch center; native non-passive touch listeners. `CanvasNode` calls `e.stopPropagation()` on `mousedown`/`touchstart` to prevent the canvas from starting a pan when clicking a node. (3) **Compositional API**: `<Canvas><CanvasNode .../><CanvasEdge .../></Canvas>` — `CanvasNode` is now a proper component that reads from `CanvasCtx` and renders its own DOM; `CanvasEdge` is a descriptor (returns null, Canvas reads its props). Canvas still accepts `nodes`/`edges` data props for backward compat; compositional children take precedence. `CanvasEdge` added to `index.js` exports and TypeScript types. `onNodeMove(id, x, y)` added for compositional-mode edit drag (vs `onNodesChange` for data mode). Storybook stories updated to use compositional API. a1-web configurator switched to `<CanvasNode>/<CanvasEdge>` children; `onNodeMove` tracks positions in edit mode. (4) **`edgeStyle` prop** on Canvas: `"straight"` (default) | `"curved"` (quadratic Bézier with perpendicular offset capped at 80px). Per-edge `curved` boolean on `CanvasEdge` overrides the canvas-level default. Edge rendering switched from `<line>` to `<path>` so both modes share one path builder. Configurator "Edge shape" control added. TypeScript types and component registry docs updated. |
| 2026-06-23 | **Canvas + CanvasNode components** (A1-312) — new infinite pan/zoom canvas for node graphs. `Canvas` accepts `nodes` (id, x, y, label, shape, color, subtle, sublabel) and `edges` (id, from, to, direction, variant, weight, label) data props and renders nodes as labeled shapes connected by SVG lines with optional arrowheads. Node shapes: `circle` (default), `square`, `squircle`, `rectangle`. Color variants: `neutral / info / success / warn / error / accent` with `subtle` surface tint mode. Edge variants: `solid / dashed / dotted`; weights `normal / heavy`; directions `to / from / both / none`. Features: dot-grid overlay, CSS-transform zoom toward cursor (wheel), pan (space+drag or middle-click), zoom controls overlay (zoom in/out/fit/reset), draggable nodes in `mode="edit"`. `CanvasNode` exported separately. Token `component.canvas.node.size` (80px) added to `system/tokens/component/canvas.json`. Design rules in `system/rules/canvas.yaml`. Storybook stories: Default, EditMode, Shapes, Colors, EdgeVariants, NoGrid, PageBackground. a1-web configurator at `/components/canvas` with `bareDisplay: true`. Registered in Data category (`data.js`, `detail/index.js`). Status: `experimental`. |
| 2026-06-23 | **Snackbar — Native / Pure / Web codebase targets + `<a1-snackbar>` web component** (A1-309) — Snackbar configurator gains a **View as** selector (React · Native · Pure · Web) with snippet builders for all four targets. Pure HTML BEM classes (`.a1-snackbar`, `.a1-snackbar--default`, `.a1-snackbar--{position}`, `__content`, `__action`, `__close`) added to `packages/pure/dist/a1-base.css`; Pure row updated to ✓ in components registry. New `packages/web-components/src/components/snackbar/a1-snackbar.js` Lit element: reflects `open`, `position`, `action-label`, `dismissible`; dispatches `a1-action` + `a1-close` (bubbles, composed). Button lg font weight reduced from extra-bold (900) to bold (700) across `button.css`, `a1-base.css`, and the `<a1-button>` web component. Button md class fix: `_classes()` no longer emits `a1-button--md` for md size, matching the React Button where md is the default/base style. |
| 2026-06-23 | **Button small size fix** (A1-274) — `size="sm"` was rendering ~37px instead of 28px because the default `padding-block` (10px per side) overrides `min-height`. Added `component.button.small.paddingBlock` token (`{base.spacing.4}` = 4px); applied as `padding-block` in `.a1-button--sm` (React `button.css`) and `.a1-button-small` (`a1-base.css`, `a1-pure.css`). Tokens rebuilt. |
| 2026-06-23 | **Spacing utility zero values** — `spacing.css`, `Spacing.stories.jsx`, the a1-web utility registry, and the Utilities foundation page now include explicit `0` reset values for padding, margin, and gap utilities (`a1-p*-0`, `a1-m*-0`, `a1-gap-0`). In the editor this is distinct from **None**, which removes the utility class. |
| 2026-06-23 | **Utilities foundation category tabs** — the a1-web Utilities foundation page now uses A1 Tabs to group spacing, gap, width, screen-reader-only, and component-support content into separate category panels while preserving the existing demos and lookup tables. |
| 2026-06-23 | **Utilities override component defaults** — spacing and width utility classes now use explicit override declarations, so editor-applied utilities win on the actual component root when a component already defines the same property (for example Card padding or Section min/max width). |
| 2026-06-23 | **a1-web component configurators — shared Utilities accordion** — `ComponentDetailPage` now mounts `UtilityControls` centrally for every component detail Configure panel when the component type accepts utilities. Component catalog titles/ids are normalized to page-definition component names before hitting `utilityRegistry.ts` (e.g. Icon Button → `IconButton`, Textarea → `TextareaField`). Utility selections live on `config.utilities`; `ComponentDetailPage` resolves the validated class string and passes it into detail previews/snippets as `utilityClass`, so the class is emitted on the actual component root instead of a wrapper around the preview. Section uses the same central accordion placement as every other component while still applying utilities to the real Section preview/snippet directly, avoiding duplicate accordions. |
| 2026-06-23 | **Utilities foundation component lookup** — the a1-web Utilities foundation page now renders a component-by-utility lookup table from `componentCategories` plus `utilityRegistry.ts`, so docs show the same accepted utility families as the editor configurators. `utilityRegistry.ts` now exports `utilityTypeForCatalogComponent()` for shared catalog id/title normalization. |
| 2026-06-23 | **a1-web Backlog horizontal scroll strips** (A1-194) — desktop backlog swimlanes now render inside a focusable horizontal scroll strip. The strip contains the existing A1 `Grid`, but `a1-web-backlog-board-grid` overrides its tracks to `minmax(var(--base-content-width-xs), var(--base-content-width-xs))`, matching each lane `Section`'s fixed `xs` width utility classes. This keeps cards inside their lane boundaries and moves overflow to the board strip instead of the page. a1-web only — no design-system component API change. |
| 2026-06-23 | **ToolbarGroup overflow** — added `overflow` (boolean, default false) to non-grid `ToolbarGroup`. When enabled, the group measures available inline space with `ResizeObserver`, keeps as many options visible as fit, and adds an icon-only overflow `ToolbarMenu` (`more_horiz`) containing the full option list in original option order. `ToolbarGroup` options also gained `showLabel` for per-option visible-label overrides and `overflowPriority` so visible buttons can prioritize common options without reordering the menu. Updated `Toolbar.jsx` / `toolbar.css` / `.d.ts` and added an `Overflow group` story. a1-web utility controls now pass all registry values to one overflowing ToolbarGroup instead of manually composing a separate More menu, and the Toolbar configurator exposes `overflow` for group tools. |
| 2026-06-23 | **a1-web Editor utilities** — added component-scoped utility support for page-definition nodes. New `apps/a1-web/src/editor/utilityRegistry.ts` exposes `getAcceptedUtilitiesForComponent()` / `componentAcceptsUtility()` / `utilityClassesFor()` / `cleanUtilities()` so utility eligibility is data-driven and future utility families can expand centrally. `ComponentNode` gains `utilities` (width/spacing selections stored outside `props`), the renderer and JSX exporter merge validated classes, and the Configure panel shows a collapsed **Utilities** accordion with one labelled Toolbar per accepted utility; common values are quick buttons and less-common values sit in an overflow menu. The Section component configurator now reuses the same shared accordion and emits utility classes in its preview/snippet; Section intentionally does not accept the gap utility. The Utilities foundation page now lists every registry family plus gap and full min/max width values. `IconButton` explicitly rejects min/max width while `Button` can accept them. a1-web only — no design-system component API change. |
| 2026-06-23 | **Width utility + shared content-width tokens** (A1-282) — new `packages/react/src/utilities/width.css`: `a1-max-w-*` / `a1-min-w-*` on the `3xs…2xl` (+ full/none/0) utility scale, using logical `inline-size` (RTL-safe). New `--base-content-width-{3xs,2xs,xs,sm,md,lg,xl,2xl}` tokens (`system/tokens/size.json`, 5/10/15/20/25/30/40/50rem); `Section`'s `contentWidth` classes (`section.css`) use the `xs…2xl` subset and were retrofitted onto these tokens, removing the hardcoded rem values. Exported as `./utilities/width.css`; `Utilities/Width` Storybook story; imported in `.storybook/preview.jsx`. Tokens + html-css rebuilt (also synced a stale `--semantic-shadow-md` in generated `themes.css`). |
| 2026-06-23 | **a1-web — Main menu top-level groups got icons** (A1-262) — added an `icon` to each top-level `navItems` group in `main.jsx` (Explore → `menu_book`, Foundations → `foundation`, Components → `widgets`, Editor → `design_services`); they were the only nav entries lacking one. Renders via the existing TopHeader (`navIconPosition="start"`) + mobile SideNav drawer — no design-system component changed. Data-only. |
| 2026-06-23 | **a1-web — New ticket dialog: focus Title + submit on Enter** (A1-281) — `CreateTicketDialog.jsx` focuses the Title on open (a `titleRef` focused via `requestAnimationFrame` in the open-effect, after the Dialog's `showModal()`, since the always-mounted dialog can't use React's mount-time `autoFocus`) and submits on **Enter** in the Title (`onKeyDown` → existing `handleSubmit`, guarded for empty/busy + IME composition). No design-system component changed. |
| 2026-06-23 | **a1-web — Build with AI includes linked tickets** (A1-283) — the Build-with-AI plan now folds in the ticket's linked tickets (A1-218). `buildTicketContext` / `buildPlanRequest` / `developPlanLocally` (`services/backlog/devPlan.ts`) take a `linked: BacklogItem[]` arg and add a **Linked tickets** section (ref + type/status + title + short description); `TicketAiPrompt.jsx` resolves `item.links` against `backlog.items` and threads it through. No design-system component changed. |
| 2026-06-22 | **a1-web — Data-driven collections (fill items/options from data)** (A1-94) — a component's array prop can be generated from a dataset. New `ComponentNode.collections?: Record<prop, { dataset, mode, column?, map?, limit?, random? }>` (`pageTypes.ts`); node field so configurator round-trips don't clobber it. New `src/data/collections.ts`: `COLLECTION_TARGETS` (DefinitionList `items`; ChoiceGroup/Select/Autocomplete `options`) + `expandCollection(binding, target, datasetMap, rowContext, seedKey)` — modes `rows` (one entry per row, columns→fields via `map`, with limit/random), `fields` (one `{label: column.name, value: cell}` per column of the active row — DefinitionList "show all details", composes with detail-page/repeat rowContext), `distinct` (one entry per unique value of a column — auto-growing ChoiceGroup categories). `pageRenderer` expands it into `resolvedProps[target.prop]` after binding resolution and skips the DefinitionList inline-item editors when `node.collections.items` is set. Editor UI `src/editor/EditorCollectionControls.jsx` (dataset + mode + column + field-map + limit/random), threaded `handleSetNodeCollections` (mirrors repeat) through `EditorPage` → `EditorAsidePanel` → `EditorPropsPanel`. No design-system component changed. |
| 2026-06-22 | **a1-web — Detail pages (one template per item)** (A1-94) — a page tagged to a dataset shows one item's details, addressed by `?item=<__id>`. `PageMetadata` gains `detailDataset` + `detailPreviewId` (`pageTypes.ts`); `PageConfigForm` gets a **Shows details for** dataset select + **Preview item** picker (`EditorPropsPanel` `PageDetailField`); `handlePageMetadataChange` persists them. `RenderPageDefinition` takes an `itemId` prop and builds a root `rowContext` for the detail dataset (`findRowIndexById` on `__id`; live = URL item, editor = `detailPreviewId`, fallback first row) — so non-repeated `{{ dataset.column }}` resolve to the current item. `EditorPreviewPage` reads/carries an `item` URL param through `navigateToScreen`; `pageRenderer` link clicks parse `&item=` (`itemIdFromHref`) and pass it via `onNavigate(pageId, { item })`. `PageLinkField` emits `?page=<id>&item={{ key.__id }}` when the target page is a detail page (resolves the target's definition for `detailDataset`) so list cards repeated over the dataset deep-link per item. Plus a one-time **row-id backfill** in `DataSourcesContext` for datasets created before ids. No design-system component changed. |
| 2026-06-22 | **a1-web — Repeat limit + random; "Crocheted animals" sample** (A1-94) — `ComponentNode.repeat` extended from a dataset-key string to `string | { dataset, limit?, random? }` (legacy string still accepted via `normalizeRepeat`). New `src/data/repeat.ts`: `normalizeRepeat` + `pickRepeatIndices(rowCount, cfg, seedKey)` — caps to `limit` (≥1) and, when `random`, Fisher–Yates shuffles via a **seeded** mulberry32 PRNG (seed = node id + row count) so the selection is stable across renders yet differs per element; returns **original** dataset indices so bindings resolve correctly. `pageRenderer` uses it; `EditorBindControls` gains **Show at most** (`NumberField`) + **Pick rows at random** (`Switch`); `onSetRepeat` now passes the config object (threaded unchanged through `EditorPropsPanel`/`EditorAsidePanel`/`handleSetNodeRepeat`). Second built-in dataset sample **Crocheted animals** (`services/dataSources/samples.ts`): 8 amigurumi with name/description/hours/price/customizable(bool)/image (verified low-res Unsplash photo URLs). No design-system component changed. |
| 2026-06-22 | **a1-web — Repeat element over a data source** (A1-94) — an element can repeat once per dataset row ("auto-build UI from a dataset"). New `ComponentNode.repeat` field (dataset binding key) in `pageTypes.ts`. `pageRenderer.tsx` expands a `node.repeat` node into one `RenderNode` per row, each wrapped in a `PageDataContext` whose `rowContext[key] = i`; `resolveRef`/`resolveBinding`/`resolveBindingsToString` (`data/bindings.ts`) now take a `RowContext` so a token's default row is the active repeat row (an explicit `.N` index still wins). In the editor the first copy stays interactive; copies 1..N render with `EditorModeContext` `enabled:false` (read-only projections); preview renders all non-interactive. Set via a **Data → Repeat for each row** menu in `EditorBindControls`, threaded `handleSetNodeRepeat` (mirrors `setNodeLockInNode`/`handleSetNodeLock`) through `EditorPage` → `EditorAsidePanel` → `EditorPropsPanel`. `repeat` lives on the node (not props) so configurator round-trips don't clobber it. No design-system component changed. |
| 2026-06-22 | **a1-web — Bind-to-data menu in the Configure panel** (A1-94) — selecting an element shows a **Bind to data** section (`src/editor/EditorBindControls.jsx`) listing its bindable fields (Text content + free-text string props via `BINDABLE_PROP_KEYS` — `href`/`src`/`alt`/`caption`/`label`/… ; enums excluded), each a `SelectField` menu of the project's datasets (optgroup per dataset, columns as options). Picking sets the field to `{{ key.column }}`; a Clear button unbinds. Wired into `EditorPropsPanel` — reuses the node's `configToNodeUpdate(config)` as the base, overriding the chosen field via `onNodePropsChange`. Datasets filtered by `datasetAvailableToProject(ds, projectId)`; locked fields omitted. No design-system component changed. |
| 2026-06-22 | **a1-web — Data binding into pages** (A1-94) — a component's text/content or a string prop can bind to a dataset value via a `{{ dataset.column[.row] }}` token, resolved at render. New `src/data/bindings.ts` (grammar + `buildDatasetMap`/`resolveBinding`/`resolveBindingsToString`/`hasBinding`; whole-token keeps raw type, embedded interpolates, unresolved renders literally). `pageRenderer.tsx` gains a `PageDataContext` (built in `RenderPageDefinition` from the active project's datasets via `datasetAvailableToProject` + `getActiveProjectId`) that `RenderNode` consumes to resolve string props (after the resolvedProps merge) and text content (a new bound branch that shows the resolved value read-only on the canvas, before InlineEditable). New `EditorDataPanel.jsx` + a **Data** tab in `EditorAsidePanel` listing each dataset's columns as click-to-copy `{{ key.column }}` tokens. No page-definition schema change (bindings are plain string values). No design-system component changed. Repeating over rows is a later step. |
| 2026-06-22 | **a1-web — Data sources (datasets) feature** (A1-94) — new **Data sources** page (`?page=data`, left nav under the editor group) for reusable datasets: named tables of columns + rows edited in the A1 `DataGrid`, **scoped** via `projectIds` (empty = global, else specific projects — the image/pattern idiom), with a **JSON importer** and **autosave**. Storage mirrors the backlog service: a dedicated Supabase **`data_sources`** table (RLS + `set_updated_at` trigger + Realtime/poll) when signed in, else gzipped localStorage. New `apps/a1-web/src/services/dataSources/` (`types`/`backend`/`localBackend`/`supabaseBackend`/`dataSourcesStore`/`importJson`/`samples`), `src/data/DataSourcesContext.jsx` + `DataSourcesView.jsx`; `main.jsx` registration (PAGES/PAGE_TITLES/nav/render/provider). Built-in **"Users" sample** seeded once into an empty workspace. Consumes the experimental design-system `DataGrid` via the package `./components/*` subpath; `react-data-grid` added as an a1-web runtime dep. **Cloud needs the `data_sources` DDL run** (`apps/a1-web/supabase/schema.sql`); degrades gracefully until then. No existing design-system component changed (not in the configurator). |
| 2026-06-22 | **`DataGrid` — experimental react-data-grid wrapper (Storybook-only)** (A1-94) — added `packages/react/src/components/data-grid/` to evaluate **react-data-grid** (Comcast, `7.0.0-beta.59`, `devDependency`) as the editable **data editor** for the page-data / data-binding feature. Thin wrapper (`DataGrid.jsx`) merges an `a1-data-grid` class onto the grid root; `data-grid.css` maps every `--rdg-*` theming variable to an A1 token (the library scopes its CSS in `@layer rdg`, so unlayered A1 rules win → follows the active theme). Re-exports `textEditor` + `SelectColumn`. Four stories (Editable / RowSelection / SortableResizable / ReadOnly). **Intentionally NOT a finished component:** not in `src/index.js`, not in the registry tables, not in the a1-web configurator — Storybook only, pending evaluation. No existing component/token/theme changed. |
| 2026-06-22 | **a1-web — Backlog linked tickets** (A1-218) — explicit, symmetric ticket-to-ticket links (distinct from A1-161 merge). New `links: string[]` on `BacklogItem` + `UpdateTicketPatch` (`types.ts`); defaults/migration in both backends (`localBackend` createItem + listItems map; `supabaseBackend` `rowToItem` + `rowFromPatch`; `schema.sql` `links jsonb default '[]'` + guarded `alter table`); `linkTickets`/`unlinkTickets` in `backlogStore` (write both sides) + `link`/`unlink` on `BacklogContext`. UI: `TicketMergePanel` (the dialog's Linked-tickets tab) gains a linked-tickets list (open + unlink), a **Link** action on similarity suggestions + the context menu, and a "Link or merge by ID" field. Built from existing A1 components — no design-system change. |
| 2026-06-22 | **a1-web — Backlog views: segmented control + one filter set for all views** — the top-level Board/All tickets/My queue/Virtual team **`Tabs` were replaced by a `SegmentedControl`** at the top of the right-hand filters panel (icon segments, `fullWidth`, `labelMode="selected"`); the main area renders the active view by `tab` directly. The panel's **Type/Priority/Size/Scope + search now drive all ticket views** via a shared `filteredItems = applyFilters(searched, filters)` — board (`byStatus`), the All-tickets `DataTable` (its own column `filters` removed), and My queue. Sort stays board-only; filters hidden only on the dev-only Virtual team view. `pages/Backlog.jsx` only — no design-system change. |
| 2026-06-22 | **a1-web — Backlog board: drag-to-restatus + always-show swimlanes** — desktop board cards are now draggable between swimlanes to change status (`BoardCard` `draggable`/`onDragStart`; `BoardColumn` `onDragOver`/`onDrop` → `backlog.update(item, { status })`; `dragOverStatus` highlight via `.a1-web-backlog-lane--drop`). A toggled-on swimlane **always renders** even when empty/filtered (dropped the `byStatus[s].length > 0` filter + the "no tickets match" short-circuit), so columns stay stable while filtering/dragging. Keyboard path = the ticket dialog's status control. `pages/Backlog.jsx` + a `styles.css` lane rule; both `Card` and `Section` already forward DOM props (`...props`) so no design-system change. |
| 2026-06-22 | **a1-web — Backlog filters: app right-hand panel + persist + clear all** (A1-154) — the board's Sort + Type/Priority/Size/Scope filters **moved into the app's right-hand aside rail** (the same `PageLayout` aside slot the editor/theme configurators use — `main.jsx` adds `#a1-web-backlog-aside-slot`; `Backlog.jsx` `createPortal`s the filter panel into it), so it spans the whole page and **auto-moves to a “Filters” BottomSheet at xs/sm**. The board keeps only the swimlane toggles. Filters **persist across reloads** (localStorage `a1-backlog-filters`, with sort + search), and a **“Clear filters (n)”** action resets filters + search in one click; the panel heading shows the active count. `pages/Backlog.jsx` + `main.jsx` — `DEFAULT_FILTERS` / `loadFilterState` / `countActiveFilters` + a persist `useEffect` + portal; built from existing A1 components, no design-system change. Also on the same branch: Backlog header re-layout (pill `SearchField` top-right of the title; New ticket → small secondary button below the description). |
| 2026-06-22 | **SearchField — new field-family component** — a search input with a **leading search icon** and a **trailing clear button** (shown only when there's a value). Composes `TextField` via `inputOverlay` (like `NumberField`), renders `type="search"` (native clear suppressed), works controlled/uncontrolled (clear dispatches a native input event so controlled `onChange` fires), adds `onSearch` (Enter) / `onClear` / `clearLabel`, and is **pill-shaped** by default (overrides `--a1-field-border-radius` → `--base-radius-pill`). New `SearchField.jsx` + `.d.ts` + `.stories.jsx`; CSS in `field.css` (`.a1-field--search` / `--search-has-value`, logical padding, native-clear suppression — no new tokens); exported from the package index; new `field.clearSearch` label (`system/labels/field.json`, 7 locales). a1-web: the **Components sidebar** search now uses `SearchField` (was a bare `TextField type="search"`), and a **configurator** page was added (`detail/search-field.jsx` via `createFieldModule` + Clear-button-label control; registered in `detail/index.js` and `data.js` Inputs as `search-field`, React/beta). components.md registry updated. **React only** (Pure/Native + the `examples/a1-pure` page are follow-ups). |
| 2026-06-22 | **a1-web — Backlog smart search** (A1-187) — a relevance-ranking search across the whole backlog (new `apps/a1-web/src/services/backlog/search.ts`: `smartSearchBacklog`/`searchBacklog`/`parseSearch`), local + deterministic (no API/credits, reuses the duplicate-finder's tokenising). Scores weighted fields (ref > title > scope/labels > description), recognises a direct `A1-<n>` reference, AND-matches multiple terms, and parses `field:value` qualifiers (`is:open`/`is:closed`, `type:`, `status:`, `priority:`, `scope:`). Wired into `pages/Backlog.jsx` as a page-level `TextField` above the tabs that filters board lanes (relevance order), the All-tickets table (replaces its old title/requester search), and My queue, with a result count + clear `IconButton`. Built from existing A1 components — **no design-system component changed**. |
| 2026-06-22 | **a1-web — Build with AI develops a plan via local AI** — the Backlog ticket dialog's dev-only **Build with AI** tab (`src/backlog/TicketAiPrompt.jsx`) now turns a ticket into a **development plan** instead of concatenating its fields. New `src/lib/localAi.ts` (an Ollama HTTP client — `listLocalModels`/`chooseModel`/`localChat`, host+model in localStorage; **no Anthropic API/credits**) and `src/services/backlog/devPlan.ts` (`buildTicketContext`, `PLAN_SYSTEM`/`buildPlanRequest` for the local model, and `developPlanLocally` — a deterministic staff-developer planner that scales by complexity, branches steps/files by type+scope, and surfaces unanswered Virtual PO questions). The tab sends the ticket to a local LLM when one is reachable, else falls back to the deterministic planner, so it always yields a real editable plan; a "Show raw ticket details" toggle keeps the old view, and it reports the local model + elapsed time when used. Dev-only; built from existing A1 components — **no design-system component changed**. |
| 2026-06-22 | **a1-web — Virtual Information Architect (dev only)** (A1-176) — a new local, deterministic "virtual team" member (no API/credits) that audits **information architecture** against IA/UX heuristics, alongside the Virtual PO in the dev-only **Virtual team** tab of the Backlog. New engine `apps/a1-web/src/services/architect/` (`types.ts`, `heuristics.ts` — 13 category-tagged heuristics: home affordance, ordering, top-level count, group size/cohesion, naming clarity (vague labels), redundancy, scalability, label casing/length, duplicate siblings, depth, top-level icons; `mainMenu.ts` builds the audited NavModel from live `componentCategories`/`foundations`; `architect.ts` `auditNav()` + profile; `index.ts`). UI `src/backlog/VirtualArchitectPanel.jsx`: launcher card → **Dialog** (`size="lg"`) with **Findings** (status-striped `Card`s per finding) and **History** tabs (`Tabs equalHeight`). It's **read-only** (reports + suggests) but can **file any finding as a backlog ticket**; de-dup is **persistent** (each ticket carries the finding's stable ref, so an already-filed finding shows "Filed as A1-n" and is skipped by "File all" across reloads). Acronyms (FAQ/API) exempt from the casing check. Built entirely from existing A1 components — **no design-system component changed**. Wired into `pages/Backlog.jsx` (dev-only tab). Page-architecture/flow/object-model/a11y-architecture reviews are a documented follow-up roadmap. |
| 2026-06-22 | **Slider — detent ticks/handle alignment + thumb fits the channel** — the detent tick dots and labels drifted off the thumb centre (most visible at the ends), and the thumb bulged *outside* the track channel (contradicting the "track contains the thumb" intent). Two root causes in `slider.css`: (1) the thumb's 2px ring used default `content-box`, so its rendered width was `thumb-size + 4px` — wider than the `thumbLeft(p)` = `thumb-size/2 + p·(100% − thumb-size)` calc the ticks/labels use, pushing the thumb centre off the ticks; (2) the track's 1px `border` inset the thumb's travel range. Fixes: thumb pseudo-elements (`::-webkit-slider-thumb` / `::-moz-range-thumb`) now `box-sizing: border-box` (rendered width = `thumb-size`, ring drawn inside, thumb sits flush in the channel); the track channel outline is an **inset `box-shadow`** instead of a `border` (no travel inset) on both `::-webkit-slider-runnable-track` and `::-moz-range-track`; and the tick dot transform is `translate(-50%, -50%)` (was `-60%, -33%`) so it centres exactly on `thumbLeft(p)`. Native thumb centre now equals the tick/label position at every `p`. **`slider.css` only — no API/prop change**, all sizes/themes, token-only. |
| 2026-06-21 | **Marshmallow theme — semantic shadow reuse** — changed Marshmallow's button, switch thumb, and segmented thumb component shadow hooks to consume the theme's semantic shadow/well variables (`--semantic-shadow-md/lg/sm/xs`, `--semantic-well`) instead of repeating raw component-level shadow stacks. Dialog and Card were already wired to semantic shadows. No component API change. |
| 2026-06-21 | **Slider — handle inside the track + bordered channel** — restyled so the track is taller than the thumb and *contains* it (4px inset all round) rather than the thumb sitting on a thin rail; the whole track is now the touch target (bigger), and the track gets a subtle 1px border (`--a1-slider-track-border` → `--semantic-color-border-default`). Sizes scale together (compact track/thumb 20/12, default 24/16, comfortable 32/24 px) via the existing variant layer; thumb centered by the existing `(track − thumb)/2` margin formula (now positive). `slider.css` only — **no API/prop change**, all themes, token-only (passes the new lint + CSS gate). Improves WCAG 2.2 target size. |
| 2026-06-21 | **Marshmallow theme tweaks** (A1-222) — **theme-token only** (`system/themes/marshmallow/theme.json` + `tokens/typography.json`; no component changes). Primary button made subtler so its dimension reads: light `accent-300` fill + dark `accent-900` text (was saturated solid), `--component-button-border-radius` 14px → 10px, and an inset-structure box-shadow. Tuned raised/inset neumorphic shadows + recessed **wells** (`--semantic-well` / `-soft`) across buttons, cards, dialog, fields, switch & segmented-control tracks/thumbs; new elevated `--semantic-shadow-lg` (Menu / Snackbar / Toolbar / TopHeader). Heading + display font → **Space Grotesk** (weights 500/600; Nunito body), added to the a1-web font link. Components without a shadow-token hook (badge, banner, status-bar, circular-progress, step-tracker, tree-menu, bottom-sheet, icon-button) and a Marshmallow **dark mode** are deferred (would need component CSS, out of the theme-only scope). |
| 2026-06-21 | **Rules → real enforcement: `eslint-plugin-a1` + CI gate** (A1-37) — the design rules are no longer only documentation. New workspace **`packages/eslint-plugin-a1`** with four rules linked to the YAML: `no-uppercase-text` (`system-no-uppercase-content`), `no-raw-color` (new `system-tokens-only` rule), `no-nested-interactive-in-card` (`card-navigation-no-nested-interactive`), `single-primary-button` (`button-single-primary-action`); each carries its YAML `a1RuleId` + RuleTester tests (4 suites). **YAML stays source of truth**: a rule opts in with `enforcement.eslint: <name>` (+ `css: true`); `scripts/build-eslint-rules.mjs` generates `lib/generated/rule-meta.js` and **validates a 1:1 mapping** (`--check` fails CI on drift). Root **`eslint.config.mjs`** (flat, ESLint 10 + typescript-eslint) gates `packages/react/src` + `apps/a1-web/src`: **error on production source, warn on stories**; **CSS arm** `scripts/lint-a1-css.mjs` enforces no-uppercase + tokens-only on hand-authored component CSS (`a1-base.css` included; the `a1-pure.css` example + generated theme files excluded). New scripts `lint` / `lint:fix` / `lint:rules` / `build:eslint-rules`; CI **`.github/workflows/lint.yml`**. **Fixed real violations:** `DataTable.jsx` avatar initials (per-char `charAt(0).toUpperCase()`), raw colours → tokens in `tokens/_shared.jsx` + `page-nav.css` + `token-select.css`. **Also fixed YAML parse bugs** in `motion.yaml` + `side-nav.yaml` (unquoted `colon-space` values) that were silently dropping those rules from the app. a1-web Rules page gained an **Enforced** column + filter (Lint / Lint + CSS / Docs) from the YAML `enforcement` field. No design-system component API changed. |
| 2026-06-21 | **Theme — Marshmallow (soft pillowy pastel / subtle neumorphism)** — new system theme `system/themes/marshmallow/` (`theme.json` + `tokens/typography.json`). Dusty-lavender accent ramp on warm marshmallow-cream surfaces (`surface-page` `#EBE6E1`, `surface-card` `#F3EFEB`, recessed `surface-field` `#E6E0D9`), reusing crochet's pastel info/success/error/warn ramps. **Subtle neumorphism via tokens only:** buttons read `--component-button-box-shadow` (raised dual highlight+shadow) / `-hover` (more lift) / `-active` (inset = pressed-in) + `--component-button-press-transform: translateY(1px)`; cards/raised surfaces get a soft dual `--semantic-shadow-xs`/`-sm`; softened `--semantic-shadow-md` + `--component-dialog-shadow`. Generous rounding (`--base-radius-md/lg/xl`, button `14px`, card `18px`, dialog `20px`). Type: **Varela Round** display+headings (single 400 weight; hierarchy from size) + **Nunito** body (`--theme-a1-marshmallow-font-*`). **One component change (theme-agnostic, no-op for existing themes):** `.a1-button--tertiary` now nulls `--component-button-box-shadow`/`-hover`/`-active` so ghost buttons stay flat (a raised shadow would halo a transparent body). Registered in `apps/a1-web/src/main.jsx` (themeOptions + class toggle), `aiProjectStyle.ts` vibes, `.storybook/preview.jsx` (toolbar + toggle + cleanup), Varela Round added to `apps/a1-web/index.html` font link. React + a1-web (matches crochet/aperture — no Pure dist). |
| 2026-06-21 | **a1-web — Virtual team panel moved into a Backlog tab** (A1-210) — the dev-only `VirtualTeamPanel` was a bordered `Section` stacked above the entire Backlog page; it's now a fourth **tab** ("Virtual team", `groups` icon) next to Board / All tickets / My queue (`src/pages/Backlog.jsx`). Both the `Tab` and `TabPanel` are `import.meta.env.DEV`-gated. `VirtualTeamPanel` shed its redundant outer bordered-card `Section` + duplicate "Virtual team" `Heading` (the tab provides the title) — now a Fragment of the content `Stack` + the preview `Dialog`, keeping the "Dev only" badge + intro. No design-system component changed. |
| 2026-06-21 | **Card status — `card-status-not-color-only` rule, lighter warn/success stripe, Backlog status mapping** (A1-188 follow-up) — **Rule:** new `card-status-not-color-only` in `system/rules/card.yaml` — a Card's `status` must be conveyed by text, not the colour stripe alone (WCAG 2.1 SC 1.4.1, Level A); pair with `statusLabel` or status text the card carries/sits within (e.g. a labelled board lane). **Bug fix (same file):** `card.yaml` never parsed — the `cards-in-grid` rule's `do:` contained `{{ xs: 1, md: 3 }}` (a bare `colon-space` YAML reads as a nested mapping), so `ruleStore`'s try/catch silently dropped **all** Card built-in rules; quoted the value so the file parses (6 rules now load in the a1-web Rules page + Storybook `Rules.stories`). **Styling:** Card `warn`/`success` stripe colours are now **two ramp steps lighter** (warn-500 `#d97706`, success-500 `#16a34a`) via new `component.card.status.{neutral,info,success,warn,error}` tokens (`card.json`) that the `.a1-card--status-{tone}` classes consume — the very dark `-700` background tone read as brown/near-black in a thin stripe. **a1-web Backlog:** ticket→stripe mapping moved to `STATUS_STRIPE_TONE` / `STATUS_STRIPE_PULSE` in `services/backlog/types.ts` (New → no stripe, Triaged → neutral, Accepted → info, In progress → info + pulse, Done → success + pulse, Released → success; terminals unstriped); board + queue cards use them. Card.d.ts/components.md updated. Token + html-css rebuilt. |
| 2026-06-21 | **Card — `status` side stripe + badge, with in-progress pulse** (A1-188) — new `status` (`"neutral" \| "info" \| "success" \| "warn" \| "error"`) draws a tokenized **side stripe** down the card's inline-start edge (clipped `::before`, colors mirror MessageBadge tones); `statusLabel` renders a matching `MessageBadge` at the top of the content (the "stripe with a badge"); `statusPulse` **subtly pulses** the stripe for in-progress work and **respects `prefers-reduced-motion`** (static under reduce). New tokens `component.card.statusStripe.width` (4px) + `component.card.statusPulse.duration` (1800ms) in `system/tokens/component/card.json`. Card.jsx (`VALID_STATUS`, classes, badge) / card.css (`.a1-card--has-status`/`--status-{tone}`/`--status-pulse` + `@keyframes a1-card-status-pulse` + reduced-motion guard + `.a1-card__status-badge`) / Card.d.ts / Card.stories.jsx (argTypes + `Status` and `In progress (animated)` stories). a1-web **Card configurator** (`detail/card.jsx`): Status-stripe swatch `Choice`, conditional Status-label field + "Pulse (in progress)" toggle, all with switch-linked helper text; snippet + Preview wired; `ComponentDetailPage` property rows added. **Applied in a1-web Backlog**: board + queue cards map ticket status → stripe tone (via `STATUS_TONE`) and pulse when `in_progress`. Verified with a headless render harness (9 cases) + token/CSS build. **React only** (Pure has no Card; React Native Card parity is a follow-up). |
| 2026-06-21 | **a1-web — Virtual PO: smarter, non-repeating questions + changelog moves** (A1-209) — fixes the PO "asks a question, I answer, then it just repeats it" bug and adds backlog/changelog awareness. **No-repeat:** `runPersona`/`reviewOne` (`src/services/backlog/personas/runPersona.ts`) now read the ticket thread and never re-ask a question already asked — matched by a stable `key` on each `PersonaQuestion` *or* by exact text (legacy keyless questions still de-duplicated) — asking **≤2** at a time and **none** while a question is awaiting an answer, so after you answer it asks the *next* unasked ones instead of looping. **Smarter bank:** `productOwner.ts` now builds a **keyed, ordered question bank** (`buildQuestions`) chosen for the ticket — scope-aware (component variants/a11y, token/theme ripple, "which surface?"), **backlog-aware** (`evaluate(item, { items })` + `findRelated` → asks if it relates to another open ticket on the same scope, citing its `A1-<n>`), plus detail/users/slice/success/risk/acceptance — instead of the same generic "business value?" each run. **Changelog moves:** new `personas/shipped.ts` (`shippedStatusByNumber`) parses this app's CHANGELOG; a bulk run **moves any open ticket whose `A1-<n>` appears in it forward** (Released under a version heading, Done under Unreleased) with a move note, rather than re-reviewing it. Types: `PersonaQuestion.key`, `PersonaContext`, `PersonaChange.status/statusFrom`, `PersonaRunSummary.moved`; `BacklogContext` imports the CHANGELOG (`?raw`) + passes the item list; `VirtualTeamPanel` shows the **moved** count. Local, no API/credits. Verified with a 16-case mock-store runner harness. **No design-system component changed.** |
| 2026-06-21 | **a1-web — Backlog ticket dialog split into tabs** (A1-207) — `src/backlog/TicketDetail.jsx` now spreads the ticket view across A1 `Tabs`: **Details** (description, triage toolbars, metadata, attachments), **Comments** (the former "Activity" thread + activity log, with a count badge of real messages), **Linked tickets** (the `TicketMergePanel` similarity finder/merge, moved out of Details), and two **`import.meta.env.DEV`-gated** tabs — **Build with AI** (`DevAgentButton`) and **Virtual PO** (`TicketPersonaReview`) — that replace the old Details "dev-tools" area, so they never appear in production. Tabs carry Material Symbols icons; `equalHeight` retained so switching doesn't resize/recentre the dialog. Help: "Linked tickets" tab wording + a board-article note. **No design-system component changed** — built from existing A1 components. |
| 2026-06-21 | **a1-web — Backlog link & merge duplicate tickets** (A1-161) — a **local similarity finder** (`src/services/backlog/similarity.ts`: TF-cosine over title/description, title-weighted, + scope/type bonuses; deterministic, **no API/credits**, mirrors the Virtual Team) ranks likely-duplicate tickets and powers **merging two into one**. New **`TicketMergePanel`** in the ticket dialog: a "Linked & similar tickets" section with similarity score + reasons, a **Merge…** action (choose which survives — the other is closed `status='duplicate'`, `duplicate_of` → survivor, with its **comments, votes, and description moved over**), a "merged into this" list, a duplicate→canonical banner, and a **Merge by ID** fallback. **CreateTicketDialog** flags possible duplicates live as you type. Store: `mergeTickets()` + a `mergeItem(fromId,toId)` backend primitive (Supabase + local — reassigns thread, unions votes) + context `merge()`. Uses the **existing** `duplicate_of` column (no migration for current DBs; guarded `alter … if not exists` note added). Sync CLI exports `duplicateOf` (read-only). Help: new "Linking and merging duplicate tickets" article. **No design-system component changed** — built from existing A1 components. |
| 2026-06-21 | **a1-web — Build with AI prompt + per-ticket Virtual PO + backlog-read fix** — **Build with AI:** the ticket dialog's dev-only "Build with AI" tab (`src/backlog/TicketAiPrompt.jsx`) assembles a copy-pasteable prompt (metadata + description + discussion) in an editable code block to paste into a coding-agent chat. (An autonomous Claude Agent SDK version — local server + `dev-agent:work` CLI — was built then **rolled back** the same day: max-turns + the in-app log was lost to HMR reloads; removed the SDK dep, `scripts/dev-agent-*.mjs`, and the npm scripts.) **Per-ticket PO:** dev-only "Review with {role}" in the ticket dialog (`src/backlog/TicketPersonaReview.jsx`) reviews one ticket + re-evaluates on change — extracted `reviewOne` / `runPersonaOnItem` (shared with the bulk panel) + `reviewItem` context action. **Fix:** the Supabase backlog backend now `select('*')` instead of an explicit column list, so a just-added optional column (`reviews`) no longer breaks `listItems()` on a DB that hasn't run the migration (the "no backlog items" regression). |
| 2026-06-21 | **a1-web — Virtual Team / Virtual Product Owner (dev-only, local, no API)** — a pluggable persona engine (`src/services/backlog/personas/`: `types.ts`, `productOwner.ts`, `index.ts` registry, `runPersona.ts`) that reviews the backlog with pure heuristics — the PO sets priority/size from impact + vote demand and asks clarifying questions. Dev-only "Virtual team" panel on the Backlog page (`import.meta.env.DEV`-gated) with **preview (dry run) → apply**. **Reviewed tag**: per-persona `reviews[id] = { at, sig }` on the ticket (new `reviews jsonb` column on `backlog_items` + types + both backends) shown as a tag; re-evaluates only when a content **signature** (`reviewSignature`) changes. **Answerable questions**: question comments carry `meta.options` → an inline **A/B/C/Other** mini-form; answers link via `meta.answersCommentId` (`answerQuestion` store fn + context `answer`). Store: `addPersonaComment` (persona-attributed, `meta.persona`). Verified E2E: review → tag → answer → idempotent re-run skips unchanged. No design-system change. |
| 2026-06-21 | **a1-web — removed the Roadmap (md) page** — deleted `?page=todo` (the read-only `TODO.md` viewer, `pages/TodoPage.jsx`) now that the Backlog (`?page=backlog`) replaces it; removed from `PAGES` / `PAGE_TITLES` / `RESOURCE_PAGE_IDS` + icon + import + render in `main.jsx`. `TODO.md` itself remains as the agent-maintained backlog file (read by agents + `backlog:import`). A stale `?page=todo` link degrades to home. |
| 2026-06-21 | **a1-web — Help screenshots across topics, desktop + mobile** (A1-105) — a reusable Playwright capture script (`apps/a1-web/scripts/help-screenshots.mjs`, `npm run help:screenshots`) writes real UI screenshots to `public/help/` at **both desktop and mobile** sizes (`<name>.png` / `<name>-mobile.png`); 7 states cover Projects list, the Editor menu / mobile nav drawer, a project's Pages tree, the editor canvas + Configure panel, Patterns, Image library, and the Backlog board. Help renders them via a responsive `Shot` component (`useIsMobile` hook → device-appropriate image) inline-in-order across the Getting started, Projects, Pages & navigation, Selecting & editing, Patterns, Media & AI, Preview, and Backlog topics. Images are committed app assets (not gitignored). |
| 2026-06-20 | **a1-web — Help content refresh + moved to Resources** — added Help topics for **Patterns** (governed reusable compositions: create-from-selection/scratch/duplicate, use & detach, lock node/content/props + slots + project scope), **Accounts & the shared workspace** (invite-only sign-in, shared cloud workspace, cross-device sync, attributed history), the **AI icon finder**, and **responsive preview** at device widths; **corrected** the stale "everything is stored locally / isn't uploaded anywhere" claims (now: cloud sync when signed in vs local-only when signed out). Also **moved the Help entry from the Editor top-nav menu to Resources** (`RESOURCE_PAGE_IDS` + icon; removed the Editor-menu item). `Help.jsx` (HELP array) + `main.jsx` nav. No design-system change. |
| 2026-06-20 | **a1-web — Help page PageNav + changelog ticket refs** — the Help page's category jump **buttons** were replaced with the `PageNav` component (sticky, active-section tracking, reading-progress bar) in a content (`lg:3`) + sidebar (`lg:1`) `Grid`, mirroring the TODO page's `NavLayout`; the nav reflects the visible categories so it narrows with search. Resolves **A1-106**. Also: **changelog entries now reference their backlog ticket** — `(A1-<n>)` after the bold lead title (annotated A1-106 / A1-108 / A1-115 in the a1-web CHANGELOG); convention documented in `project-workflows.md` invariant 9a + `backlog/README.md`. `apps/a1-web/src/pages/Help.jsx` only — no design-system change. |
| 2026-06-20 | **a1-web — Backlog board Size filter** — added a **Size** `ToolbarGroup` (All / XS / S / M / L / XL) to the board's filter toolbars (beside Type and Priority), filtering tickets by `complexity` (the size shown on each card's `ComplexityBadge`). `apps/a1-web/src/pages/Backlog.jsx` only — `applyFilters` + `filters.complexity` state + a new `Toolbar`; no design-system change. |
| 2026-06-20 | **Tabs — overflow no longer forces width (line variant)** — a `line` tab strip with more tabs than fit (e.g. the Releases version tabs / a release with many entries) was **widening its container and forcing a horizontal page scroll** instead of scrolling internally: the scrollable list (`flex: 1`) couldn't shrink below the tabs' intrinsic `min-content` width (default `min-width: auto`), so `overflow-x: auto` never engaged. Fix: `min-width: 0` on `.a1-tab-list--scrollable` (+ defensive `min-width: 0` / `max-width: 100%` on `.a1-tabs` and `.a1-tab-list-wrapper`) so the strip scrolls internally and the **existing** prev/next chevron arrows appear automatically. Also keep the **selected** tab in view by adjusting only the strip's own `scrollLeft` (never `scrollIntoView`, which would move the page). No new prop. tabs.css + Tabs.jsx (`TabList` reads `value`, scroll-into-view effect) + annotated `Overflow (many tabs)` story. Verified via Playwright on `?page=releases` at 1280/760/380 px: 0 px page overflow, internal scroll engaged, next-arrow present. |
| 2026-06-20 | **Tabs `labelMode`** (`"all"` default \| `"selected"`) + xs/sm label fix — **removed** the unconditional `@media (--bp-sm-down)` rule that hid inactive-tab labels at xs/sm (it blanked label-only tab strips like Board/All/My-queue). Replaced with an **opt-in** `labelMode="selected"`: inactive tabs render **icon-only** (the label stays in the DOM for the accessible name) and only the active tab shows its label — pair with a Tab `icon` and give **every** Tab a label. Mirrors `ToolbarGroup`'s `labelMode`. Tabs.jsx/.css (`.a1-tabs--label-selected`)/.d.ts + `Label on selected (icon-only inactive)` story + a1-web Tabs configurator (Label-mode Choice with switch-linked helper, snippet). **a1-web:** the Backlog board is now **responsive** — md+ keeps the zero-gap Grid of swimlanes; **xs/sm shows a tab per swimlane** (`labelMode="selected"`, compact, icon + count), reusing a shared `LaneCards` for the paginated cards. |
| 2026-06-20 | **Dialog — backdrop-click dismissal** — a **dismissable** Dialog (one with `onClose`) now closes when the **backdrop** (the area outside the dialog box) is clicked, matching Escape and finally fulfilling the already-documented `onClose` contract ("Escape, close button, or backdrop click"). Robust against accidental dismissal: requires the press **and** release to both land on the backdrop, so dragging to select text and releasing past the edge won't close it. **Non-dismissable** dialogs (no `onClose`) are unaffected. No new prop — it's the default for dismissable dialogs. Dialog.jsx (mousedown/click listeners on `<dialog>`, gated on `open && onClose`) + components.md Dialog "Dismissal" note. |
| 2026-06-20 | **Tabs `equalHeight`** (opt-in, default `false`) — keeps every panel mounted and stacked in one grid cell so the panel area always reserves the **tallest** panel's height; switching tabs no longer resizes the container. Inactive panels are `visibility: hidden` + `aria-hidden` (hold their height but stay out of the tab order / a11y tree). Default unmounts inactive panels as before. For tabs inside a **Dialog/overlay** so it doesn't resize and move the targets; leave off on pages so tabs keep natural per-tab height. Tabs.jsx (context flag + TabPanel) / tabs.css (`.a1-tabs--equal-height`, `.a1-tab-panel--stacked` / `--inactive`) / .d.ts + `Equal height (stable container)` story. **a1-web:** the Backlog **board is now pure A1** (zero-gap `Grid` of `Section` swimlanes with alternating surfaces + a left-border vertical divider; replaces the custom `.a1-backlog-board` CSS), each lane **paginates** (`Pagination`, 5/lane), board **filters are toolbars** (Type/Priority `ToolbarGroup`, Scope/Sort `ToolbarMenu`), board **cards are navigation cards** opening the dialog + a right-click **`ContextMenu`** of actions, and the **ticket dialog** uses Details/Activity **tabs** with `equalHeight`, status as a primary `ToolbarGroup` + "More" `ToolbarMenu`, metadata in a `DefinitionList`, and markdown-rendered descriptions. |
| 2026-06-20 | **a1-web — Backlog (lightweight Jira)** — new `?page=backlog` (Resources menu) replacing the read-only `TODO.md` view (now "Roadmap (md)"). Supabase-backed ticket tracker, shared + attributed like `edit_history`, with a local fallback store. Tickets carry `A1-<n>` numbering, type (bug/feature/chore), suggested priority (P0–P3) + complexity (XS–XL), scope, screenshot attachments (reuse image library), votes, a comment/Q&A thread, an activity log, and in-app notifications. Board / All-tickets table / My-queue views; create-from-anywhere via a header flag button + pre-scoped buttons in the page-editor toolbar, every component page, and the theme editor. DB: `backlog_items`/`backlog_comments`/`backlog_votes`/`backlog_notifications` added to `apps/a1-web/supabase/schema.sql`. Local sync CLI `apps/a1-web/scripts/backlog-sync.mjs` (`npm run backlog:pull`/`:push` → `backlog/tickets.json` + `backlog/BACKLOG.md`). New `system/labels/backlog.json` (7 locales, registered in main.jsx `allLabels`) + Help "Backlog & feedback" section. **No design-system component changed** — built entirely from existing A1 components. Files: `apps/a1-web/src/{services/backlog/*, backlog/*, pages/Backlog.jsx}` + main.jsx wiring (BacklogProvider wraps `<App/>`; header actions). |
| 2026-06-20 | **Card — hero badge** — `heroBadge` (+ `heroBadgeStatus` default `neutral`, `heroBadgePosition` default `top-end`): a `MessageBadge` overlaid on the hero band, placed via a **3×3 grid** (`{top/middle/bottom}-{start/center/end}`). Renders only with `iconDisplay="hero"`. Card.jsx/.css (`.a1-card__hero-badge--*`)/.d.ts + `Hero badge` story + a1-web Card configurator (label + status + 3×3 alignment-grid picker). |
| 2026-06-20 | **TopHeader** — selected/current **parent flyout** icon fix: the leading icon now inherits `currentColor` (matches the selected text colour) instead of staying `text-muted`. top-header.css (scoped to `.a1-menu-item--active` / `[aria-current="page"]`). |
| 2026-06-20 | **Code** — copy + Show more/less now render **inline** in one actions row (`.a1-code-block__actions`) when a collapsible block also has a copy button; the collapse-overflow detection no longer self-cancels (cap applies whenever collapsible+not-expanded, fade only when clipped). `collapsible` stays ignored in `editable` mode (a1-web configurator disables it there). Code.jsx/.css. |
| 2026-06-19 | **Grid `alignItems`** (`start` / `center` / `end` / `stretch`) — cross-axis (vertical) alignment of items in their row; `stretch` gives equal-height items filling the row height (the grid default, now an explicit control). Grid.jsx/.css/.d.ts + `Vertical stretch` story + a1-web Grid configurator (Align items control). |
| 2026-06-19 | **Code `collapsible`** + `collapsedLines` (default 14) — cap a long read-only block with a fade + a Show more/less toggle (toggle only appears when the content overflows; block, non-editable only). Code.jsx/.css/.d.ts + `Collapsible` story + `code.showMore`/`code.showLess` labels (7 locales) + applied to the a1-web project-JSON dialog (`collapsedLines={20}`). |
| 2026-06-19 | **Paragraph `weight`** (`regular` / `medium` / `semibold` / `bold`) — body text weight via the base font-weight tokens (`--a1-paragraph-weight`; omit to inherit). Paragraph.jsx/.css/.d.ts + `Weights` story + a1-web configurator. |
| 2026-06-19 | **PageNav** sticky + scroll-container fix, and a1-web **TODO** page. **PageNav:** now **sticky on desktop** (≥769px; `top` = `--a1-page-nav-top` so consumers offset a sticky header; `max-height` + internal scroll for long lists); and the progress bar + active-section highlight now **track the nearest scrollable ancestor** (window scroll froze them inside a viewport-height `PageLayout`) — used for both the scroll listener and the IntersectionObserver `root`. page-nav.jsx/.css. **a1-web:** new **TODO page under Resources** (renders the repo-root `TODO.md`: standard Resources header + **Overview / Current / Roadmap tabs** + a right-column **sticky PageNav** in the long tabs); **Projects removed** from the Resources menu; a1-web sets `--a1-page-nav-top` to clear its 64px TopHeader. |
| 2026-06-19 | **Toolbar** prop forwarding + a1-web Sticky Actions / Accordion / Toolbar configurators. **Toolbar:** `ToolbarMenu` / `ToolbarGroup` / `ToolbarDivider` now spread `...rest` + merge `className` (matching Toggle/Button), so callers can attach `data-*` / `className` to a tool's root. Toolbar.jsx. **a1-web configurators (no other DS change):** Sticky Actions dropped the per-button label inputs; Accordion now exposes **`subtext`** + **`divider`**; the **Toolbar configurator is now a data-driven, canvas-selectable tool editor** — the bar is built from `config.tools` (toggle / button / menu / group / divider), **clicking a tool in the preview selects it** (capture handler maps the click to `data-tool-id`; selection stored in config so Preview ↔ panel stay in sync), and the panel edits the selected tool (type, label, icon, menu items / group options) with move-left/right reorder + add/remove. Snippet generated from the tool list. |
| 2026-06-19 | **Slider** compact size + a1-web Slider configurator. **Slider:** the **compact** track/thumb step up one notch (track `base-spacing-6`→`8`, thumb `16`→`20`) so the smallest slider stays grabbable (labels unchanged; this matches the default track/thumb — sizes still differ by label scale). slider.css. **a1-web configurator (no DS change):** detents are now edited with the **DefinitionList tabs pattern** (tab per detent + per-item editor + Add) instead of accordions; **valuePosition / Show value / Disabled** are folded into one **Value bubble** toolbar (position icons + toggle icons). Field-family Size density picker also now labels the selected density (`DensityChoice` → `labelMode="selected"`, across all field-family configurators). |
| 2026-06-19 | **SegmentedControl** fixes + a1-web Heading/Switch configurators. **SegmentedControl:** `fullWidth` now fills its container (added `inline-size: 100%` — `display: flex` alone left it content-width inside a centering parent); the segment **icon is one step larger** (`1.25em`); the **md inline padding steps down** (`segmentPaddingInline` 0.75rem → 0.5rem token, md-only since sm/lg have own tokens — run `npm run build:tokens && npm run build:html-css`); **sm** tightens the icon↔label gap. segmented.css + segmented.json. **a1-web configurators (no DS change):** Heading's "edit-in-preview / markdown" guidance is gated behind the **Helper text** toggle (new `ConfigHelp` gate in configKit); Switch's **Size** is the density icon picker, **Label position** an icon pair, and **Checked/Disabled** a single `FieldState` "State" toolbar. |
| 2026-06-19 | **Autocomplete** grouped/icon options + a1-web icon picker. Autocomplete options gained **`icon`** (a Material Symbols glyph beside the option / chip / selected value) and **`group`** (a **sticky category heading** before each group; results ordered by each group's first appearance), plus **`maxVisible`** (cap rendered options for huge lists — excess hidden behind a `moreText` "keep typing" footer). Backward compatible (no `group`/`icon`/`maxVisible` → unchanged). Autocomplete.jsx/.css/.d.ts + `Grouped options + icons` and `Grouped + capped` stories. **a1-web:** the configurator **`IconSelect`** is now built on the grouped Autocomplete — all 2106 Material Symbols as filterable options **grouped by Material category** (with the glyph shown per row), capped at 200 with a keep-typing footer; replaces the old giant native `SelectField`. The AI-find-icon button is unchanged. |
| 2026-06-19 | **Grid** + **Accordion** + a1-web Layout & Display configurators. **Grid:** `gap="none"` (→ 0, via the shared `resolveSpacing` convention; added to `GapKey`) and responsive **8-column** classes (`.a1-grid--{xs…xl}-8`) so `columns={{…:8}}` works at any breakpoint. Grid.jsx/.d.ts/grid.css + Column Counts / Gap Scale stories. **Accordion:** `divider` accordions now add a `padding-block-start` (scaled to `size`) to the open panel so the first item doesn't touch the divider line. accordion.css. **a1-web configurators (no DS change):** Section Surface bar now has **Inverse** as an inline `ToolbarToggle` (divider-separated; the standalone Inverse toggle removed); Stack folds the **Wrap** toggle into the Direction bar (single-value mode only; `ResponsiveControl` children now get a 3rd `bp` arg, `null` in single mode); Card **Icon display** is None-first with the standard `block` (circle-slash) none icon and **defaults to none**, and **Bare** moved into the Variant bar as a divider-separated toggle; Grid configurator gains 6/8/12 columns + a none gap. |
| 2026-06-19 | Toolbar: `ToolbarGroup` gained **`labelMode`** (`"all"` default \| `"selected"`) — `"selected"` shows the label only on the currently selected option; the rest render icon/swatch-only (a `"none"`/empty value falls back to the standard none icon) while keeping an `aria-label`. For swatch/variant pickers where only the chosen option needs naming. Toolbar.jsx/.d.ts + `Label on selected only` story. **a1-web configurators:** applied to Section **Surface**/**Gradient** and Button/IconButton **Variant** (variant now shows an icon per option — emphasis ramp `star`/`star_half`/`star_outline` + `delete`/`check_circle` — with the label only on the selected one). Also added a shared **Helper text** toggle to every configurator: a footer Switch (off by default) provides `ConfigHelpContext`; shared kit controls (`Choice`/`ConfigSlider`/`DensityChoice`/`FieldState`/`ResponsiveControl`) accept a `helper` string rendered as a muted summary beneath the control only when the toggle is on (`WithHelp`). a1-web + Toolbar only. |
| 2026-06-18 | Added **BottomSheet** (React only): a fixed bottom panel with **no scrim** (separation via upward shadow), a **drag handle** that resizes between detents (collapsed → expanded viewport-height fractions), internal content scroll, no footer, and an in-flow spacer so page content scrolls clear of the collapsed footprint. **xs + sm only**. `system/tokens/component/bottom-sheet.json`, `components/bottom-sheet/` (jsx/css/d.ts/stories), exported from the index, registered in the Overlay table. Run `npm run build:tokens && npm run build:html-css`. |
| 2026-06-18 | PageLayout: fixed the **viewport-height + SideNav** layout at md and below. The `--viewport-height .a1-page-layout__sidebar .a1-side-nav { position: relative }` rule was unscoped, so it overrode the SideNav's md-down **fixed-overlay** behaviour at every breakpoint — the sidebar stayed in flow and its ~280px slot kept reserving space, so the main column never expanded when the nav collapsed. Scoped that override to **`@media (--bp-lg-up)`**; at md-down the SideNav is a fixed overlay again, the `:has(.a1-side-nav)` slot collapses to 0, and `__main` fills the freed width. Affects the a1-web editor / theme editor (any `viewportHeight` PageLayout). page-layout.css only. |
| 2026-06-18 | **Z-index & layering** documented in `project-foundations.md` (new "Z-index and Layering" section): two mechanisms (the native `<dialog showModal>` **top layer** vs. the numeric **z-index scale** — Sticky 100 / Pinned 200 / Popover 1000 / Modal 1100 / Toast 1200), an audit of every component's current value, and the rule that anything which must sit **above a modal** (menus, context menus, tooltips, toasts) must live in the **top layer** (Popover API or portal into the dialog), since `z-index` can't cross the top layer. Interim fix: **Autocomplete** now portals its listbox into the nearest open `<dialog>` (else `<body>`) so the menu isn't painted behind a modal Dialog. |
| 2026-06-18 | Autocomplete: when opened with a value already selected, the menu now **highlights and scrolls to the selected option** (`block: 'nearest'`) instead of starting at the top — single-select jumps to the value, multi-select to the first selected. Active option stays scrolled into view during arrow navigation. `Autocomplete.jsx` (two effects: select-on-open + scroll-active). |
| 2026-06-18 | Autocomplete: added **`variant="color"`** — renders a colour **swatch** beside each option, multi-select chip, and the selected value (a per-option `swatch` colour, or the option `value` in colour mode). Pairs with `allowCreate` to make a "pick a colour from the palette, or type a hex" field. `Autocomplete.jsx`/`.css` (`.a1-autocomplete__swatch`, `--color`)/`.d.ts` (option `swatch?`, `variant`) + `Color variant` story. Used by the a1-web Theme editor's type-detail **Text colours** controls to pick from the theme's ramp. |
| 2026-06-18 | Autocomplete: the listbox dropdown now **portals to `document.body`** and is positioned `fixed` under the control (tracking it through ancestor scroll/resize), so it **escapes any clipping ancestor** — previously an Autocomplete inside an `Accordion` (or any `overflow:hidden`/scroll container) had its menu cut off. No API change; outside-click now also ignores clicks within the portaled list. `Autocomplete.jsx` (createPortal + `useLayoutEffect` position) + `autocomplete.css` (`.a1-autocomplete__listbox--floating`). |
| 2026-06-18 | Added **Aperture** theme (`.a1-theme-aperture`): modern/minimal photography-portfolio look — near-monochrome graphite accent on clean whites (Apple + Audi inspired), refined Apple-blue info + Audi-red error, small radii; **Pinyon Script** (script) display, **Playfair Display** (serif) headings, **Manrope** (elegant sans) body. `system/themes/aperture/` (theme.json + tokens/typography.json); registered in a1-web theme switcher + Google Fonts; run `npm run build:tokens`. |
| 2026-06-18 | Autocomplete: **multi-select** now shows a **checkbox** on each option (matching CheckboxGroup exactly — box size, border, radius, info fill, SVG tick) and the menu **stays open** until dismissed (selecting toggles; selected options stay listed, checked). Styling aligned to the **field family**: `surface-field` background, `border-strong` border, size-based radius (md/sm/lg), the **required** treatment (info border + leading accent) and **comfortable** required `MessageBadge`, and a label gap before the badge. Autocomplete.jsx/css. Also: Tabs `.a1-tab-list--scrollable` pinned `overflow-y: hidden` (overflow-x alone made the browser compute overflow-y to auto → a stray vertical scroll on horizontal tab strips). a1-web: Tabs and Autocomplete **configurators** now manage child items with the DefinitionList tabs pattern (tabs + per-item editor + canvas-click sync). |
| 2026-06-18 | Figure: added a **placeholder** for missing/broken images — `placeholder` (boolean, default true) renders a tokenized diagonal-stripe pattern + centered `placeholderIcon` (default `image`) when `src` is empty or fails to load (e.g. a deleted library image), instead of a broken `<img>`. Respects `aspectRatio`/`size`, 4:3 fallback box. Figure.jsx/css/d.ts + `Placeholder pattern` story. |
| 2026-06-18 | DataTable: added column **`type="image"`** — renders a small thumbnail from a URL string or `{ src, alt }` (empty → dashed placeholder); `.a1-data-table__thumb` CSS (~2.5rem, ~2rem compact). Used by the a1-web image library **table view** (Grid/Table toggle), which uses the table's built-in `selectable` + `onDeleteSelected` for **row selection + bulk delete**. DataTable.jsx/css + components.md. |
| 2026-06-18 | Added **Autocomplete** (React only): a filtering combobox in single- or multi-select (`multiple`) mode, with optional create (`allowCreate` + `onCreate`) for values not in the list. Field-family sizing/label/hint/error; ARIA combobox/listbox + full keyboard nav. `packages/react/src/components/autocomplete/` (jsx/css/d.ts/stories), exported from the index. Registered in the Inputs table. Used by the a1-web image library for category tagging. |
| 2026-06-18 | SegmentedControl: **completed the `size` variants** (the `sm`/`lg` classes had no CSS — `sm` now uses tighter padding + a font size one step down (`body-xs`), `lg` roomier padding + `body-md`; added `segmentPaddingBlock/InlineLg` tokens) and added **`labelMode`** (`"all"` default \| `"selected"` — only the selected segment shows its label, the rest render icon-only via `ariaLabel`/`label`; options without an icon always show their label). Used by the a1-web editor aside SegmentedControl. SegmentedControl.jsx/css/d.ts + `Label on selected only` story + size/labelMode argTypes. Also: a1-web SegmentedControl configurator now manages options with the **DefinitionList tabs pattern** (tabs + per-item editor + sm Add button) instead of accordions. |
| 2026-06-18 | a1-web: **Image library** — upload/manage images locally and use them in Figures. New **Image library** editor page (`image-library/ImageLibraryView.jsx`; route `image-library`, in the Editor menu + Projects home): upload (button/drag-drop), a per-image **context menu** (Rename / Custom crop / Delete), a **Custom crop** (Figure crop tool → default crop stored on the image via `setImageCrop`; thumbnails render the crop and Figures start from it, overridable), and a **View toolbar** (density + square/natural thumbnails). Images are downscaled and stored as Blobs in **IndexedDB** (`lib/imageLibrary.ts`). The **Figure configurator** gains a `photo_library` picker (`detail/ImageLibraryDialog.jsx`) that stores `a1img://<id>` as `src` (and applies the image's stored crop); an `ImageLibraryProvider` (`editor/ImageLibraryContext.jsx`, wrapping the app) + `pageRenderer` resolve the ref to a local `object:` URL at render time, keeping page JSON to a tiny id. Device-local (lives in this browser). No design-system change — Figure is unchanged. a1-web only. |
| 2026-06-18 | a1-web: **"Make with AI" entry point** — the project sidebar's **Add page** footer (Pages tab) is now a **SplitButton** whose **Make with AI** menu item adds + opens a page and lands the editor on the **AI tab with the prompt focused**. New `aiComposePageId` flag in `main.jsx` → `EditorPage`/`EditorAsidePanel` (`composeWithAi` switches to the AI tab on mount then calls `onAiComposeConsumed`) → `EditorChatPanel` (`requestFocus` one-shot focuses the instruction textarea via a forwarded ref). Full-width split button via `.a1-web-add-split`. a1-web only — no design-system change. |
| 2026-06-18 | a1-web: **Navigation Cards can link to a project page** — the Card configurator's navigation `href` control is now the shared project-scoped **`PageLinkField`** (same as Button/Link/IconButton) instead of a plain URL text field, so a `variant="navigation"` card can target another page in the project (degrades to a "Link URL" field on the standalone detail page). No design-system change: Card already renders `variant="navigation"` + `href` as an `<a>`, and the renderer already wires `/?page=` hrefs to intra-prototype navigation for any node. card.jsx configurator only. |
| 2026-06-18 | Banner: added **`variant="calendar"`** — an event/date callout that replaces the status icon with a tokenized date block (new `date` prop: `Date` \| ISO string \| `{ month, day }`), adds an `eyebrow` overline above a larger `title`, and keeps `action`/`onDismiss`. Neutral defaults the date block to the action colour; `status` still tints it. Month names stay sentence case (no uppercasing). `role="group"` (not `alert`) for calendar. Banner.jsx/css/d.ts + `Calendar` / `CalendarWithLink` / `CalendarStatuses` stories + a1-web configurator (Variant gains Calendar; eyebrow + Date month/day fields via FieldRow; icon controls hidden for calendar). React only (Banner has no Pure/Native impl). |
| 2026-06-18 | a1-web: **AI page editor (editor chat)** — a new **AI** tab in the editor aside (`editor/EditorChatPanel.jsx`) where you describe a change to the whole page and Claude applies it. `lib/aiPage.ts` sends the live page JSON + the `a1-agent-brief.md` spec as context and gets back `{ message, page }` (full updated `PageDefinition`); the page is committed to the editor history as one undoable step (`onApplyDefinition` threaded `EditorAsidePanel → EditorPage` → `history.commit`). Multi-turn (keeps instruction→reply summaries; re-sends the live page each turn). Browser-side Anthropic SDK, shared localStorage API key, `claude-opus-4-8`, streamed + adaptive thinking. Mirrors the AI image/icon tools. a1-web only — no design-system component/token changes. |
| 2026-06-18 | a1-web: **Project shared layout** — per-project editable chrome document (`__layout__`, `projects/projectLayout.ts`): a `TopHeader` + page-content **`Outlet`** + footer, edited on the canvas (`EditorPage` `documentKind="layout"`, auto-saves via `saveProjectLayout`), opened from All pages → "Shared layout". New editor-only **`Outlet`** type/component; `combinePageIntoLayout` composes a page into the layout (replaces the Outlet, injects auto nav/logo). Standalone prototype renders pages inside the shared layout. `Project.meta` added. Staged: editor Preview-tab/edit-canvas composition, project-settings panel, PageLayout-shell editing. a1-web only. |
| 2026-06-18 | a1-web: **DataTable renders real supplied data** in the editor (`EditorDataTable` renders the real `DataTable` from the node's `columns`/`rows` when present, else the sample Preview), and **import consistency lint** (`projectStore.lintDefinition` → non-blocking warnings in Upload JSON: per-page TopHeader in a project, Section missing contentWidth / zero or mismatched neighbor padding, sibling Cards not in a Grid, card Figure missing/invalid `aspectRatio`). New rules `section-primary-content-width` (section.yaml), `cards-in-grid` + `card-image-aspect-ratio` (card.yaml); `a1-agent-brief.md` consistency rules + corrected DataTable/Figure prop notes. a1-web + docs/rules only. |
| 2026-06-18 | a1-web: **Upload project from JSON** (`projects/ProjectImportDialog.jsx` + `projectStore` `validateProjectImport`/`importProjectJson`) — paste a project bundle or page definition into an editable `Code` block on the Projects page; live validation blocks on parse/structure errors, warns on unknown component types; on import creates + opens a new project preserving page hierarchy. Added **`packages/react/ai/a1-agent-brief.md`** as a self-contained page-definition and project-bundle reference for zero-context agents. Its registry snapshot is maintained alongside `page-definition-standard.md` and the code-owned registry. a1-web + docs only. |
| 2026-06-17 | a1-web Patterns: **blank-area Slots + nested patterns + inline per-property locks**. (1) **Slot** — a new editor-only `Slot` component (constrained drop zone): a dashed accent-bordered Section with a centred label while empty, transparent children when filled; configurable **label** + **allowed components** + **allowed patterns** + **min/max item count**, enforced when adding/dropping **into** it (`EditorPage` `slotRejection` + a `handleNodeDelete` min guard: disallowed types blocked, a full slot rejects adds, deleting below min blocked). When the Add target is a slot, the **Add panel filters** components/patterns to only what's allowed (and shows "full") via `slotFilter`. Registered in `pageTypes`/`componentRegistry`/renderer `CONTAINER_TYPES`/catalog (Layout "Blank area") + `EditorPropsPanel` `SlotEditorControls` bridge. (2) **Nested patterns preserved** — "Create pattern from selection" keeps nested instances as `PatternRef` (`extractPatternSource`); "Detach pattern" stops at nested instances (`detachPatternMeta`). (3) **Inline per-property lock icons** in pattern authoring (`configLock.jsx` `Lockable` authoring mode + `EditorPropsPanel` `onSetLock`) replace the per-prop switch list (`PatternLockControls` now only has Lock component / Lock text content); locked Sliders/ResponsiveControls now show the overlay. a1-web only — no design-system component/token changes. |
| 2026-06-17 | a1-web Editor **Add panel**: now derives **category order / names / component order / labels / icons** from the source of truth (`pages/components/data.js` `componentCategories`) instead of the catalog's own grouping, so it always matches the Components section (new components auto-slot in; empty categories like Overlay are hidden). `componentCatalog.ts` is now just the addable-node pool, resolved by id via new `CATALOG_ENTRIES_BY_ID` / `catalogIdForType` exports. Added a **view toolbar**: icon **grid/list** toggle + **Group by category** toggle (off → single alphabetical list). a1-web only. |
| 2026-06-17 | a1-web Editor: **7 more components addable** — Slider, Tabs, Toolbar, Field Row, Page Nav, Tree Menu, Data Table. Added to `editor/pageTypes.ts` + `editor/componentRegistry.ts` + `editor/componentCatalog.ts` + config bridges in `editor/EditorPropsPanel.jsx` (reusing each detail `Controls`). Slider/Page Nav render real components with real props; Field Row is a real container (added to renderer `CONTAINER_TYPES`); Tabs/Toolbar/Tree Menu/Data Table render via thin **editor adapters** (new `editor/editorComponents.jsx`) reusing the detail `Preview` (node props carry the configurator config). No design-system component/token changes. Deferred per request: Inline, Inline Editable, Side Nav, Page Layout, Dialog, Menu, Context Menu, Snackbar, Notification. |
| 2026-06-17 | a1-web: **canonical component icons** — every component now has one icon on `componentCategories[].components[].icon` in `apps/a1-web/src/pages/components/data.js` (exported as `COMPONENT_ICONS`); editor Add-panel catalog icons aligned to it; `system/icons/icon-usage.md` gained a Component Icons table. The source of truth for the icon representing each component everywhere. |
| 2026-06-17 | Toolbar: added rule **`toolbar-surface-contrast`** (`system/rules/toolbar.yaml`) — a Toolbar must sit on a **panel or page** surface, never a **raised** surface (the bar uses the raised token, so it has no contrast on a raised background). Wired into the a1-web Rules tab; noted in the Toolbar registry notes. a1-web: the Button page's platform switch was renamed **"View as" → "Codebase"** and moved into the **Configure tab**; and a **Responsive preview** control (Fit / XS–XL) was added to the **Display** tab for every component — it renders the preview in an iframe sized to the breakpoint (real nested viewport) and scales it to fit the panel (`ResponsivePreviewFrame`). |
| 2026-06-17 | Added **SplitButton** (React only): a primary action joined to a caret toggle that opens a `Menu` of related actions; composes `Button` + `Menu` (`actions`, `variant`/`size`/`icon`/`loading`/`disabled`, `menuLabel`/`toggleLabel`); CSS-variable architecture over button/divider tokens (shared pill outline + hairline divider). SplitButton.jsx/css/d.ts + stories; exported from the package; surfaced via the a1-web Button **Split (menu)** toggle (not a separate menu page). |
| 2026-06-17 | Button: a long label now **wraps** to multiple lines (centered) and grows the button height — removed the fixed `height`/`max-height` and `white-space: nowrap`, kept `min-height` for the single-line target, added `overflow-wrap: anywhere`. |
| 2026-06-17 | Section: a Section with **no `align`** now flows as **block** (block children full-width, natural-width content unstretched) instead of a stretch grid; a `gap` without alignment becomes margins between direct children, while an aligned Section (or `height="hero"`) keeps grid + grid-gap. section.css only. |
| 2026-06-17 | a1-web **component page "View as"** (Button first): a detail module that exports `viewAsModes` gets a React/Native/Pure toolbar at the top of the Configure tab; `Preview`/`Controls`/`Snippet` receive the active `viewAs` to switch the rendered props and the code (React JSX / React Native `onPress` / Pure HTML) and hide inapplicable props. a1-web Page Layout configurator also rebuilt to render a realistic real `PageLayout` with all props configurable (slot toggles, sidebar/aside placement, sticky/viewport). |
| 2026-06-17 | a1-web component menu **re-categorised** (`apps/a1-web/src/pages/components/data.js` `componentCategories` + the menu hierarchy table here): order is now **Layout & Display → Typography → Actions & Controls → Navigation → Inputs → Feedback & Messaging → Media and iconography → Overlay → Data**. **Layout**→**Layout & Display**, **Actions**→**Actions & Controls**, **Feedback**→**Feedback & Messaging**. **Tabs**, **Link**, and **Accordion** moved into **Actions & Controls**; **Figure** into **Media and iconography**; the **Disclosure** menu category was retired. **Cluster** was removed from the menu and registry metadata (PACKAGE_COVERAGE / COMPONENT_STATUS / COMPONENT_RELATED) — the deprecated component code remains. The per-package coverage tables below stay grouped by implementation family; the menu hierarchy table is the source of truth for category membership. |
| 2026-06-16 | Card: padding reduced **20px → 16px** — `base.space.card.padding` now resolves to `{base.spacing.16}` (was `{base.spacing.20}`), so `--component-card-padding` is `1rem`. Token + html-css outputs rebuilt. |
| 2026-06-16 | Slider: added **`bubbleLabel`** (an alternate value-bubble label — node or `(value, detent) => node`; visual only, `aria-valuetext` unchanged; falls back to current bubble content), and the value bubble is now **anchored by progress** (and its caret tracks the thumb) so it never spills past the control edges at the extremes. Slider.jsx/css/d.ts + `Bubble label (long size names)` / `Bubble stays within the track at the ends` stories. |
| 2026-06-16 | a1-web configurator controls refresh: all single-select controls render as `Toolbar` groups (shared `Choice` helper in `detail/configKit.jsx`) and t-shirt size/spacing scales (xs–xl, sm/md/lg, gap/content-width) as compact subtle **`ConfigSlider`**s (empty/`none` stop → `--`; the value bubble spells the size out, e.g. "Small", via `bubbleLabel` while the detent reads "Sm"). New `configKit` helpers: **`DensityChoice`** (field Size as an icon-only `density_small`/`medium`/`large` picker) and **`FieldState`** (a Toolbar of toggle buttons for boolean state). Button/IconButton/Link icon controls are a None/Left/Right icon group; Button Full width/Loading/Disabled, Code Options, field Required/Disabled/Read only, and Choice Group Required/Hide indicator/Inline icon/Multiple are Toolbar toggle groups; Slider detents are Accordions; Inline Editable "Display as" is an icon group. a1-web only — no design-system component/token changes beyond Slider above. |
| 2026-06-16 | a1-web Heading configurator: rebuilt on the Section pattern — single-select controls (Heading mark / Underline style / Type / Element / Size / Color-with-swatches) are `Toolbar` groups, Margin is a subtle `Slider`, and the editable-preview's on-selection heading-mark toolbar uses a floating `<Toolbar overlay>` with `ToolbarToggle`s. An **Advanced** accordion (with summary subtext) holds Align / Margin / Text wrap (toggle). |
| 2026-06-16 | Accordion: added **`subtext`** (secondary info in the trigger, shown only while collapsed — a glanceable summary — and hidden when open; truncates) and **`divider`** (optional bottom border to separate stacked accordions). Also **removed the trigger border-radius** and **reduced the inline padding** (sm/md/lg = `--base-spacing-4`/`8`/`12`). Accordion.jsx/css/d.ts + `Subtext` story. The a1-web Section configurator uses `subtext` to summarise each group's applied properties when collapsed, and `divider` between groups. (React only — not yet in the Pure `<details>` version.) |
| 2026-06-16 | Toolbar: a **radio group item** (`aria-checked`, can't be deselected) no longer shows a hover/active change — clicking it does nothing, so the feedback was misleading. A **toggle** (`aria-pressed`, can be turned off) keeps its hover feedback. a1-web **Section configurator** reorganised into **accordions**: a **Sizing** accordion (open) = Padding / Gap / Content width **subtle Sliders**, a **Background** accordion (open) = Inverse + Surface + Gradient (+ position), a **Border** accordion = Border size (Slider) + style/variant/sides + Radius, and an **Advanced** accordion = Height + Alignment + Element. Surface / Gradient / Border variant show colour **swatches**; gradient-position arrows point inward; Border style stays labelled (Material Symbols has no distinct solid/dashed/dotted glyphs). a1-web config side panel no longer scrolls horizontally (`overflow-x: clip`). |
| 2026-06-16 | IconButton: added **`sm`** size (24×24px target — WCAG 2.2 AA minimum — with a 16px icon; `.a1-icon-button--small` / Pure `.a1-icon-button-small`) and **enlarged the `lg` icon** to 32px (`--base-spacing-32`; was an undersized 20px). IconButton.jsx/css/d.ts + Pure + stories + a1-web configurator. |
| 2026-06-16 | Slider: added **`variant="subtle"`** — shows the selection (fill, thumb, active detent) in neutrals (`--semantic-color-text-default`) instead of the action colour, via a new `--a1-slider-active-color` var. Slider.jsx/css/d.ts + `Subtle variant` story + a1-web configurator; used by the Section configurator's Content width slider. |
| 2026-06-16 | Slider: added field-family **`size`** ("compact" / "default" / "comfortable") and a **visible `label`** (rendered above the control via `htmlFor`, sized per `size`) so a Slider matches form fields; track heights bumped (compact `--base-spacing-6`, default `8`, comfortable `12`). Slider.jsx/css/d.ts + `Sizes` story + a1-web configurator (Size control, visible label). a1-web Section configurator: **Content width** uses a compact Slider with a detent per size; **gradient position** arrows now point **inward** (toward the centre); the Display tab align default for **Slider** is `none` (it's full-width, classified Flexible). |
| 2026-06-16 | Section: added **`borderSides`** prop (`"all"` default \| array of `top`/`right`/`bottom`/`left`) — partial borders via per-side `border-width` CSS vars (`.a1-section--border-sided` + `.a1-section--border-side-*`); requires `borderSize`. Section.jsx/css/d.ts + `Border sides` story + a1-web configurator (All button + per-side toggles) + editor Section bridge. |
| 2026-06-16 | Toolbar: visual refresh — **unselected** tools use muted text, the **selected** tool uses default text on a darker neutral fill (`text-default` 16% mix) instead of the solid action background (more subtle, less contrasty). Added a **`swatch`** option (any CSS color, bordered so a light swatch doesn't bleed into the bar) on `ToolbarToggle` / `ToolbarButton` / `ToolbarGroup` options — used in the a1-web Section **Surface** control to preview surface colours. The `label` caption dropped to the new **`--semantic-font-size-body-3xs`** (0.5rem) token. New typography token `base.font.size.scale.25` (0.5rem) → `semantic.font.size.body.3xs`. |
| 2026-06-16 | a1-web Section configurator: single-select controls converted from `ChoiceGroup` to the **Toolbar** component — Element / Surface / Padding / Gap / Height / Gradient / Border size / Border style / Border variant / Radius as labelled `ToolbarGroup`s, Align as an icon group, Gradient position as the 3×3 icon grid; **Surface** shows colour swatches; **Content width** uses a `Slider` with a detent per size (`--`, XS…2XL); Inverse is now a `Toggle`. Configurator-only (no design-system change). |
| 2026-06-16 | a1-web Figure configurator: the **Custom (freeform) crop** moved off the canvas into a **dialog** — clicking "Custom" opens a `Dialog` showing the image with the freeform crop rectangle, the aspect-ratio presets below the image, and Apply/Cancel (draft state so Cancel discards). The canvas preview now always shows the result. `FigureCropTool` renders the ratio `Toolbar` below the stage (no longer an `overlay`). |
| 2026-06-16 | Toolbar: the `label` caption font size dropped one step (now `--semantic-font-size-body-2xs`, below the compact form-label size). a1-web Figure configurator reorg: Caption position moved directly under the "Caption screen-reader only" switch; crop controls grouped at the bottom (Aspect ratio → Crop grid when a ratio is set → a **Custom** button that toggles the freeform crop tool, replacing the "Custom" aspect-ratio option). |
| 2026-06-16 | Figure: added a **freeform `cropRect`** prop (`{ x, y, width, height }` fractions 0–1) — a non-destructive sub-rectangle crop rendered via a clipping wrapper (`.a1-figure__crop`) that scales/positions the image from CSS vars; the natural ratio is measured on load to size the box. Takes precedence over `aspectRatio`/`crop`; radius moves to the clip wrapper. Figure.jsx/css/d.ts + `FreeformCrop` story. (React only so far — not yet in Pure.) The a1-web configurator's **crop tool** was rebuilt as an in-preview editor (`FigureCropTool.jsx`): toggling "Crop image" turns the preview into a full-size cropper where the user draws a freeform rectangle (dimmed outside), refines it by dragging edges/corners, and constrains it to a standard ratio via a floating **overlay `Toolbar`**; the result is stored as `cropRect` metadata. Replaces the earlier focal-point crop control; editor Figure config bridge maps `cropRect`. |
| 2026-06-16 | a1-web Figure configurator: contextual controls — the **Align** group is hidden when `size` is Auto (alignment only applies once the figure has a constrained width), and **Caption position** is hidden when the caption is screen-reader-only (matching the existing Crop-hidden-unless-aspectRatio behavior). Preview + snippet omit the corresponding props in those states. |
| 2026-06-16 | a1-web component configurator: the **Display** Section alignment now defaults per component — `center` for natural-width (content-sized) components, none/full-width for flexible components (Paragraph, fields, tables) — and re-applies on navigation. Both remain overridable in the Display tab. |
| 2026-06-16 | Figure: added more standard **aspect ratios** — `4:3`, `3:2`, `2:3`, `3:4`, `9:16`, `21:9` (alongside `16:9`, `1:1`). React (`.a1-figure--ratio-*`) + Pure (`a1-pure.css` / `a1-base.css` `.a1-figure-ratio-*`) + Figure.d.ts + Storybook control + a1-web configurator. The a1-web Component-browser sidebar ("Component tree") was also rebuilt on the **TreeMenu** component (was `SideNav`), keeping search, active state, and SPA navigation. |
| 2026-06-16 | Toolbar: **overflow handling** — the bar now caps to `max-inline-size: 100%` and non-grid `ToolbarGroup`s wrap their own buttons, so tools wrap onto new rows instead of overflowing the container (no horizontal scroll). |
| 2026-06-16 | Toolbar: `showLabel` (Toggle/Button/Menu) and `showLabels` (Group) now accept a **responsive breakpoint object** (`{ xs?, sm?, md?, lg?, xl? }`) in addition to a boolean — labels can show/hide per breakpoint. Implemented with cascading per-breakpoint visibility classes on the label span (`a1-toolbar__label--show/hide-{bp}`) in `@media (--bp-*-up)`, mirroring Divider's responsive pattern; when a label can be hidden at any breakpoint the tool keeps an `aria-label` for a stable accessible name. `ToolbarShowLabel` type added; Storybook `Responsive labels` story added. |
| 2026-06-16 | Figure: **size scale** reworked — renamed `xxs`→`2xs`, added `3xs` (5rem), `xl` (50rem), `xxl` (60rem); full scale is now `3xs`/`2xs`/`xs`/`sm`/`md`/`lg`/`xl`/`xxl` (5/8/12/20/30/40/50/60rem), incl. center-align overrides. **align** gained `"none"` (now the default — normal flow; `start`/`center`/`end` are explicit, `start` adds `margin-inline-end:auto`). **radius**: removed the duplicative default — the image is square by default (the old base `var(--base-radius-container)` was an undefined token resolving to 0, identical to `radius="none"`), so the a1-web Radius control drops the redundant "Default" entry. The a1-web configurator also hides the **Crop** control unless an `aspectRatio` is set (crop has no effect on natural-ratio images). Figure.jsx/css/d.ts/stories + a1-web configurator + editor Figure config bridge updated. |
| 2026-06-16 | Toolbar: added `fullWidth` (boolean, default false — fills its container and grows the tools to share the width, dividers staying natural; default is `fit-content`) and an accessible-theme / high-contrast **border** (no boundary by default; `[data-theme='accessible']`, `@media (prefers-contrast: more)`, and `@media (forced-colors: active)` raise `--a1-toolbar-border-width` to `--component-divider-size-sm`). d.ts / Storybook (`Full width`) / a1-web configurator (Full width toggle) updated. |
| 2026-06-16 | Menu: fixed dropdown misalignment when the anchor sits inside a transformed/filtered ancestor (e.g. an `overlay` Toolbar positioned with a CSS `transform`, which becomes the containing block for the menu's `position: fixed`). `updatePosition` now measures where the menu actually landed and corrects by the containing-block offset so it stays anchored to the viewport. Affects `ToolbarMenu` opened inside an overlay toolbar. |
| 2026-06-16 | a1-web Figure configurator: the single-select controls now use the **Toolbar** component instead of `ChoiceGroup` — Radius / Size / Aspect ratio as labelled `ToolbarGroup`s (showLabels), Align / Caption position as icon `ToolbarGroup`s, and Crop as the 3×3 `ToolbarGroup columns={3}` grid; each wrapped in a `Toolbar label="…"`. Configurator-only (no design-system component/token change). |
| 2026-06-16 | Menu: the selected/active `MenuItem` (`--active`) now uses the **TreeMenu selected pattern** — a solid `--semantic-color-action-background` fill with `--semantic-color-action-foreground` text/icon (and hover/pressed/focus action states) — replacing the previous left-border + tinted-surface treatment, for a clearer highlight. Used by `ToolbarMenu`. |
| 2026-06-16 | Toolbar: added `label` (optional caption above the bar, styled to match the compact ChoiceGroup label; supplies the accessible name via `aria-labelledby`), `overlay` (floating, elevated surface — shadow + border — for a bar that hovers over content; consumer positions it), and the **`ToolbarMenu`** sub-component (button → dropdown `Menu` of choices: text-size t-shirt picker, list-type none/bulleted/numbered/checklist; caret + `aria-haspopup`/`aria-expanded`; active item marked). **Removed `ToolbarSelect`** (native select) in favour of `ToolbarMenu`. Stories + a1-web configurator updated (text-editor uses size/list menus; new Overlay example). |
| 2026-06-16 | Added **Toolbar** component (React only): compact container (`role="toolbar"`) grouping editing controls on a subtle surface, with sub-components `ToolbarToggle` (aria-pressed), `ToolbarButton`, `ToolbarGroup` (single-select radio-semantics button group; `columns` → grid like a 3×3 crop picker; icon-only or `showLabels`; clearly highlighted selection; auto "none" icon), `ToolbarMenu` (button → dropdown Menu), `ToolbarDivider`, and `TOOLBAR_NONE_ICON` (`block`). CSS-variable architecture (`--a1-toolbar-*`) over existing tokens (color-mix hover, `--component-divider-size-*` divider, focus ring). Storybook (text editor, menu, alignment, crop grid, labeled, overlay, toggles); a1-web Actions configurator (`toolbar: 'experimental'`); exported from index. Retains ChoiceGroup for full-size selectors. |
| 2026-06-16 | Slider: detent labels can carry an optional `icon` (Material Symbols name) shown in the label row instead of text (e.g. alignment icons); a1-web configurator gained an editable detent list (value / label / optional icon, add & remove). |
| 2026-06-16 | Added **Slider** component (React only): range input with optional **detents** (snap stops + optional labels like None/XS/SM/MD/LG), a value bubble that follows the thumb and flips above/below to stay in the viewport, continuous (`min`/`max`/`step`) or detent mode. Built on native `<input type="range">` for keyboard/focus/touch/form a11y; value announced via `aria-valuetext`. CSS variable architecture (`--a1-slider-*`) over existing tokens (no new token file). Storybook stories (Default, detents, labeled size control, value below, custom format, disabled); a1-web Actions configurator page; exported from package index; registered in components.md + a1-web data (`slider: 'experimental'`). |
| 2026-06-16 | Figure: added `xxs` to the `size` scale (`.a1-figure--xxs`, max-width 8rem — below `xs` at 12rem), including the center-align override. Figure.jsx/d.ts/stories ("Size scale") + a1-web configurator updated. |
| 2026-06-16 | Figure: added `aspectRatio` ("16:9" \| "1:1") and `crop` (center/top/bottom/left/right + 4 corners) props. `aspectRatio` locks the image to a fixed ratio cropped to fill via `object-fit: cover`; `crop` sets the focal point via `object-position` (CSS var `--a1-figure-crop`). React (`.a1-figure--ratio-*`, `.a1-figure--crop-*`) + Pure/base (`.a1-figure-ratio-*`, `.a1-figure-crop-*`); a1-web configurator + Storybook ("Aspect ratios", "Crop points") updated. No new tokens (aspect-ratio/object-position are structural). |
| 2026-06-16 | a1-web Editor **Projects**: the Editor opens on a Projects home; each project owns an isolated, hierarchical page tree (drag-and-drop sets the hierarchy; `level` 1–3 = nesting depth, capped at 3) edited from a tabbed sidebar (Pages tree + Layers component tree). Page metadata = title/icon/description/level. A `TopHeader` is auto-generated from the hierarchy (level-2/3 → dropdown menus) and rendered above every page in edit/preview/prototype. Links/Buttons target any page in the same project (project-scoped page-link selector). New `apps/a1-web/src/projects/` module (`projectStore.ts`, `projectNav.ts`, `ProjectsList.jsx`, `ProjectPagesPanel.jsx`, `ProjectWorkspaceSidebar.jsx`, `AllPagesView.jsx`); `EditorSidebar` now exports `ComponentTreePanel`; `PageMetadata` gained optional `icon`. No design-system component/token changes. |
| 2026-06-15 | Prop forwarding: MessageBadge, MessageEmptyState, Banner, Pagination, TopHeader, BottomDrawer, and Accordion now spread `...rest` (and merge `className`) onto their root element. These previously dropped unknown props, so the a1-web Editor's injected `data-editor-node` / `onClick` never reached the DOM and clicking them on the canvas didn't open the config panel. Fixes editor selection for those components. |
| 2026-06-15 | IconButton: added `as` prop (default `"button"`) + `href` — `as="a"` renders the icon button as a navigation link while keeping its styling; `disabled` maps to `aria-disabled` for anchors; IconButton.d.ts + Storybook `As link` story added; a1-web icon-button configurator gained a page-link selector (shared `PageLinkField` with Button/Link); editor renderer renders Button/IconButton with an href as anchors and wires intra-prototype navigation via onClick |
| 2026-06-15 | TreeMenu: added `draggable` prop (boolean, default false) and `onMove` callback (`{ draggedId, targetId, position }`) — enables drag-and-drop reordering and reparenting; drop-before/drop-after indicators use 2px accent line; drop-into shows tinted background + inset ring; collapsed branches auto-expand after 600 ms on hover; items cannot be dropped on themselves or descendants; HTML5 DnD API (no library); anchor label-btns have `draggable={false}` to suppress browser link-drag; CSS classes `a1-tree-menu__row--dragging`, `--drop-before`, `--drop-into`, `--drop-after`; Storybook `Draggable` story with live state management; EditorSidebar wired with `draggable` + `onMove`; EditorPage processes moves via `pendingMove` command prop using `extractFromNodes` + `insertInNodes` helpers; history entry double-click also triggers rename |
| 2026-06-15 | Added ContextMenu component (React only): right-click menu portaled to document.body; controlled via open/x/y; items support type=item/divider/group, icon, shortcut, variant=destructive, active state; keyboard Arrow/Home/End/Enter/Escape; clamped to viewport; Storybook stories (Default, EditorStyle, StaticPreview); a1-web detail page with file and editor examples; exported from package index; registered in components.md and a1-web data |
| 2026-06-15 | TreeMenu: added onHoverChange prop — fires on mouseenter/mouseleave of each label button; used by the Editor sidebar to mirror hover outlines onto the canvas; EditorSidebar handleTreeHover uses DOM querySelector to set data-editor-hover attr; handleTreeSelect adds scrollIntoView after selection |
| 2026-06-15 | Editor: left click on canvas selects innermost element; right click opens ContextMenu (replaces inline EditorNodePicker) with element picker + Delete action; Delete triggers delete node in EditorPage with Snackbar undo; Snackbar.d.ts added to type the component |
| 2026-06-15 | TreeMenu: separated expand/collapse from selection — toggle icon (`add_box` / `indeterminate_check_box`) only expands/collapses; label click only selects; Enter/Space selects only (no longer also toggles); keyboard Arrow Right/Left still control expand/collapse; added `showExpandControls` prop (renders "Expand all" / "Collapse all" buttons above the tree using `unfold_more` / `unfold_less` icons); reduced item `padding-block` from `base-spacing-4` to `base-spacing-2`; CSS rewritten to use `.a1-tree-menu__row` wrapper + `.a1-tree-menu__toggle` + `.a1-tree-menu__label-btn` structure; Storybook `WithExpandControls` story added; a1-web configurator gained `showExpandControls` switch |
| 2026-06-15 | Added TreeMenu component (React only): hierarchical navigation tree, unlimited nesting, icons, `href` support, full ARIA tree role + keyboard nav (Arrow/Home/End/Enter/Space), roving tabindex, expand/collapse with CSS grid animation, solid action-background selected state; component tokens in `system/tokens/component/tree-menu.json`; Storybook stories (FileExplorer, DocumentNavigation, NoIcons, DeepNesting); a1-web detail page with item editor and code snippet |
| 2026-06-15 | Code: added `editable` prop (boolean, default false) — renders `<textarea>` instead of `<pre><code>` for live editing; `onChangeValue` callback delivers the current string on each keystroke; `copyCode` copies from live textarea value when combined; CSS class `a1-code-block--editable` on wrapper, `a1-code-block__textarea` on the textarea; Storybook `Editable` story added |
| 2026-06-15 | Stack now preserves internal CSS variables when callers pass `style`, preventing direction/gap/align/justify/wrap from being reset by custom inline styles |
| 2026-06-15 | Added a1-web Stack configurator for `as`, direction, semantic/numeric gap, align, justify, and wrap |
| 2026-06-15 | Card now fills its containing inline size by default so container-query previews and grid tracks do not collapse to content width; a1-web Card configurator gained element and navigation href controls |
| 2026-06-15 | Section `height="screen"` now sets `align-content: start` so grid rows do not stretch extra viewport height and `gap` remains token-accurate |
| 2026-06-15 | Section gained opt-in border props (`borderSize`, `borderStyle`, `borderVariant`) using Divider tokens plus tokenized `radius`; Storybook, property docs, and a1-web controls updated |
| 2026-06-15 | Added a1-web Section configurator as bare display: sample Heading + Paragraph inside the Section, controls for Section props, and no outer Display tab controls |
| 2026-06-15 | Snackbar simplified to one default visual style; status variants removed from React, Storybook, a1-web controls, and current docs; `inverse` removed as a public prop; default surface fixed to dark neutral background with light foreground |
| 2026-06-15 | Added a1-web feedback configurators for Notification, Snackbar, Status Bar, Circular Progress, and Step Tracker |
| 2026-06-15 | Split the old Feedback "Message" registry entry into Badge (`MessageBadge` in React, `Badge` in Native) and Empty State (`MessageEmptyState` in React, `EmptyState` in Native); a1-web now exposes separate Badge and Empty State component pages/configurators |
| 2026-06-15 | Comfortable required marker: field family + Checkbox/Radio/Choice group legends now render the `MessageBadge` required indicator at `size="sm"` with `icon={null}` (small badge, no icon) |
| 2026-06-15 | FieldRow added to registry (React, Inputs category) and a1-web (component card, packages, status, related, configurator); Fieldset configurator added to a1-web (simple grouped example, no FieldRow) |
| 2026-06-11 | Added StickyActions component (React only): fixed bottom action bar for flows and wizards; contentWidth prop mirrors Section's max-width values; children stacked with gap; safe-area-inset-bottom for notch devices; z-index 150; rule: do not combine with BottomDrawer; rule: always nest a ButtonContainer; Onboarding template updated to use StickyActions |
| 2026-06-11 | Added BottomDrawer component (React + Pure): fixed bottom nav bar, max 5 items, icon-above-label stacked layout, badge support, shares --a1-nav-stacked-* vars with TopHeader; navIconPosition="hidden" added to TopHeader |
| 2026-06-11 | Added semantic.color.surface.card token; Card (React + Pure) now uses --semantic-color-surface-card; dark mode and Fresh theme overrides applied across all build pipelines |
| 2026-06-10 | Removed SystemBanner component; replaced by `<Banner variant="system">` — all internal usages updated; `packages/react/src/components/system-banner/` deleted |
| 2026-06-10 | DataTable: added `notices` prop (`{ content: ReactNode; afterRow?: number }[]`) — full-width no-padding rows inserted at arbitrary positions in tbody; `afterRow` is 0-based (default 0 = before all rows); multiple entries at the same position stack in order; CSS class `a1-data-table__notice-cell` (padding:0); `a1-data-table__notice-row` (zebra-safe) |
| 2026-06-10 | NumberField unit fix: unit shifts with value via JS-tracked `--a1-field-number-width` + `field-sizing:content`; `.a1-field__unit` is now `flex:1` + clickable so clicking right of value focuses input |
| 2026-06-10 | Section `gap="xl"` (40px) + Stack `gap="xl"` added; `semantic.spacing.gap.xl` token added; CSS class `a1-section--gap-xl`; Stack resolves "xl" to `var(--semantic-spacing-gap-xl)` |
| 2026-06-14 | ChoiceGroup: added `iconOnly` per-option boolean to `ChoiceOption` — visually hides label/subtext for that tile (sr-only), centers the tile's icon; composable with group-level `hideIndicator` and `iconOnly`; CSS class `a1-choice-item--icon-only` |
| 2026-06-10 | IconButton: added `size` prop ("md" default, "lg" = 3.5rem height / 2.5rem icon, matches Button large); CSS class `a1-icon-button--large` (React), `a1-icon-button-large` (Pure/Base) |
| 2026-06-10 | ButtonContainer: added `fillButtons` prop — Button children fill remaining space (`flex:1`); IconButton children stay at natural square width; always row layout; CSS class `a1-button-container--fill-buttons` (React), `a1-button-container-fill-buttons` (Base) |
| 2026-06-10 | Added StepTracker component (React + Pure): non-interactive step position indicator, pill + dot display, left/center/right/full alignment |
| 2026-06-10 | Added Fresh theme: sky-blue accent/info (#0A62DC), teal-green success (#209261), red error (#D11720), amber warn (#D19317), Nunito body/heading (ExtraBold headings), Baskerville display, 10px large radius, mint gradient background; added base.spacing.48 token |
| 2026-06-09 | Icon: added `size` prop (xs/sm/md/lg/xl/jumbo/xJumbo) and `color` prop (muted/accent/inverse/success/error/warn/info); size classes auto-set opsz for optimal rendering; size/color classes added to a1-base.css and a1-pure.css |
| 2026-06-09 | Dialog: added `status` prop (success/error/warn/info/neutral) rendering a full-bleed hero band with status icon; `onClose` now optional (omit to hide close button); removed A11y example stories |
| 2026-06-09 | Added CircularProgress component (React + Pure): SVG-based ring progress indicator, xs/sm/md/lg sizes, indeterminate spin, custom inner content, xs places children after ring, conic-gradient Pure implementation |
| 2026-06-09 | Added DefinitionList component (React + Pure): semantic label/value pairs, row/column direction, sm/md/lg sizes, auto/fixed responsive label widths, Heading-based value typography, optional copy value buttons, and usage rules |
| 2026-06-09 | Calendar: added `selectable` prop (opt-in date selection); out-of-range dates fully blocked when minDate/maxDate set; React package bumped to 0.4.1 |
| 2026-06-09 | StatusBar pause button → `<Button size="sm" variant="secondary">` with "Pause"/"Play" text; added system/labels/status-bar.json with translations (es/fr/de/pt/ja/zh/ar); React package bumped to 0.4.0 |
| 2026-06-09 | Prop consistency pass (v0.4.0): Tab status "warning"→"warn"; Section alignment→align; Notification variant→status, "default"→"neutral"; Heading/Paragraph align now accepts "start"/"end"; TextField/TextareaField/SelectField/Fieldset labelPosition "side"→"before"; Grid gap adds "xs"; SegmentedControl.d.ts created with size typed; DataTable density→size (omit=auto) |
| 2026-06-08 | Fixed StatusBar RTL indeterminate animation (physical left/right anchor + animation-direction: reverse); added pause/resume button appearing after 3 s; label accepts ReactNode |
| 2026-06-08 | Added StatusBar component (React only): value/max fill, indeterminate loading, three sizes, four label positions (RTL-aware), accessible-theme track border |
| 2026-06-08 | Added Choice Group inlineIcon prop: places tile icon left of label/subtext instead of above |
| 2026-06-08 | Added no-nested-cards rule to Card guidance in Layout notes |
| 2026-06-08 | Added Snackbar notes: props (open, variant, position, inverse, actionLabel/onAction, onClose), accessibility behavior (role/aria-live auto-set by variant), and non-modal usage guidance |
| 2026-06-08 | Added rules from all system/rules YAML files: Typography (sentence case, no uppercase, no font-weight on states), Button (vs-link, single-primary, tertiary icon+verb, icon a11y label), SideNav (sticky desktop, mobile overlay, internal scroll), Badge/Message (standard variants, status-only, color+text, use sparingly), PageLayout (no-gap rule) |
| 2026-06-08 | Added Card vs Section guidance to Layout notes; added card-not-for-page-sections rule to system/rules/card.yaml |
| 2026-06-08 | Revised Choice Group: removed direction prop; columns now accepts number or responsive breakpoint object; size uses compact/default/comfortable convention; indicator in top-start corner |
| 2026-06-08 | Added Choice Group component (React only); single/multi-select, size/gap/columns props, icon + subtext per option |
| 2026-06-07 | Added React Code component and system labels for copy-code affordance |
| 2026-06-07 | Added Calendar to a1-web component docs (Data category, experimental status) |
| 2026-06-07 | Added Calendar component (React only); paginated variant, RTL/locale support, todayButton |
| 2026-06-07 | Simplified tables to 3 packages (React, Native, Pure); moved registry to packages/react/ai/ |
| 2026-06-05 | Added a1-web Components menu hierarchy and selected category icons |
| 2026-06-04 | Initial registry created from current package state |
